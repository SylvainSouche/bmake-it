---
schema_version: 2
id: 0f87-6ab6-6543-b0d4
alias: fetch-import-source-req-v2
type: req
status: confirmed
origin: correcting an absolute 'no delegation' claim that fetch-build-req now makes conditional
created: 2026-09-25
rel_decided_by: [0f87-6ab6-6520-f718]
rel_refines: [0f87-6ab5-8a85-7d76]
rel_supersedes: [0f87-6ab6-4e12-3429]
generator: model.py/2.4.0
checksum: f81252043b15
---

# IMPORT=fetch:<name> downloads a distfile (FETCH_URL=), verifies it against a committed distinfo (SHA-256), extracts it to a gitignored work directory, and applies any FETCH_PATCHES= in order; by default the result is compiled as this module's own SRCS through the existing compile pipeline, with no delegation to the upstream project's own build system -- unless FETCH_BUILD= is set, in which case the upstream project's own build system builds it instead (fetch-build-req)

Supersedes 0f87-6ab6-4e12-3429 (IMPORT=fetch:<name> downloads a distfile (FETCH_URL=, tried in order until one succeeds), verifies it against a committed per-module distinfo (SHA-256), extracts it to a gitignored, non-target-keyed work directory, applies any FETCH_PATCHES= in order, and compiles the result as this module's own SRCS through the existing compile pipeline -- no delegation to the upstream project's own build system).

Carries forward what remained valid and states what changed.
