---
schema_version: 2
id: 0f87-6aaa-6a5e-378f
alias: build-workspace-aggregation-req-v2
type: req
status: confirmed
origin: corrected 2026-09-16 -- the first version wrongly coupled REPORT= to continue-vs-abort
created: 2026-09-16
rel_decided_by: [0f87-6aaa-6a4c-3725]
rel_supersedes: [0f87-6aaa-665e-fded]
generator: model.py/2.4.0
checksum: fe699705a50f
---

# A framework/workspace-level bmake all always captures each module's build output to a log file (<module>/build/<KEY>/build.log, tee'd so console output is unchanged). One module's build failure does not stop every other module from still being attempted, unless FAIL_FAST=yes (fail-fast-independent-of-report); REPORT=yes generates a workspace-level build-report/index.html dashboard listing every framework/module with PASS/FAIL and a link to its build.log, mirroring the test dashboard

Supersedes 0f87-6aaa-665e-fded (A framework/workspace-level bmake all always captures each module's build output to a log file (<module>/build/<KEY>/build.log, tee'd so console output is unchanged) -- REPORT unset keeps today's fail-fast-on-first-error abort; REPORT=yes instead continues past a failing module so every module gets a chance to build, and afterward generates a workspace-level build-report/index.html dashboard (build-report/index.html) listing every framework/module with PASS/FAIL and a link to its build.log, mirroring the test dashboard).

Carries forward what remained valid and states what changed.
