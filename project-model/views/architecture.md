# Architecture

Generated from `project-model/` · schema_version 2 · 294 objects in model
Sources: 87 REQ
Generated 2026-09-07. Regenerate rather than edit — hand edits are lost and drift silently.

## Requirements

### $OS_ARCH identifies OS+architecture only; debug/release build variant does not get its own directory in the build tree

`REQ-variant-out-of-scope-for-dirtree` · status **confirmed** · id `6f38-6a89-bb05-134c`

$OS_ARCH (used throughout the build/$OS_ARCH/ tree, see REQ-build-output-tree-structure) encodes only operating system and CPU architecture (e.g. linux-x86_64, macosx-arm64), never build variant. Debug and release/optimized builds of the same target share the same build/$OS_ARCH/ subtree and therefore overwrite each other -- there is no side-by-side coexistence of variants the way there is for different OS/arch targets. Resolves the "variant" open follow-up on REQ-build-output-tree-structure.

Refines: `build-output-tree-structure`

### A dedicated make target builds native OS deployment packages (pkg/deb/rpm/msi/...) from a target's build output; packaging is not part of the default build

`REQ-packaging-native-formats-separate-target` · status **confirmed** · id `6f38-6a89-baec-0f81`

Packaging/installation-package creation is invoked explicitly (a separate make target from the default build), and produces native, per-OS package formats rather than (or in addition to) a generic archive: macOS .pkg, Linux .deb and .rpm, Windows-side .msi (scope TBD given Cygwin/WSL-only Windows support), and likely a *BSD ports-style package. Exact target name, per-OS toolchain used to build each package format (e.g. dpkg-deb, rpmbuild, pkgbuild), and whether *BSD ports packaging is in scope are still open follow-ups.

### A distinct build step copies each module's linked artifacts into its framework's and the workspace's build/$OS_ARCH/{bin,lib}, after module-level compilation and linking

`REQ-aggregation-step-copies-outputs` · status **confirmed** · id `6f38-6a89-baec-3053`

Build proceeds in (at least) two steps: (1) compile and link each module into its own module/build/$OS_ARCH/ (obj/ for intermediates, plus the linked artifact); (2) copy the resulting linked artifacts up into the consolidated framework/build/$OS_ARCH/{bin,lib} and workspace/build/$OS_ARCH/{bin,lib} directories. Copy, not symlink, is the mechanism. Refines REQ-build-output-tree-structure by resolving its open 'aggregation mechanism' question.

Refines: `build-output-tree-structure`

### A framework contains one or more modules

`REQ-framework-contains-modules` · status **confirmed** · id `6f38-6a88-1836-a7fa`

Framework is the unit used to organize source code above the module level. Each framework contains one or multiple modules.

### A framework's makefile declares PREREQS=fw1 fw2 ... , a space-delimited ORDERED list of frameworks it may link_with; all prereqs are implicitly public (no per-entry visibility qualifier); an incorrectly ordered list should produce a build error

`REQ-framework-prereqs-macro-req` · status **confirmed** · id `6f38-6a8a-0550-cadc`

Refines REQ-framework-prereqs and REQ-uniform-makefile-filename-req with the concrete macro: PREREQS=<fw1> <fw2> ... in the framework's makefile. The list is ordered, and the order is meaningful enough that misordering it should be flagged as a build error. Open follow-up, explicitly deferred by the user: the precise semantics of misordered and how this relates to detecting dependency cycles between frameworks are NOT yet decided -- do not assume a specific algorithm here.

Refines: `framework-prereqs`, `uniform-makefile-filename-req`

### A framework's makefile declares a prereqs list of other frameworks it depends on; prereq headers are accessible when building, and generated binaries can be linked against prereqs

`REQ-framework-prereqs` · status **confirmed** · id `6f38-6a88-1836-f948`

Framework-level makefile defines `prereqs`: a list of frameworks that this framework uses. Effect: while building this framework, the public headers (include directory) of every listed prereq framework are accessible, and binaries generated by this framework may be linked against binaries generated by the prereq frameworks.

### A frameworks modules are discovered by scanning for *.m-suffixed immediate subdirectories; a workspaces frameworks are discovered by scanning immediate subdirectories -- neither is explicitly listed in the parent makefile

`REQ-modules-and-frameworks-auto-discovered-req` · status **confirmed** · id `6f38-6a8a-dba2-dcc2`

Extends the auto-discovery philosophy of REQ-module-src-auto-discovery up two levels. At framework level: mk.framework.mk scans the frameworks own directory for immediate subdirectories matching *.m (REQ-module-dir-naming-convention) and treats each as a module to build. At workspace level: mk.workspace.mk scans the workspace root for immediate subdirectories and treats each as a framework -- exact discrimination signal for what counts as a framework subdirectory (vs. e.g. build/) is a related open follow-up, see REQ-framework-discovery-signal-req.

Refines: `module-src-auto-discovery`, `workspace-build-sequence-req`

### A library module declares SHLIB_MAJOR (and optionally SHLIB_MINOR); mk.lib.mk emits libfoo.so.MAJOR[.MINOR] with an ELF symlink chain on FreeBSD/Linux, and libfoo.MAJOR.dylib with a dylib symlink chain PLUS linker -compatibility_version/-current_version flags on macOS

`REQ-shlib-major-minor-cross-platform-emission-req` · status **confirmed** · id `6f38-6a8a-d9a5-6177`

Closes the open follow-up on REQ-versioned-naming-bsd-lib-mk-req. Module makefile macros: SHLIB_MAJOR=<n> (required for a versioned shared library), SHLIB_MINOR=<n> (optional, finer versioning). mk.lib.mk (REQ-uniform-makefile-filename-req) emits per platform: FreeBSD/Linux -- libfoo.so.MAJOR[.MINOR] as the real linked artifact, libfoo.so symlinked to it, identical to real bsd.lib.mk behavior; macOS -- libfoo.MAJOR.dylib as the real linked artifact, libfoo.dylib symlinked to it, and the linker invoked with -compatibility_version MAJOR -current_version MAJOR.MINOR (or similar) so the version is also embedded as native Mach-O metadata, not just expressed via filename. This is the single mechanism used regardless of phase-1 vs. later targets.

Refines: `versioned-naming-bsd-lib-mk-req`

### A module is the basic link unit: it generates an executable, a static library, or a shared library

`REQ-module-is-basic-link-unit` · status **confirmed** · id `6f38-6a88-1836-352a`

Module is the smallest unit that produces a link output. Every module generates exactly one of: executable, static library, shared library.

### A module makefile declares INCL=<header1> <header2> ... to name which of its generated headers are promoted to public (copied up to the framework build/<KEY>/include/ on module build); unlisted generated headers remain module-private

`REQ-incl-macro-promotes-generated-headers-req` · status **confirmed** · id `6f38-6a8a-9b1e-1229`

Closes the open declaration-macro follow-up on REQ-generated-headers-in-build-tree-req. INCL=<header1> <header2> ... in a module makefile (mk.prog.mk or mk.lib.mk context) lists, by filename, which of that modules generated headers should be promoted to public. On module build, exactly those files are copied from module/build/<KEY>/include/ up into framework/build/<KEY>/include/ (the generated-only framework-level directory, per REQ-generated-headers-in-build-tree-req). Any generated header not named in INCL= stays private to the module.

Refines: `generated-headers-in-build-tree-req`

### A module supplies additional compile or link options by appending directly to bsd makes own native CFLAGS/CXXFLAGS/LDFLAGS/YFLAGS/LFLAGS variables in its makefile (e.g. CFLAGS+=-DFOO); no project-specific LOCAL_*FLAGS wrapper macros exist

`REQ-flags-reuse-bsd-native-vars-req-v2` · status **confirmed** · id `0f87-6a95-270b-85c4`

Supersedes REQ-flags-reuse-bsd-native-vars-req (unchanged content, re-pointed ancestor only). Closes REQ-module-makefile-fields' original additional compilation or link options provision with a concrete mechanism: standard bsd make variables (CFLAGS, CXXFLAGS, LDFLAGS, YFLAGS, LFLAGS for the in-scope languages per REQ-supported-languages) are used unchanged, extended via ordinary += in a module makefile. This is consistent with the project's general principle of reusing bsd-make-native variables wherever they already exist, applied identically to how TARGET=/TARGET_ARCH=, DESTDIR/PREFIX, and DEBUG_FLAGS/optimization flags were handled.

Why this exists as a supersession rather than an edit: this REQ genuinely refines REQ-module-build-macros-req -- that REQ specifies which macros a module's makefile holds (PROG=/LIB=/LIB_SHARED=/LINK_WITH=), and this REQ is the more precise statement that the makefile can *also* append to existing bsd-native macros on top of declaring those project-specific ones. The `refines` edge itself was correct; only its target needed to move once the v1->v2 migration marked REQ-module-build-macros-req superseded without updating objects that refined it (DEC-view-drift-refines-repoint-dec). REQ-module-build-macros-req was split across two successors -- REQ-module-prog-or-lib-macro-req (the PROG=/LIB= declaration facet this REQ refines) and REQ-libs-macro-link-only-headers-via-prereqs-req (the unrelated LIBS=/LINK_WITH= facet) -- so `model.py supersede` correctly refused to guess and the refines edge was added explicitly via `model.py link`. No content changed.

Refines: `module-prog-or-lib-macro-req`

### A module's directory is named NAME.m, where NAME is the module/library/program name unless overridden

`REQ-module-dir-naming-convention` · status **confirmed** · id `6f38-6a88-1836-bfe8`

Module directories follow the convention `NAME.m`. If no explicit library or program name is given elsewhere in the module's makefile, NAME (the part before `.m`) is used as the output name.

### A module's makefile declares either PROG=<name> (executable, via .include <mk.prog.mk>) or LIB=<name> (library, via .include <mk.lib.mk>)

`REQ-module-prog-or-lib-macro-req` · status **confirmed** · id `0f87-6a94-678e-e640`

Restates the part of REQ-module-build-macros-req that survived its later corrections unchanged: the module-level macro selecting executable-vs-library output. PROG=<name> selects an executable build (.include <mk.prog.mk>). LIB=<name> selects a library build (.include <mk.lib.mk>); whether the result is shared or static is controlled by LIB_SHARED=YES|NO, per REQ-lib-shared-is-new-not-reused-req. Linking against other libraries is controlled separately by LIBS=, per REQ-libs-macro-link-only-headers-via-prereqs-req, which superseded this REQ's original LINK_WITH= design. Default naming when PROG=/LIB= is left unset is covered by REQ-prog-lib-default-to-module-name-req.

### A module/framework resource source tree uses share/common/, share/<os>/, and share/<os>_<arch>/ subdirectories, most specific wins, with no toolchain axis

`REQ-share-dir-fallback-hierarchy` · status **confirmed** · id `6f38-6a89-bccb-d5b8`

Resolves the open naming and fallback-granularity follow-ups on REQ-resource-file-model. The resource source directory is named share/. Within it, three specificity levels are recognized: share/common/ (files applicable regardless of target), share/<os>/ (files applicable to any architecture of a given OS, e.g. share/freebsd/), and share/<os>_<arch>/ (files applicable only to one specific OS+arch pairing, e.g. share/freebsd_x64/), used only when that level of override is actually needed by the resource in question. When staging resources into build/<KEY>/share/ (or similar, per REQ-resource-file-model) for a given target, the most specific applicable variant wins over less specific ones for files present at multiple levels. Unlike the compiled-artifact target key (OS+arch+toolchain, see REQ-target-key-includes-toolchain), the resource fallback key is OS+arch only -- toolchain does not affect resource variant selection.

Refines: `resource-file-model`

### A workspace is a set of frameworks

`REQ-workspace-is-set-of-frameworks` · status **confirmed** · id `6f38-6a88-1836-fe77`

Workspace is the top-level organizational unit. A workspace contains some number of frameworks.

### A workspace may declare an ordered list of parent workspaces; the build looks up missing elements in parent workspaces in declaration order

`REQ-workspace-parent-search-order` · status **confirmed** · id `6f38-6a88-1836-74c9`

A workspace can have a set of 'parent' workspaces. When the build needs an element (framework/module/artifact) not found in the current workspace, it looks it up in the listed parent workspaces, trying them in the order they were declared. Open question: exact declaration syntax/file for the parent list is not yet specified. CONFIRMED (2026-08-22): this is the ONLY prerequisite-access mechanism -- purely dynamic search, no physical-copy mode, no dedicated setup command (see DEC-6f38-6a89-bd4b-3e07 and NREQ-6f38-6a89-bd4b-c4ce).

### A workspace's makefile declares PARENT_WS=<path1> <path2> ... , an ordered, space-delimited list of ABSOLUTE paths to parent workspace roots; relative paths are not used

`REQ-workspace-parent-macro-absolute-req` · status **confirmed** · id `6f38-6a8a-0550-79c4`

Refines REQ-workspace-parent-search-order and REQ-uniform-makefile-filename-req with the concrete macro: PARENT_WS=<abs-path-1> <abs-path-2> ... in the workspace's makefile, preserving declaration order for the fallback search. Only absolute paths are supported -- relative paths were explicitly considered and rejected as too fragile/confusing.

Refines: `workspace-parent-search-order`, `uniform-makefile-filename-req`

### A workspace-level distrib/<KEY>/ directory, parallel to build/<KEY>/ and keyed the same way, is where packaging targets (make port, make pkg, ...) stage an install-prefix-shaped tree before building the actual per-format package artifact from it

`REQ-distrib-dir-for-packaging-staging-req-v2` · status **confirmed** · id `0f87-6a94-696d-a08d`

Supersedes REQ-distrib-dir-for-packaging-staging-req (unchanged content, re-pointed ancestor only). Introduces distrib/<KEY>/ at workspace root (same target-key scheme as build/<KEY>/, REQ-target-key-omit-defaults-req), populated by the packaging targets (REQ-packaging-per-format-targets-req) as a staging area mirroring an install-prefix layout (bin/lib/share, matching REQ-build-output-mirrors-prefix-layout). The actual package artifact for each format is built from this staged tree, not directly from build/<KEY>/ and not from an arbitrary temporary directory. Exact mechanism for populating distrib/<KEY>/ (reusing the install machinery with DESTDIR=distrib/<KEY>, or a related-but-distinct copy step) is an open follow-up, not yet nailed down.

Why this exists as a supersession rather than an edit: the original refined REQ-destdir-prefix-confirmed-req, which the v1->v2 migration correctly marked superseded (by REQ-prefix-is-new-not-reused-req) without updating objects that refined it (DEC-view-drift-refines-repoint-dec). No content changed; only the refines target moved to the live successor.

Refines: `packaging-per-format-targets-req`, `build-output-mirrors-prefix-layout`, `prefix-is-new-not-reused-req`

### All source files found in a module's src directory are automatically taken as compilation inputs; the makefile does not need to enumerate them

`REQ-module-src-auto-discovery` · status **confirmed** · id `6f38-6a88-1836-74a5`

A module has a `src` directory. Every source file physically present in that directory is treated as compilation input by the make command -- there is no need to list files in a SRCS-style variable in the makefile.

### Any build-system mechanism that shells out to a nested make invocation to perform discovery or dependency-order analysis (module discovery, framework discovery, LIBS=/PREREQS= topological ordering) must guard against self-reentrant and concurrent re-invocation: the invoked make binary must be resolved to an absolute path, never a bare name looked up via PATH, and any cache file the mechanism writes must not be unconditionally deleted-and-regenerated in a way that races a concurrent or nested invocation of the same discovery for the same directory

`REQ-nested-invocation-reentrancy-guard-req` · status **confirmed** · id `0f87-6a95-6a53-9e2e`

Concretely, for the current implementation: gen-mod-order.sh and gen-fw-order.sh must resolve their own $BMAKE to an absolute path before invoking it (mirroring the fix already applied to tests/harness/run-tests.sh's wrapper generation), and mk.framework.mk/mk.workspace.mk's discovery steps must not blindly 'rm -f <cache> && regenerate' on every parse -- at minimum this should be skipped when a fresh cache already exists for the current parse, and ideally guarded against concurrent regeneration (e.g. a lock file, or writing to a temp file and renaming atomically into place).

Why this is a REQ and not just a bugfix note: the failure mode is a property of the mechanism (shelling out to a nested instance of the same build tool for discovery/analysis), and it recurs everywhere that pattern is used -- it was found once, in module/framework discovery, but the same shape of risk applies to any future dependency-analysis mechanism built the same way (e.g. the deferred order-cache-auto-triggered-req 'make order' mechanism). Recording it as a requirement on the mechanism class means future implementations of that class are checked against it, not just the two scripts that happened to be examined.

### Before linking a module, mk.prog.mk/mk.lib.mk verify each LIBS= entry's build artifact actually exists; a missing one produces an explicit 'dependency not yet built' error naming the specific missing library, rather than an opaque linker failure

`REQ-link-time-not-yet-built-guard-req` · status **confirmed** · id `6f38-6a91-03ec-fc4e`

Adds a fail-fast guard to the link step of REQ-fused-build-per-module-full-dependency-order-req: immediately before invoking the linker, for each name in LIBS=, check that the corresponding library artifact exists (module-level build/<KEY>/lib/ for a sibling module, or framework-level build/<KEY>/lib/ for a PREREQS-resolved framework library). If any are missing, fail with an explicit error identifying which dependency is missing and why (e.g. 'cannot link hello: prerequisite library greet (from LIBS=) has not been built yet'), rather than proceeding to invoke the linker and surfacing its own generic missing-library error. This should essentially never fire in normal operation, since REQ-order-target-blank-run-req already sequences builds correctly -- its value is as a clear diagnostic when something has genuinely gone wrong (stale cache, manual/partial build, bug), rather than a cryptic '-lgreet: No such file or directory' from the linker itself.

Refines: `fused-build-per-module-full-dependency-order-req`

### Both modules and frameworks may carry resource files (non-source runtime data); resources support per-target variants with a fallback hierarchy, and are staged into build/<KEY>/ alongside obj/bin/lib, copied upward module to framework to workspace the same way as linked artifacts

`REQ-resource-file-model` · status **confirmed** · id `6f38-6a89-bc24-f8a0`

A module and/or a framework may each have their own resource-file source location (naming of that source directory -- e.g. res/, resources/ -- not yet decided, tentative working name 'res/' pending a naming pass alongside src/, include/, local/include/). Resources are staged into the target's build/<KEY>/ tree (see REQ-build-output-tree-structure) in a dedicated subdirectory alongside obj/, bin/, lib/ (tentatively 'res/' or 'share/', name TBD), and follow the same copy-upward aggregation from module level to framework level to workspace level as linked artifacts (REQ-aggregation-step-copies-outputs) -- no separate 'runtime view' materialization step. Resources support per-target variants: a resource can have a generic version plus more specific overrides, with the most specific applicable variant winning at staging time (fallback chain, analogous to real mkmk's specifics_$OS hierarchy, see OBS-mkmk-runtime-view-and-resources). Open follow-ups: (a) exact resource source-directory name(s) at module and framework level, (b) exact variant fallback levels for our compound target key -- e.g. does specificity go full-target > OS-only > generic, or some other granularity -- not yet decided.

Refines: `build-output-tree-structure`, `aggregation-step-copies-outputs`

### Build order for both module-level (LIBS=) and framework-level (PREREQS=) dependency graphs is generated via: query each unit dependency macro in isolation (bmake -f <dir>/makefile -V <MACRO>), topologically sort (Kahn), ignore out-of-scope references, emit a generated SUBDIR= .mk fragment consumed by bsd.subdir.mk

`REQ-dep-order-generation-mechanism-req` · status **confirmed** · id `6f38-6a8e-7e12-a30d`

Formalizes the working prototype (gen-mod-order.sh) into the design: implements the previously-undesigned recursion-ordering piece of REQ-fused-build-per-module-full-dependency-order-req and REQ-framework-build-order-follows-prereqs-graph-req. At module level: mk.framework.mk queries each *.m directorys LIBS= via , builds the dependency graph (edges only among locally-discovered modules; references to anything else -- e.g. a PREREQS-resolved framework library -- are silently ignored for this graph, since that dependency is satisfied by REQ-headers-resolved-from-source-req/REQ-aggregation-step-copies-outputs at the framework level, not module-level ordering), topologically sorts it (Kahn algorithm), and writes the result to a generated SUBDIR= .mk fragment consumed via .include before .include <bsd.subdir.mk>. The identical mechanism applies one level up at workspace level, querying each frameworks PREREQS= instead of LIBS=. This generated fragment is the same artifact referred to (at a policy level, without this concrete mechanism) in REQ-prereqs-resolution-cached-like-depend-req -- exact caching/staleness-detection triggers for regenerating it remain deferred per that REQ and OBS-cache-mechanism-needs-empirical-check, but the GENERATION algorithm itself is now confirmed and no longer an open design question. Cycle handling: a genuine cycle causes the generation step to fail loudly with a clear diagnostic (confirmed via prototype), rather than the vaguer "surfaces as a header-not-found error" behavior anticipated by REQ-prereqs-misorder-no-validation-req for framework-level PREREQS misordering -- this is arguably a strict improvement (an explicit cycle error is clearer than an indirect compile failure) and does not contradict that REQ, which only committed to NOT implementing dedicated validation, not to suppressing a natural byproduct of the sort algorithm itself.

Refines: `framework-build-order-follows-prereqs-graph-req`, `fused-build-per-module-full-dependency-order-req`, `prereqs-resolution-cached-like-depend-req`

### Build outputs live under a build/$OS_ARCH/ directory at module, framework, and workspace levels; obj/ holds per-module intermediate object files, bin/ and lib/ hold final linked artifacts, keyed per target to support cross-compiling multiple targets from the same source tree

`REQ-build-output-tree-structure` · status **confirmed** · id `6f38-6a89-ba51-340e`

At the module level: module/build/$OS_ARCH/obj/ holds intermediate compiled object files (.o) for that module's sources -- pure build-time scratch space, safe to delete/clean independently per target. At the framework level: framework/build/$OS_ARCH/{bin,lib}/ holds the linked artifacts (executables, shared/static libraries) produced by the framework's modules for that target, consolidated so other frameworks/modules can find them at a predictable path without reaching into per-module scratch directories. At the workspace level: workspace/build/$OS_ARCH/{bin,lib}/ similarly consolidates across all frameworks in the workspace. Because every level's build tree is keyed by $OS_ARCH, several different targets can be built from the same checked-out source tree at the same time without clobbering each other. Supersedes REQ-build-obj-os-arch-output (module-only obj/<OS_ARCH>). Open follow-ups: (1) exact aggregation mechanism from module to framework to workspace level (copy vs symlink vs direct build-to-location), (2) whether $OS_ARCH also needs to encode build variant (debug/release) as a separate axis, (3) packaging/distribution format is still undecided and not yet part of this structure.

### Compiler-native -MD dependency tracking is confirmed to work automatically ONLY when using FreeBSD's own actual bsd.sys.mk (e.g. on a real FreeBSD host's native /usr/share/mk); it is confirmed ABSENT from NetBSD's own bundled mk set. Whether MacPorts' bsdmake (a phase-1 target host) includes it is unverified and must be checked on a real Mac before relying on it there

`REQ-md-dependency-tracking-freebsd-specific-req` · status **confirmed** · id `6f38-6a91-a2cf-5c3c`

Scopes REQ-compiler-native-dependency-tracking-req correctly: its confirmed-working claim applies specifically to FreeBSD hosts using FreeBSDs own native share/mk unchanged, not to the BSD-make family in general. Confirmed ABSENT in NetBSDs own bundled mk-netbsd set (zero -MD wiring, zero rebuild on header change). For macOS (a phase-1 host, REQ-phase1-concrete-targets), whether MacPorts own bsdmake port includes equivalent -MD wiring in its own bsd.sys.mk is UNKNOWN -- genuinely untestable without a real Mac (same gap as Tier 1.3 in spec/60-bsd-make-validation-plan.md, now understood to cover this too, not just Mach-O dylib versioning). Design consequence: mk.prog.mk/mk.lib.mk should not assume -MD-based dependency tracking is inherited for free on every phase-1 target. If MacPorts bsdmake turns out to lack it, the fallback is straightforward -- mk.prog.mk/mk.lib.mk can add -MD -MF<generated-path> to CFLAGS/CXXFLAGS themselves and .include the resulting per-object files, replicating what FreeBSDs bsd.sys.mk already does, rather than needing mkdep or any more complex mechanism.

Refines: `compiler-native-dependency-tracking-req`

### Copy-up collision handling is diff-aware: identical files copy silently; differing files that are a genuine (non-overlay) collision emit a warning but the build still proceeds (overwrite, not hard error); the deliberate share/ specificity overlay is exempted from warnings entirely since it is expected, by-design behavior

`REQ-copy-up-collision-diff-aware-req` · status **confirmed** · id `6f38-6a8d-1de3-1237`

Refines the copy-up mechanism (REQ-aggregation-step-copies-outputs, module/framework/workspace bin/lib copy-up) and the share/ overlay (REQ-share-overlay-copy-order) with a unified collision policy: byte-identical source/destination -> silent, no message. Differing content, NOT explained by the deliberate share/ specificity cascade (common -> os -> os_arch) -> emit a warning identifying the colliding artifact and its sources, but do NOT stop the build -- the copy proceeds (last-copied-wins, following whatever order the aggregation naturally runs in). Differing content THAT IS explained by the share/ specificity cascade (a more-specific variant intentionally overwriting a less-specific one) -> no warning at all, this is expected behavior, not a collision. Exact warning message wording/format not yet specified -- open follow-up for implementation.

Refines: `aggregation-step-copies-outputs`, `share-overlay-copy-order`

### Cross-compilation also works within the same OS across architectures (e.g. Linux/arm64 host targeting Linux/amd64), not just across OS boundaries

`REQ-cross-compile-same-os-cross-arch-req` · status **confirmed** · id `0f87-6a95-c754-c645`

Same mechanism as the cross-OS directions (clang --target/--sysroot), but the amd64 Linux sysroot needed two things the aarch64 one didn't surface: (1) usr/lib/gcc/<multiarch>/<ver> for crtbeginS.o/crtendS.o/-lgcc -- omitted by mistake when only usr/lib/<multiarch> was copied, caught by a link failure, not a silent wrong build; (2) the /lib64/ld-linux-x86-64.so.2 interpreter symlink must be copied as a dereferenced regular file, not a symlink -- its target is an absolute host path (/lib/x86_64-linux-gnu/...) that only resolves inside the sysroot-building container, not under --sysroot as lld sees it. Verified end-to-end: built on an arm64 Linux container, the resulting x86-64 ELF was then actually executed (not just file(1)-checked) via --platform linux/amd64 qemu-user emulation, printing the expected output. See tests/harness/run-matrix.sh (cross-linux-arm64-amd64 job) and mk/mk.toolchain.llvm.mk.

Refines: `target-arch-toolchain-selection-is-custom-req-v4`

### Cross-compilation to NetBSD is supported as a fourth cross target, alongside freebsd/linux/macos

`REQ-cross-compile-netbsd-target-req` · status **confirmed** · id `0f87-6a97-d20a-cb7c`

Same --target/--sysroot mechanism as the other three, with two NetBSD-specific quirks found empirically: (1) usr/include/machine is normally created by the NetBSD installer, not shipped in the raw comp.tar.xz release set -- must be created manually as a symlink to the arch-specific header subdir (e.g. usr/include/aarch64) or <sys/cdefs.h> fails to find <machine/cdefs.h>. (2) unlike freebsd/linux/macos, clang's NetBSD driver support (at least in the clang-14 this was found on) does not auto-add the sysroot's usr/lib to the linker search path from --sysroot alone -- ld.lld reports "unable to find library -lc" even though usr/lib/libc.so genuinely exists; needs an explicit -L${BMK_NETBSD_SYSROOT}/usr/lib. The sysroot itself is built from NetBSD's own freely-redistributable release sets (base.tar.xz + comp.tar.xz, the same official-OS-artifact pattern as FreeBSD's base.txz) -- no NetBSD host or VM needed to build it. Verified: genuine NetBSD ELF produced (file(1): 'for NetBSD 10.1', dynamically linked, interpreter /libexec/ld.elf_so). See mk/mk.toolchain.llvm.mk and tests/harness/fetch/build-sysroot-netbsd.sh.

Refines: `target-arch-toolchain-selection-is-custom-req-v4`

### Debug vs release build variant is controlled via real bsd makes own native flag variables (DEBUG_FLAGS for debug symbols, an optimization-level flags variable for release), not a new unified MODE= switch

`REQ-debug-release-via-native-bsd-flags-req` · status **confirmed** · id `6f38-6a8c-b939-38a7`

No new abstraction is introduced for debug/release selection, unlike TOOLCHAIN= (REQ-toolchain-cli-variable-req), because bsd make already provides this natively: DEBUG_FLAGS (conventionally -g) adds debug symbols, and an optimization-level flags variable (exact name TBD -- COPTFLAGS or CFLAGS-family, needs verification against real bsd.*.mk source) controls optimization. These are set/overridden directly on the make command line or in a module makefile, the same as any ordinary bsd make variable -- no MODE=debug|release wrapper. This is consistent with REQ-variant-out-of-scope-for-dirtree (debug/release never gets its own build/<KEY>/ subdirectory -- switching flags and rebuilding overwrites the existing target output in place). Exact optimization-flags variable name is an open follow-up pending empirical bsd.*.mk verification.

Refines: `variant-out-of-scope-for-dirtree`

### Each cross target's clang triple/sysroot layout must match that platform's real distro/vendor conventions, not a generic guess

`REQ-cross-compile-per-target-triple-quirks-req` · status **confirmed** · id `0f87-6a95-b3d4-d67d`

Concrete, empirically-verified per-target requirements: (1) freebsd triple is <arch>-unknown-freebsd. (2) linux triple is <arch>-linux-gnu with NO 'unknown' vendor component -- clang's multiarch header search needs the triple to match the distro's exact directory naming (Debian: aarch64-linux-gnu); the sysroot must also include the top-level ld-linux-*.so.1 symlink (lives in /usr/lib on merged-/usr distros like Debian, not physically in /lib despite /lib -> usr/lib), and must be extracted on a case-sensitive filesystem (Linux kernel headers collide on macOS APFS default). (3) macos triple is <arch>-apple-macos<version>, and needs an explicit -Wl,-platform_version,macos,<min>,<sdk> linker flag to work around a driver/linker split-version mismatch seen in some clang/lld distro packages (confirmed on Debian's clang-22/lld-22 against a modern Xcode 26 SDK). Verified end-to-end for all three: genuine FreeBSD ELF, genuine Mach-O arm64 executable, and a genuine Linux ELF that was actually executed on real Linux (not just file(1)-checked). See mk/mk.toolchain.llvm.mk and tests/harness/run-matrix.sh (cross-linux-freebsd, cross-linux-macos, cross-netbsd-linux jobs).

Refines: `target-arch-toolchain-selection-is-custom-req-v4`

### Each native packaging format has its own named make target (e.g. make port, make pkg, ...); there is no single generic 'make package' target that takes a format parameter

`REQ-packaging-per-format-targets-req` · status **confirmed** · id `6f38-6a8a-0550-b9ed`

Refines REQ-packaging-native-formats-separate-target: the packaging mechanism is one make target per format, not a unified target with a format argument. Confirmed target names so far: port (BSD ports-style package) and pkg (macOS-style package). Full mapping of target name to OS/format for every combination in REQ-os-label-set-req is still open (e.g. deb/rpm target names for Linux, msi handling for win/Cygwin-WSL).

Refines: `packaging-native-formats-separate-target`

### Every framework makefile must declare PREREQS= explicitly, even with no value, e.g. PREREQS= for a framework with no prerequisites

`REQ-prereqs-mandatory-even-empty-req` · status **confirmed** · id `6f38-6a8a-f6ee-940d`

Strengthens REQ-framework-prereqs-macro-req: PREREQS= is mandatory in every framework makefile, not merely present-if-needed. An empty declaration (PREREQS= with nothing after it) is valid and means the framework has no prerequisites. Serves double duty as the discovery signal for REQ-framework-discovery-signal-req.

Refines: `framework-prereqs-macro-req`

### For TOOLCHAIN=msvc specifically, a module's shared-library build (LIB_SHARED=YES, the default) must produce a usable import library (.lib) even when the source uses no __declspec(dllexport) annotations -- matching the zero-annotation-needed convention that already works for every other toolchain/target in this project. Exact mechanism (auto-generate a .def exporting every global symbol via dumpbin/nm-equivalent, require annotations project-wide with a portable macro, or something else) is an open follow-up, not yet decided

`REQ-msvc-shared-lib-export-req` · status **confirmed** · id `0f87-6a9a-b51c-b267`

Refines: `win-target-tiered-toolchain-req`

### Frameworks are built in a topological order derived from the workspace-wide PREREQS dependency graph: a framework only builds after all frameworks named in its PREREQS have completed building; behavior on an actual dependency cycle remains unspecified

`REQ-framework-build-order-follows-prereqs-graph-req` · status **confirmed** · id `6f38-6a8a-a40e-8342`

Refines REQ-workspace-build-sequence-req: step 2 (resolve PREREQS) does not just establish header search paths, it also determines framework BUILD ORDER for the whole workspace -- a topological order over the dependency graph formed by every frameworks PREREQS declarations. This is required because a dependent framework needs its prereqs build output to already exist: compiled libraries for linking (build/<KEY>/lib/) and any promoted public generated headers (build/<KEY>/include/, REQ-generated-headers-in-build-tree-req) -- hand-written headers impose no such ordering since they are read from source. Consistent with REQ-prereqs-misorder-no-validation-req, no cycle detection is implemented; an actual dependency cycle across frameworks has undefined behavior for now, deferred.

Refines: `framework-prereqs-macro-req`, `workspace-build-sequence-req`

### Generated binaries and libraries follow host OS naming conventions: program NAME.exe on Windows / NAME on *NIX and macOS; libraries prefixed lib on *NIX (not on Windows)

`REQ-output-naming-os-conventions` · status **confirmed** · id `6f38-6a88-1836-5571`

Program output naming: `NAME.exe` on Windows, plain `NAME` on *NIX and macOS. Library output naming follows OS convention: `lib` prefix used on *NIX (implicitly also macOS, per 'os standards are followed'), no `lib` prefix on Windows. Exact extension conventions per platform/type (.a/.so/.dylib/.lib/.dll) not yet enumerated as a REQ -- follow-up needed.

### Generated headers are build artifacts, not source: private by default in module build/<KEY>/include/; explicitly-promoted public ones are copied up to a generated-only framework build/<KEY>/include/, which IS consulted (alongside fw/include/) when resolving a PREREQS frameworks headers; no workspace-level build/<KEY>/include/ is ever used for compilation

`REQ-generated-headers-in-build-tree-req` · status **confirmed** · id `6f38-6a8a-9a77-a8c9`

Refines REQ-headers-resolved-from-source-req and REQ-module-level-build-subdirs-req. Module level: generated headers (from lex/yacc or other tooling) land in module/build/<KEY>/include/, private to that module by default. Promotion: a module can declare specific generated headers as public (exact declaration macro TBD, open follow-up); on module build, those are copied up to framework/build/<KEY>/include/, which contains ONLY generated headers promoted this way (never hand-written ones -- those stay exclusively in fw/include/). Cross-framework header resolution (REQ-headers-resolved-from-source-req) is amended: for a PREREQS-declared framework, the search covers BOTH its fw/include/ (source) and its build/<KEY>/include/ (promoted generated) -- this is the one deliberate exception to build/<KEY>/ never being consulted for headers. Workspace level: no build/<KEY>/include/ exists for compile-time use at all; any workspace-wide header consolidation happens only during packaging/install (REQ-packaging-per-format-targets-req), never during ordinary compilation.

Refines: `supported-languages`, `module-level-build-subdirs-req`, `headers-resolved-from-source-req`

### Header search paths for a framework are built directly from the include/ directories of the frameworks listed in its PREREQS (resolved locally first, then via PARENT_WS in order); build/<KEY>/ is never consulted for headers

`REQ-headers-resolved-from-source-req` · status **confirmed** · id `6f38-6a8a-967a-e5b0`

When compiling within a framework, the header search path includes: (1) its own local/include/ and, for each module, that modules own include/ (REQ-three-level-header-visibility-req); (2) for each framework named in its PREREQS= (REQ-framework-prereqs-macro-req), that frameworks include/ directory -- resolved by finding the prereq framework first within the current workspace, then falling back through PARENT_WS workspaces in declared order (REQ-workspace-parent-macro-absolute-req) if not found locally. This lookup always reads directly from a frameworks include/ SOURCE directory. No build/<KEY>/ output directory is ever consulted for header resolution, at framework or workspace level -- build/<KEY>/ only ever holds obj/bin/lib/share (REQ-build-output-tree-structure), never headers. Confirms/aligns with real mkmks own behavior (see OBS-mkmk-command-phases-and-cli, OBS-mkmk-identitycard-mechanism).

Refines: `three-level-header-visibility-req`, `workspace-parent-macro-absolute-req`, `framework-prereqs-macro-req`

### Headers have three visibility levels: framework/include (public, to prereq-declaring frameworks), framework/local/include (to the frameworks own modules), and module.m/include (to that modules own sources only)

`REQ-three-level-header-visibility-req` · status **confirmed** · id `6f38-6a8a-04f2-45d5`

Supersedes REQ-framework-header-exposure. Three header directories, each with a distinct audience: framework/include/ headers are visible to any other framework that declares this one as a prereq (via PREREQS=, see REQ-framework-prereqs-macro); framework/local/include/ headers are visible to all modules within this framework but not to other frameworks; module.m/include/ headers are visible only to that modules own source files, not even to sibling modules in the same framework. No cross-level include-direction enforcement (e.g. public cannot include local) has been decided yet -- open follow-up, lower priority.

### LIB_SHARED=YES|NO is a NEW, project-specific macro (not a reuse of any native bsd.lib.mk mechanism); mk.lib.mk must translate it into the real bsd.lib.mk mechanism (SHLIB_MAJOR presence), and suppressing the static archive entirely (true shared-only output) needs custom logic since bsd.lib.mk has no native way to do that

`REQ-lib-shared-is-new-not-reused-req` · status **confirmed** · id `6f38-6a8d-7578-0ea3`

Corrects REQ-lib-shared-defaults-yes-req and REQ-lib-shared-no-is-static-req in light of OBS-validation-lib-shared-does-not-exist-natively: LIB_SHARED= is reclassified from "reused native" to "new, project-specific" in the macro reference (50-makefile-macros.md). Behavior LIB_SHARED=YES (default) must implement in mk.lib.mk: if the module did not set SHLIB_MAJOR explicitly, auto-derive/set a default value (scheme TBD -- open follow-up) so the underlying bsd.lib.mk SHLIB_NAME logic fires and a shared library actually gets built. Behavior LIB_SHARED=NO: do not set SHLIB_MAJOR, so only the static .a gets built -- this case already matches bsd.lib.mk default behavior directly, no extra work needed. Open, NOT yet resolved by this finding: bsd.lib.mk always builds the static .a whenever LIB is set, with no discovered native suppression mechanism -- so a module with LIB_SHARED=YES will, by default reuse of bsd.lib.mk, ALSO get a static .a as a byproduct unless custom mk.lib.mk logic explicitly suppresses it. Whether that byproduct is acceptable or must be suppressed is an open design decision, not yet made.

### Module directory name case-sensitivity is whatever the host filesystem provides; the build system does not itself enforce or normalize case

`REQ-module-name-case-sensitivity-req` · status **confirmed** · id `6f38-6a8a-9181-8613`

Module directory names (NAME.m, per REQ-module-dir-naming-convention) are treated with whatever case-sensitivity the underlying filesystem provides -- distinct names on a case-sensitive filesystem, potentially colliding on a case-insensitive one (e.g. macOS's typical default). The build system does not add its own case-normalization or collision detection layer on top of the filesystem's own behavior.

Refines: `module-dir-naming-convention`

### Module makefile uses LIBS=lib1 lib2 ... (dev and/or system libraries to link with, link-time only, replacing LINK_WITH). Header include-path resolution is driven solely by the containing framework's PREREQS -- LIBS has no role in header lookup

`REQ-libs-macro-link-only-headers-via-prereqs-req` · status **confirmed** · id `6f38-6a8a-943b-a3af`

Supersedes REQ-module-build-macros-req's LINK_WITH design. New macro: LIBS=<lib1> <lib2> ... in a module's makefile, space-delimited, covering both project-internal ('dev') libraries and external system libraries, used exclusively at link time. Header visibility/lookup (REQ-three-level-header-visibility-req) is resolved entirely through the containing framework's PREREQS=... declaration (REQ-framework-prereqs-macro-req) -- a module's LIBS list does not affect which headers are visible to it. PROG=/LIB=/LIB_SHARED= macros and their defaults (REQ-prog-lib-default-to-module-name-req, REQ-lib-shared-is-new-not-reused-req) are unaffected by this change.

### Module output naming supports an embedded version number following standard *nix conventions for versioned shared libraries (e.g. soname-style MAJOR.MINOR.PATCH suffixing)

`REQ-versioned-output-naming-req` · status **confirmed** · id `6f38-6a8a-0550-8bb8`

Extends REQ-output-naming-os-conventions: in addition to the base NAME/libNAME naming already decided, the build system must support attaching a version number to a module's output filename, following standard *nix shared-library versioning conventions (e.g. libfoo.so.1.2.3 with libfoo.so and libfoo.so.1 as symlinks, or libfoo.1.dylib on macOS). Exact mechanism -- new module makefile macro(s) to declare the version, whether/how symlink chains get generated, SONAME handling per platform -- is not yet specified; this REQ records the requirement itself, not yet the design.

Refines: `output-naming-os-conventions`

### Module-level build/<KEY>/ contains obj/ (intermediate objects), bin/ or lib/ (the module's own linked artifact, matching whether it's a PROG or LIB module), and share/ (staged module resources) -- same subdirectory names as framework/workspace level

`REQ-module-level-build-subdirs-req` · status **confirmed** · id `6f38-6a8a-9181-1ebf`

Confirms and closes the open naming question in REQ-build-output-tree-structure and spec/directory-layout.md: module/build/<KEY>/ holds obj/ for intermediates, bin/ (PROG modules) or lib/ (LIB modules) for that module's own linked artifact prior to copy-up (REQ-aggregation-step-copies-outputs), and share/ for staged module-level resources prior to copy-up (REQ-resource-file-model). Naming is consistent with framework/workspace level bin/lib/share.

Refines: `build-output-tree-structure`

### Modules and frameworks build via a fused compile+link (and header-generation) cycle, one unit at a time, in an order where a dependency (via LIBS, or via a consumed promoted-generated header) is FULLY completed before its dependent starts building at all

`REQ-fused-build-per-module-full-dependency-order-req` · status **confirmed** · id `6f38-6a8a-d8b2-fbd8`

Supersedes REQ-sibling-module-ordering-link-only-req. Build strategy: each module (and each framework) is built as one fused unit -- generate headers if applicable, promote per INCL= (REQ-incl-macro-promotes-generated-headers-req), compile src/ to obj/, link to bin/ or lib/, copy up -- matching real bsd.*.mk idiom rather than separate global compile-all/link-all phases. Build ORDER across modules within a framework, and across frameworks in a workspace (REQ-framework-build-order-follows-prereqs-graph-req), is a topological order over TWO kinds of dependency edges: (1) a modules LIBS= entries (REQ-libs-macro-link-only-headers-via-prereqs-req), and (2) any consumption of another modules/frameworks promoted-generated headers (REQ-generated-headers-in-build-tree-req). Either kind of edge requires the depended-on unit to be FULLY built (not just compiled) before the dependent starts. As with framework-level ordering, no cycle detection is implemented (REQ-prereqs-misorder-no-validation-req) -- an actual cycle has undefined behavior.

Refines: `workspace-build-sequence-req`, `framework-build-order-follows-prereqs-graph-req`

### PARENT_WS and PREREQS resolution results are cached in a generated, auto-.included .mk fragment (modeled directly on real bsd makes .depend mechanism), not recomputed by walking the filesystem on every make invocation

`REQ-prereqs-resolution-cached-like-depend-req` · status **confirmed** · id `6f38-6a8c-2159-4b07`

Confirms a caching strategy for the workspace-level build-sequence steps that resolve PARENT_WS (REQ-workspace-parent-macro-absolute-req) and each frameworks PREREQS (REQ-framework-prereqs-macro-req) -- both header search paths and build order (REQ-framework-build-order-follows-prereqs-graph-req). Rather than re-walking workspaces and framework makefiles on every make invocation, results are written to a generated .mk file that gets .included automatically, exactly mirroring real bsd makes .depend idiom (a generated file, regenerated via an explicit step, consumed via .include on later runs) -- consistent with reusing bsd.*.mk infrastructure (CON-use-bsdmake). Open follow-ups for the implementation phase: exact generated filename, staleness-detection mechanism, and whether regeneration is triggered explicitly (an analog to make depend) or auto-detected.

Refines: `workspace-build-sequence-req`, `framework-build-order-follows-prereqs-graph-req`

### PREFIX is a NEW, project-specific variable (not a reuse of any native bsd.prog.mk/bsd.lib.mk mechanism); mk.prog.mk/mk.lib.mk must derive the individual *DIR variables (BINDIR, LIBDIR, etc.) from it themselves, since bare bsd.prog.mk/bsd.lib.mk have no such derivation. DESTDIR remains correctly bsd-make-native, confirmed working as expected

`REQ-prefix-is-new-not-reused-req` · status **confirmed** · id `6f38-6a8e-048e-f811`

Corrects REQ-destdir-prefix-confirmed-req in light of OBS-validation-destdir-native-prefix-is-not: DESTDIR is confirmed genuinely bsd-make-native and composes correctly with whatever *DIR variables are set (BINDIR, etc.) -- no change needed there. PREFIX, however, is reclassified from 'reused native' to 'new, project-specific' in the macro reference (50-makefile-macros.md): this project's mk.prog.mk/mk.lib.mk must itself derive BINDIR, LIBDIR, and any other *DIR variables from a project-level PREFIX= (e.g. BINDIR?=${PREFIX}/bin, LIBDIR?=${PREFIX}/lib), since real bsd.prog.mk/bsd.lib.mk do not perform this derivation automatically -- they only understand the individual *DIR variables directly. Also found: real bsd.prog.mk's install rule does not auto-create missing parent directories in DESTDIR (install without -d assumes the destination tree already exists) -- our install/packaging mechanism (REQ-packaging-invokes-install-into-distrib-req) needs to explicitly create bin/lib/share subdirectories under DESTDIR before invoking install, or rely on 'install -d'-style directory creation as part of that step.

### Packaging targets stage distrib/KEY by invoking install with DESTDIR=distrib/KEY, reusing the install/DESTDIR/PREFIX mechanism rather than a separate copy step

`REQ-packaging-invokes-install-into-distrib-req-v2` · status **confirmed** · id `0f87-6a94-6a03-da95`

Supersedes REQ-packaging-invokes-install-into-distrib-req (unchanged content, re-pointed ancestor only). Closes the open mechanism follow-up on REQ-distrib-dir-for-packaging-staging-req-v2: each packaging target (REQ-packaging-per-format-targets-req) begins by running the equivalent of install DESTDIR=distrib/KEY (REQ-prefix-is-new-not-reused-req), reusing the same staging mechanism as a manual make install rather than inventing a separate copy step. The per-format package artifact (.pkg, port, .deb, etc.) is then built from that staged distrib/KEY tree.

Why this exists as a supersession rather than an edit: the original refined REQ-distrib-dir-for-packaging-staging-req, which the v1->v2 migration correctly marked superseded without updating objects that refined it (DEC-view-drift-refines-repoint-dec). No content changed; only the refines target moved to the live successor.

Refines: `distrib-dir-for-packaging-staging-req-v2`

### Per-source header dependency tracking is fully native and requires no custom wiring: bsd.sys.mk already passes -MD -MF.depend.<obj> to the compiler on every ordinary compile, producing correct per-object dependency files automatically, confirmed to correctly drive incremental rebuilds when a shared header changes. Supersedes the earlier REQ-mkdep-tool-works-wiring-must-be-custom-req, which was based on testing the wrong mechanism (the separate make depend target/mkdep tool, not the compiler-native path that actually matters)

`REQ-compiler-native-dependency-tracking-req` · status **confirmed** · id `6f38-6a91-4c89-ba40`

Corrects REQ-mkdep-tool-works-wiring-must-be-custom-req: mk.prog.mk/mk.lib.mk do NOT need a custom depend: target invoking mkdep manually. Real bsd.sys.mk already wires the compiler's own -MD flag into every compile (CFLAGS/CXXFLAGS get -MD -MF${DEPENDFILE} equivalents automatically), producing a per-object .depend.<objname> file as a side effect of ordinary compilation -- no separate step, no external tool invocation, confirmed correctly triggering recompilation of exactly the affected objects when a shared header changes. This restores REQ-mkdep-style-header-dependency-tracking-req's original assumption (reuse real bsd.*.mk infrastructure, per CON-use-bsdmake) as CORRECT after all -- the earlier correction narrowing it to custom shell-script wiring was itself based on an incomplete test and is now reversed. The classic mkdep tool and the depend: target remain present in bsd.dep.mk for other/legacy purposes (their exact modern role not further investigated) but are NOT what mk.prog.mk/mk.lib.mk need to rely on for incremental header-dependency tracking.

### Phase 1 does not require macOS .app bundle output; macOS targets produce plain command-line executables/libraries via the standard bin/lib/share prefix layout

`REQ-app-bundle-deferred-req` · status **confirmed** · id `6f38-6a8a-06e9-e018`

Scopes REQ-build-output-mirrors-prefix-layout for phase 1: only the plain *NIX/MacPorts-style prefix layout (bin/lib/share) is required. macOS .app bundle output (Contents/MacOS, Contents/Frameworks, Contents/Resources, Info.plist) is deferred -- not designed, not built -- consistent with REQ-phase1-concrete-targets scoping macOS to plain command-line tooling for now.

Refines: `phase1-concrete-targets`, `build-output-mirrors-prefix-layout`

### Phase 1 supports exactly four build targets: macOS/arm64 with llvm, macOS/arm64 with gcc, FreeBSD/x86_64 with llvm, FreeBSD/x86_64 with gcc

`REQ-phase1-concrete-targets` · status **confirmed** · id `6f38-6a89-bb64-0874`

Scopes down the general cross-platform/cross-toolchain requirements (CON-cross-platform-targets, REQ-target-key-includes-toolchain, REQ-default-toolchain-llvm) to a concrete, testable phase-1 target set: {macOS, arm64} x {gcc, llvm} and {FreeBSD, x86_64} x {gcc, llvm}. Linux, other *BSDs, other architectures, and Cygwin/WSL are not required to work yet but the design should not preclude them (per the broader CON/REQ objects already recorded).

### Selecting an unrecognized cross-compile TARGET must fail loudly (a named error), never silently compile natively while claiming that target

`REQ-cross-target-selection-must-fail-loudly-req` · status **confirmed** · id `0f87-6a98-5da8-9fc2`

Same 'never silently substitute' principle REQ-toolchain-mk-must-set-explicit-cc-paths-req-v5 already establishes for host-native compiler lookup, extended to the separate lookup that selects which cross-target branch (triple/sysroot recipe) applies. The error names the actual TARGET requested and the full list of recognized ones (freebsd, linux, macos, netbsd), matching the existing error style for a missing sysroot.

Refines: `toolchain-mk-must-set-explicit-cc-paths-req-v5`

### Shared-library version numbering uses BSD make's own bsd.lib.mk conventions (e.g. SHLIB_MAJOR/SHLIB_MINOR-style macros), producing libfoo.so.X.Y with the standard symlink chain, rather than a project-invented scheme

`REQ-versioned-naming-bsd-lib-mk-req` · status **confirmed** · id `6f38-6a8a-9181-8b21`

Resolves the versioned-naming-mechanism open question from REQ-versioned-output-naming-req: adopt real BSD make's bsd.lib.mk versioning macros (SHLIB_MAJOR, optionally SHLIB_MINOR) in mk.lib.mk, producing versioned shared-library output (libfoo.so.MAJOR.MINOR with the usual libfoo.so / libfoo.so.MAJOR symlink chain on ELF-based *NIX targets), consistent with CON-use-bsdmake's principle of reusing bsd.*.mk infrastructure rather than reinventing it. Open follow-up: macOS's Mach-O dylib versioning uses a structurally different mechanism (install names, compatibility/current version fields via the linker, not a SHLIB_MAJOR-style symlink chain) -- how mk.lib.mk reconciles that with the FreeBSD-style macro is not yet worked out.

Refines: `versioned-output-naming-req`

### Staging share/ resources for a target is a three-layer overlay copy: share/common/, then share/<os>/, then share/<os>_<arch>/, each layer overwriting same-named files from the previous one

`REQ-share-overlay-copy-order` · status **confirmed** · id `6f38-6a89-bcf2-3f23`

Refines REQ-share-dir-fallback-hierarchy with the exact staging mechanism: the target build share/ output is produced by copying share/common/ first, then copying share/<os>/ over it (same-named files get overwritten, unique files from each layer are preserved), then copying share/<os>_<arch>/ over the result. Net effect: any file present at multiple specificity levels ends up as the most specific version, while files unique to a less-specific level still make it into the final staged output. This matches the copy-based aggregation mechanism already decided for bin/lib (REQ-aggregation-step-copies-outputs) -- share/ staging uses the same copy-based approach, just layered by specificity rather than by module/framework/workspace level.

Refines: `share-dir-fallback-hierarchy`

### TARGET=/TARGET_ARCH= variable NAMES are still reused (for familiarity with bsd make convention), but the actual cross-compiler SELECTION logic must be implemented by this project itself (in mk.toolchain.<name>.mk or a new mk.target.mk), not inherited from bsd.prog.mk/bsd.lib.mk, which do nothing with these variables outside FreeBSDs base-system build infrastructure

`REQ-target-arch-toolchain-selection-is-custom-req-v4` · status **confirmed** · id `0f87-6a95-2add-8126`

Corrects REQ-target-selection-uses-bmake-native-vars-req in light of OBS-validation-target-arch-inert-without-basesystem: reusing the TARGET=/TARGET_ARCH= NAMES remains the right call for familiarity, but the claim that this reuse gets working cross-compilation 'for free' from bsd.prog.mk/bsd.lib.mk is corrected -- it does not. mk.toolchain.<name>.mk (REQ-toolchain-triggers-dedicated-mk-file-req) must itself branch on TARGET/TARGET_ARCH to select the actual cross-compiler binary/flags (e.g. a real cross-toolchain triple, or clang --target=<triple> style flags) and set CC/CXX/LD/AR/AS accordingly -- this is now understood as required custom logic, not inherited behavior. This applies to every phase-1 cross-compile scenario (e.g. building freebsd-amd64 targets from a macos-arm64 host) and is a meaningful new implementation task, not just wiring.

Why this has no refines edge: the prior version refined REQ-toolchain-triggers-dedicated-mk-file-req, but that edge did not meet refines' bar -- this REQ is about TARGET/TARGET_ARCH's implementation responsibility, not a more precise statement of what TOOLCHAIN triggers. The two requirements share an implementation location (mk.toolchain.<name>.mk) but are about different variables and different facets. Recorded instead as informed_by (background this REQ leans on), per DEC-view-drift-refines-cycle-fix-dec -- this breaks a genuine 3-way refines cycle across the toolchain-selection cluster. This REQ stands as a root-level statement: nothing more general precedes it in this facet.

### TOOLCHAIN=<gcc|llvm> selects and .includes a dedicated toolchain mk file (mk.toolchain.<name>.mk) that sets CC/CXX/LD/AR/AS and related tool choices for that toolchain; these remain individually overridable via normal make variable precedence

`REQ-toolchain-triggers-dedicated-mk-file-req-v4` · status **confirmed** · id `0f87-6a95-2b36-c1c5`

Supersedes REQ-toolchain-triggers-dedicated-mk-file-req (unchanged content, re-pointed ancestor only). Completes REQ-toolchain-cli-variable-req-v4 with the concrete mechanism: setting TOOLCHAIN=gcc or TOOLCHAIN=llvm causes the build infrastructure to .include a matching mk.toolchain.<name>.mk file, which sets the underlying tool variables (CC, CXX, LD, AR, AS, and any other tool-chain-specific settings) as a coherent bundle for that toolchain. This follows the same role-specific-include pattern already used for workspace/framework/module makefiles (REQ-uniform-makefile-filename-req). Individual variables (CC=, LD=, etc.) remain overridable afterward via ordinary make variable-precedence rules, for mixed setups (e.g. clang with GNU ld) -- exact mk.toolchain.<name>.mk contents and file-naming scheme not yet fully specified, open follow-up for the implementation phase.

Why this went through several generations of supersession: this REQ's refines edge to REQ-toolchain-cli-variable-req is the one genuine refinement in a cluster that also contained a 3-way refines cycle (DEC-view-drift-refines-cycle-fix-dec) -- both toolchain-cli-variable-req and target-arch-toolchain-selection-is-custom-req were re-created to fix mistyped refines edges elsewhere in the cluster, which required this REQ to be re-pointed at each new generation in turn. No content changed at any step; only ancestor pointers moved as the cluster settled.

Refines: `toolchain-cli-variable-req-v4`

### TOOLCHAIN=msvc must work from a plain Cygwin shell with no prior manual setup -- no Developer Command Prompt, no manually-run vcvarsall.bat -- discovering the installed MSVC toolset and Windows SDK itself

`REQ-msvc-no-manual-vcvarsall-req` · status **confirmed** · id `0f87-6a9a-d35b-a3ed`

Refines: `win-target-tiered-toolchain-req`

### Test infrastructure, documentation generation, and package versioning/metadata are out of scope for the current design pass, deferred to later

`REQ-tests-docs-versioning-deferred-req` · status **confirmed** · id `6f38-6a8c-beda-c0ba`

Explicitly scopes down the current design effort: no test-declaration mechanism, no documentation-generation orchestration, and no package name/version/maintainer metadata scheme are designed at this stage. This does not preclude any particular future design -- it simply defers the question, consistent with REQ-make-port-deferred-req and the rest of the standing deferred-scope list.

### The Bmake It project itself has a script-driven test harness (not a make target) validating mk.*.mk behavior: test cases packaged as archives, each expanded into a fresh temp directory, built with bmake, result recorded against a declared expectation, temp directory deleted; documented per case

`REQ-build-system-self-test-harness-req` · status **confirmed** · id `6f38-6a8f-5206-3e89`

Distinct from and does not reopen REQ-tests-docs-versioning-deferred-req (which covers whether END-USER modules/frameworks built with Bmake It can declare their own tests -- still deferred). This REQ is about testing Bmake It's OWN mk.*.mk implementation. Design: test-case fixtures live as .tar.gz archives (not live checked-in directories), each containing a minimal self-contained project plus a small config file declaring: a description (for documentation), the bmake arguments to invoke, and an expectation (exit code, and/or a specific file expected to exist afterward, and/or expected substrings in build output). A driver script external to make itself iterates the archives: expand each into a fresh mktemp scratch directory, run bmake there, check the result against the declared expectation, print PASS/FAIL with a reason, delete the scratch directory (on PASS; a FAIL preserves its build log for inspection), and exit non-zero overall if anything failed. Confirmed working via a real prototype (prototypes/run-tests.sh + prototypes/cases/*.tar.gz) covering three of this sessions empirical findings (yacc/lex suffix rules, LIB_SHARED static+shared behavior, dynamic SUBDIR discovery) plus a deliberate-failure case confirming the FAIL path itself works correctly. Open follow-ups for a fuller implementation: test-case authoring conventions/templates, whether cases can be organized by validation-plan tier (spec/60-bsd-make-validation-plan.md), and CI integration.

Refines: `tests-docs-versioning-deferred-req`

### The build directory key (used throughout build/<KEY>/ at module, framework, and workspace levels) is a compound of OS, architecture, AND toolchain -- not OS+arch alone -- so different toolchains for the same OS/arch coexist in the same source tree

`REQ-target-key-includes-toolchain` · status **confirmed** · id `6f38-6a89-bb64-3595`

Refines REQ-build-output-tree-structure and REQ-variant-out-of-scope-for-dirtree: the directory key referred to as $OS_ARCH in earlier requirements is actually a three-part target identifier: operating system, CPU architecture, and toolchain (e.g. gcc or llvm). Two builds of the same OS/arch with different toolchains produce separate, independently addressable build/<KEY>/ subtrees at every level (module obj/, framework and workspace bin/lib/). Build variant (debug/release) is still explicitly excluded from this key -- only OS, arch, and toolchain participate. Exact string format for the compound key is an open follow-up.

Refines: `build-output-tree-structure`, `variant-out-of-scope-for-dirtree`

### The build system does not validate PREREQS ordering or detect cycles; a misordered PREREQS list surfaces only as an ordinary header-not-found compile error, not a dedicated diagnostic

`REQ-prereqs-misorder-no-validation-req` · status **confirmed** · id `6f38-6a8a-943b-c5a3`

Closes the open follow-up on REQ-framework-prereqs-macro-req regarding what happens when PREREQS is misordered: nothing special, for now. Header lookup follows PREREQS declaration order; if that order is wrong, compilation fails with a normal 'header not found' error, the same as any missing-include failure. No dedicated cycle-detection or order-validation pass is implemented at this stage -- explicitly deferred, may be added later.

Refines: `framework-prereqs-macro-req`

### The build system supports C, C++, and lex/yacc grammar sources (compiled to C/C++); other mkmk-supported languages (Java, Fortran, IDL, Express) are out of scope for now

`REQ-supported-languages` · status **confirmed** · id `6f38-6a89-b79d-c3c3`

Supported source languages: C, C++, lex, and yacc (lex/yacc are grammar sources that generate C or C++ source, matching the .l/.lc/.y/.yc suffix convention seen in the mkmk reference docs). Java, Fortran, IDL, and Express grammar -- all supported by real mkmk -- are out of scope for now. This scopes down REQ-module-src-auto-discovery and REQ-module-makefile-fields (compiler/ object-type selection only needs to cover these languages) and means Java-specific mechanisms noted in OBS-mkmk-java-and-windows-specifics do not need to be designed for at this stage.

### The build system targets userspace/userland binaries and libraries only; kernel modules/drivers and Darwin/XNU-specific kernel development are out of scope for now

`REQ-userland-only-scope` · status **confirmed** · id `6f38-6a89-bec3-9ca3`

Scopes down the set of build-object-type work: this system builds ordinary userland executables, shared libraries, and static libraries (per REQ-module-is-basic-link-unit and BUILT_OBJECT_TYPE-style object types). Kernel modules/drivers (for FreeBSD, Linux, or any other target) and Darwin/XNU-specific kernel-level development are explicitly out of scope for now, deferred like OpenBSD/NetBSD support and per-Linux-distro handling (REQ-os-label-set-req). Revisit later if kernel-level targets become needed.

### The cross-compile sysroot requirement only blocks invocations that can reach a compile step -- not a -V query or a clean

`REQ-cross-compile-check-scoped-to-actual-builds-req` · status **confirmed** · id `0f87-6a98-5dca-b115`

A loud, deliberate error over a missing sysroot exists to protect real builds, not to block every parse of a TARGET/TARGET_ARCH combination that merely resembles a cross build. Scoped via empty(.MAKEFLAGS:M-V*) && !make(clean) around the sysroot-requirement check.

Refines: `toolchain-mk-must-set-explicit-cc-paths-req-v5`

### The default C/C++ compiler is LLVM/clang; the legacy OS-native compiler (e.g. system gcc) may be used as an alternative

`REQ-default-toolchain-llvm` · status **confirmed** · id `6f38-6a88-1836-c0f9`

For C and C++ compilation, the build system can use either the legacy OS-provided compiler (e.g. system gcc) or LLVM. LLVM is the default toolchain when nothing else is specified. Open question: exact mechanism/variable to opt into the legacy compiler is not yet specified.

### The default build target is the OS/architecture of the machine running the build

`REQ-default-build-host-os-arch` · status **confirmed** · id `6f38-6a88-1836-0f69`

Absent any cross-compilation target specification, the build produces output for the host's own OS and architecture.

### The target key <os> component uses one of: macos, win, freebsd, linux; openbsd, netbsd, and Linux-distro-specific labels are deferred, not decided for phase 1

`REQ-os-label-set-req-v2` · status **confirmed** · id `0f87-6a94-696d-920d`

Supersedes REQ-os-label-set-req (unchanged content, re-pointed ancestor only). Canonical values: macos, win (Cygwin/WSL, undifferentiated for now), freebsd, linux. Phase 1 (REQ-phase1-concrete-targets) only exercises macos and freebsd. Deferred, not yet decided: whether OpenBSD/NetBSD get their own os labels or share a generic bsd-family label, and whether Linux needs per-distro os labels (e.g. for differing package-manager targets in the packaging step) or a single generic linux label suffices.

Why this exists as a supersession rather than an edit: the original refined REQ-target-key-string-format-req, which the v1->v2 migration correctly marked superseded (by REQ-target-key-omit-defaults-req) without updating objects that refined it -- refinement cannot express "this ancestor was replaced," only supersession can (DEC-view-drift-refines-repoint-dec). No content changed; only the refines target moved to the live successor.

Refines: `target-key-omit-defaults-req`

### The target key omits the toolchain suffix when toolchain=llvm (the default) and omits the ABI/libc suffix when it equals that OS default; both are shown explicitly only when non-default

`REQ-target-key-omit-defaults-req` · status **confirmed** · id `6f38-6a8a-0c4d-d8e3`

General key shape: <os>-<arch>[-<toolchain>][-<abi>], where the toolchain segment is present only if it differs from the default (llvm, per REQ-default-toolchain-llvm) -- e.g. gcc is shown, llvm is not -- and the ABI/libc segment (only applicable for OSes that have more than one ABI, currently just win) is present only if it differs from that OSs default (msvc for win, per the earlier win-amd64-msvc collapsing to win-amd64 example). Concretely, this means phase-1 target keys become: macos-arm64 (llvm, default, omitted), macos-arm64-gcc, freebsd-amd64 (llvm, default, omitted), freebsd-amd64-gcc -- superseding the earlier phase-1 key examples which always spelled out -llvm. Supersedes REQ-target-key-string-format-req and refines REQ-phase1-concrete-targets naming (does not change which 4 targets phase 1 covers, only how their keys are spelled).

### The workspace-level build proceeds in order: resolve PARENT_WS, resolve each frameworks PREREQS (headers), build each module (compile-then-link-then-copy-up to framework), copy framework outputs up to workspace, with share/ resources following the same module-to-framework-to-workspace cascade

`REQ-workspace-build-sequence-req` · status **confirmed** · id `6f38-6a8a-9be8-14df`

Consolidated build sequence, ties together REQ-workspace-parent-macro-absolute-req, REQ-framework-prereqs-macro-req, REQ-headers-resolved-from-source-req, REQ-generated-headers-in-build-tree-req, REQ-prereqs-misorder-no-validation-req, REQ-module-prog-or-lib-macro-req, REQ-module-level-build-subdirs-req, REQ-aggregation-step-copies-outputs, and REQ-share-overlay-copy-order into one ordered flow: (1) resolve PARENT_WS; (2) resolve each frameworks PREREQS for header search paths (no cycle/order validation); (3) per module: compile src/ to obj/, link to the modules own bin/ or lib/, copy the linked artifact up to the frameworks bin/lib/; (4) copy framework bin/lib/ up to workspace bin/lib/; (5) resources (share/) cascade the same way, module to framework to workspace, each level applying its own common/os/os_arch overlay before copy-up.

Refines: `aggregation-step-copies-outputs`, `share-overlay-copy-order`, `workspace-parent-macro-absolute-req`, `framework-prereqs-macro-req`, `module-level-build-subdirs-req`, `prereqs-misorder-no-validation-req`, `headers-resolved-from-source-req`, `generated-headers-in-build-tree-req`

### Toolchain selection on the make command line uses TOOLCHAIN=<gcc|llvm>; unspecified defaults to the current default toolchain, llvm

`REQ-toolchain-cli-variable-req-v4` · status **confirmed** · id `0f87-6a95-2afd-4e9b`

TOOLCHAIN is a new project-specific variable (no bsd-make-native equivalent exists). Unspecified means the current default toolchain, llvm (REQ-default-toolchain-llvm), matching how TARGET=/TARGET_ARCH= default to the current host when unset. Together with TARGET=<os>, TARGET_ARCH=<arch>, TOOLCHAIN=<gcc|llvm> determine the full compound target key (REQ-target-key-omit-defaults-req).

Why this has no refines edge: the prior version refined REQ-target-arch-toolchain-selection-is-custom-req's lineage, justified in its own body as 'completing the story alongside' that requirement -- peer language, not 'a more precise statement of' it. TOOLCHAIN and TARGET/TARGET_ARCH are sibling variables on different axes (compiler choice vs. cross-compile destination), not one refining the other. Recorded instead as informed_by, per DEC-view-drift-refines-cycle-fix-dec -- this breaks a genuine 3-way refines cycle across the toolchain-selection cluster. This REQ stands as a root-level statement: nothing more general precedes it in this facet.

### When PROG= or LIB= is not set in a module's makefile, it defaults to the module's directory name with the .m suffix stripped; for LIB= specifically, a leading 'lib' prefix on that name is also stripped (e.g. libgreet.m defaults to LIB=greet, not LIB=libgreet)

`REQ-prog-lib-default-to-module-name-req-v3` · status **confirmed** · id `0f87-6a95-509a-8559`

Supersedes REQ-prog-lib-default-to-module-name-req-v2 (extends it; the PROG= defaulting rule is unchanged). Refines REQ-module-prog-or-lib-macro-req and REQ-module-dir-naming-convention: a module directory named NAME.m defaults to PROG=NAME (if .include <mk.prog.mk>) when PROG= is left unset. For LIB= (if .include <mk.lib.mk>) when left unset, the default is NAME with the .m suffix stripped, AND, if that name starts with 'lib', that leading 'lib' is also stripped -- so libgreet.m defaults to LIB=greet, not LIB=libgreet.

Why this exists as a supersession rather than an edit: the prior version only specified stripping '.m'. Found while assessing the impl/ implementation, which additionally strips a leading 'lib' -- real bsd.lib.mk/mk.lib.mk conventions prepend 'lib' when naming output (lib${LIB}.so, lib${LIB}.a), so defaulting LIB= to the full 'libgreet' would produce a redundant liblibgreet.so. The user confirmed this should be the spec, not merely an implementation convenience (DEC-lib-default-strips-lib-prefix-dec) -- this is new precision added to the requirement, not a correction of wrong content, so it is recorded as a supersession per the model's own discipline (supersession is total; nothing here was wrong, it was incomplete).

Refines: `module-prog-or-lib-macro-req`, `module-dir-naming-convention`

### Windows target support is available via three toolchain paths -- tier 1: native MSVC (cl.exe/link.exe) run from a Cygwin-hosted bmake shell, command-line only, matching every other platform's workflow; tier 1: Cygwin-native gcc/llvm run the same way; tier 2: mingw-w64 cross-compilation from any capable host, no Cygwin required. All three produce genuine win-target output

`REQ-win-target-tiered-toolchain-req` · status **confirmed** · id `0f87-6a98-92e4-4528`

**Architecture scoping (CON-cygwin-scoped-to-x86-family-only):** Cygwin's
own cygwin1.dll runtime has no ARM64 port -- on an ARM64 Windows host it
only ever runs as x86_64 code under Windows' built-in x64 emulation, never
natively. This applies to BOTH of Cygwin's roles here: as the host-side
supplier of sh/bmake/tooling for tier-1 MSVC builds (that shell and bmake
itself are x86_64-under-emulation, regardless of the true host CPU), and
as the "Cygwin-native gcc/llvm" tier-1 ABI in its own right (that gcc/
clang is likewise x86_64-hosted and has no built-in arm64-target
cross-compilation of its own). Neither role can ever produce or represent
`TARGET_ARCH=arm64` win-target output. When TARGET_ARCH=arm64 output is
actually wanted, the only valid paths are MSVC (tier 1 -- a genuinely
multi-arch-capable toolchain per the host Visual Studio install, able to
target x86/x64/arm64 regardless of host arch) or mingw-w64 cross-compilation
(tier 2, via an aarch64-w64-mingw32 sysroot, from any host). "Cygwin" is
never itself a target-arch axis -- it is a host-tooling / ABI-flavor
choice orthogonal to, and narrower than, TARGET_ARCH.

Refines: `os-label-set-req-v2`

### Workspace, framework, and module levels each use a file named literally 'makefile', which .includes a role-specific helper: <mk.workspace.mk>, <mk.framework.mk>, <mk.prog.mk>, or <mk.lib.mk>

`REQ-uniform-makefile-filename-req` · status **confirmed** · id `6f38-6a8a-04e6-c63c`

Every level in the tree (workspace root, each framework, each module) has exactly one file named 'makefile' (not 'makefile.mk'). Each is a thin declaration file: it sets a handful of role-specific variables (see REQ-workspace-parent-macro-absolute-req, REQ-framework-prereqs-macro-req, REQ-module-prog-or-lib-macro-req) and then does a bsd-make-style .include of a shared helper file matching its role -- mk.workspace.mk at workspace level, mk.framework.mk at framework level, and either mk.prog.mk (building an executable) or mk.lib.mk (building a library, static or shared) at module level. This mirrors real BSD make's own bsd.prog.mk/bsd.lib.mk convention (CON-use-bsdmake), scoped up to this project's own workspace/framework/module notions. Supersedes REQ-module-makefile-fields (which used the filename 'makefile.mk').

### Workspace-level framework discovery treats an immediate subdirectory as a framework if it contains a makefile declaring PREREQS=; this is the sole discovery signal (build/ is excluded because it has no such makefile)

`REQ-framework-discovery-signal-req` · status **confirmed** · id `6f38-6a8a-f6ee-e69f`

Closes the discrimination-signal follow-up from REQ-modules-and-frameworks-auto-discovered-req. Workspace-level scanning (mk.workspace.mk) treats any immediate subdirectory containing a makefile with a PREREQS= declaration as a framework. Since PREREQS= is now mandatory in every framework makefile (REQ-prereqs-mandatory-even-empty-req), this check is reliable and unambiguous. build/ (and any other non-framework directory) is naturally excluded, since it has no makefile at all.

Refines: `modules-and-frameworks-auto-discovered-req`

### build/<KEY>/{bin,lib,share} mirrors a standard *NIX install-prefix layout for FreeBSD/Linux/MacPorts targets; macOS .app bundle output (a structurally different layout: Contents/MacOS, Contents/Frameworks, Contents/Resources) is a distinct, not-yet-designed alternative

`REQ-build-output-mirrors-prefix-layout` · status **confirmed** · id `6f38-6a8a-06bf-bede`

Clarifies the rationale behind REQ-build-output-tree-structure: bin/, lib/, share/ under build/<KEY>/ are not an arbitrary convenience convention -- they mirror the real install-prefix layout used by plain *NIX systems (e.g. /usr/local/{bin,lib,share}) and by MacPorts (/opt/local/{bin,lib,share}). This layout is confirmed sufficient for FreeBSD, Linux, and MacPorts-style macOS targets. A macOS .app application bundle uses a structurally different layout (Contents/MacOS/ for the executable, Contents/Frameworks/ for shared libraries, Contents/Resources/ for resources, Info.plist metadata) that bin/lib/share cannot represent. Whether/when .app bundle output is needed, and how a module or framework would opt into it, is an open follow-up -- not yet decided, and not assumed to be in scope for phase 1.

Refines: `build-output-tree-structure`

### glibc is the default libc for Linux targets and is omitted from the target key; musl is always shown explicitly (e.g. linux-arm64-musl)

`REQ-linux-libc-default-glibc-req` · status **confirmed** · id `6f38-6a8a-0d9b-2b26`

Resolves the libc-default follow-up from OBS-linux-libc-choice-parallels-win-abi, applying the general omit-if-default convention (REQ-target-key-omit-defaults-req) to Linux: glibc is the implicit default, so linux-<arch>[-<toolchain-if-non-default>] alone means a glibc build. musl must always be spelled out explicitly as an additional suffix. Still deferred, unaffected by this: Linux is not in REQ-phase1-concrete-targets, and per-distro Linux labeling remains an open follow-up per REQ-os-label-set-req.

Refines: `target-key-omit-defaults-req`

### make add-prereq FW=<name> appends <name> to the current frameworks PREREQS=; make add-parent WS=<abs-path> appends <abs-path> to the workspaces PARENT_WS=; both edit the makefile text directly rather than requiring hand-editing

`REQ-add-prereq-add-parent-convenience-targets-req` · status **confirmed** · id `6f38-6a8c-bbfe-2001`

Two convenience targets: add-prereq (run within a framework directory, argument FW=<name>, appends <name> to that frameworks PREREQS= line, preserving existing order per REQ-framework-prereqs-macro-req) and add-parent (run at workspace root, argument WS=<absolute-path>, appends the path to PARENT_WS= per REQ-workspace-parent-macro-absolute-req -- absolute paths only, consistent with that REQ). Both are pure text-editing convenience over the makefile, not search-path resolution or copying -- explicitly does not reopen or contradict NREQ-no-copy-preq-commands. Exact edit mechanism (sed-style in-place edit vs. parse-and-rewrite) not yet specified, open follow-up for implementation.

Refines: `workspace-parent-macro-absolute-req`, `framework-prereqs-macro-req`

### make automatically regenerates the order cache (running the equivalent of make order) if it is missing or stale, before proceeding with the build; make order remains separately invocable on its own

`REQ-order-cache-auto-triggered-req` · status **confirmed** · id `6f38-6a91-03ec-2589`

The order cache (REQ-order-target-blank-run-req) is treated as an implicit prerequisite of the default build target, not an optional manual step: if the cache does not exist or is detected stale (exact staleness-detection mechanism still deferred per OBS-cache-mechanism-needs-empirical-check, same as the other bsd.depend-style caches), make regenerates it automatically before proceeding, so a fresh checkout builds correctly with plain make and no mandatory separate invocation. make order remains available as its own standalone target for developers who want to inspect the resolved build order without triggering a build.

Refines: `prereqs-resolution-cached-like-depend-req`, `order-target-blank-run-req`

### make clean removes every generated/derived artifact for the selected target scope: build/<KEY>/, distrib/<KEY>/, per-module .depend files, the PARENT_WS/PREREQS resolution cache, and the dependency-order generation cache -- leaving the tree in a state indistinguishable from a fresh checkout

`REQ-clean-removes-all-derived-artifacts-req` · status **confirmed** · id `6f38-6a8f-519a-d42b`

Supersedes REQ-clean-target-scope-req's narrower build/<KEY>/-only scope. make clean (for the current or TARGET=/TARGET_ARCH=/TOOLCHAIN=-selected target, or every target at once via TARGET=all -- target-selection semantics are unchanged from the original scope, only WHAT gets removed per target is broadened) removes: build/<KEY>/ (obj/bin/lib/share/include at every level); distrib/<KEY>/ (packaging staging); any generated per-source dependency-tracking cache files (mechanism: REQ-compiler-native-dependency-tracking-req); the workspace/framework-level PARENT_WS/PREREQS resolution cache (REQ-prereqs-resolution-cached-like-depend-req); and the dependency-order generation cache (REQ-dep-order-generation-mechanism-req). Net effect: after make clean TARGET=all, the source tree should be indistinguishable from a fresh checkout -- nothing generated survives. Exact file locations/names for the non-target-keyed caches (the dependency-tracking cache files, the dependency-order cache) are still pending the empirical bsd.*.mk check (OBS-cache-mechanism-needs-empirical-check), so this REQ commits to the SCOPE, not yet the literal file list.

### make help lists every valid make target with a description, and every valid variable with a description and its valid values (e.g. LIB_SHARED=YES|NO), scoped to what's applicable at the current directory level

`REQ-help-target-req` · status **confirmed** · id `6f38-6a8f-5180-1a89`

New requirement: a make help target exists and is self-documenting. Implementation mechanism: each role-specific mk.*.mk file (workspace/framework/prog/lib/toolchain) defines its own contribution to help: (its own targets and variables, with descriptions and valid-value sets), so the aggregate make help output at any given directory level reflects exactly the makefile roles actually .included there -- no separate, hand-maintained documentation file to keep in sync. Exact output formatting not yet specified, open follow-up for implementation.

Refines: `uniform-makefile-filename-req`

### make install copies the current targets build/<KEY>/{bin,lib,share} to a real install destination, distinct from the per-format packaging targets (make port, make pkg, ...); proposed to reuse bsd makes native DESTDIR/PREFIX variables, pending confirmation

`REQ-install-target-exists-req` · status **confirmed** · id `6f38-6a8b-df45-59b4`

A make install target exists, separate from REQ-packaging-per-format-targets-req named per-format targets. It installs the current (or TARGET=/TARGET_ARCH=/TOOLCHAIN=-selected) targets build/<KEY>/{bin,lib,share} into a real destination. Proposed (not yet literally confirmed by the user): reuse bsd makes own DESTDIR (staging root) and PREFIX (final install path under DESTDIR) variables, matching the precedent set by REQ-target-arch-toolchain-selection-is-custom-req. Open follow-up: confirm DESTDIR/PREFIX specifically, and clarify the relationship between make install and the packaging targets (e.g. do make port/pkg internally invoke make install into a temporary DESTDIR as their first step?).

Refines: `packaging-per-format-targets-req`, `build-output-mirrors-prefix-layout`

### make install fails/refuses if the current (or selected) targets build/<KEY>/ output does not already exist -- it does not implicitly build first; cross-filesystem install is deferred, not designed

`REQ-install-requires-prebuilt-target-req-v2` · status **confirmed** · id `0f87-6a94-696d-71fc`

Supersedes REQ-install-requires-prebuilt-target-req (unchanged content, re-pointed ancestor only). Refines REQ-install-target-exists-req and REQ-prefix-is-new-not-reused-req: make install requires the relevant targets build/<KEY>/{bin,lib,share} to already exist and be built -- it is not implicitly dependent on all/the default build target the way some install targets are. Attempting install before building the target is an error/no-op, not an automatic build-then-install. Cross-filesystem install (exact scenario not yet fully specified by the user) is deferred, not designed at this stage -- joins REQ-app-bundle-deferred-req and the other standing later items.

Why this exists as a supersession rather than an edit: the original refined REQ-destdir-prefix-confirmed-req, which the v1->v2 migration correctly marked superseded (by REQ-prefix-is-new-not-reused-req) without updating objects that refined it (DEC-view-drift-refines-repoint-dec). No content changed; only the refines target moved to the live successor.

Refines: `install-target-exists-req`, `prefix-is-new-not-reused-req`

### make order performs a workspace-wide blank run (discovery + full PREREQS/LIBS dependency-graph resolution, no compilation or linking) and writes the topologically-sorted result to a cache; the build target consumes that cache rather than re-resolving order inline

`REQ-order-target-blank-run-req` · status **confirmed** · id `6f38-6a91-03ec-9f14`

Formalizes REQ-dep-order-generation-mechanism-req and REQ-prereqs-resolution-cached-like-depend-req into an explicit workspace-level target. make order: (1) auto-discovers every framework at the workspace root and every module within each framework (REQ-modules-and-frameworks-auto-discovered-req); (2) reads each frameworks PREREQS= and each modules LIBS=; (3) resolves the complete dependency graph -- framework-level order via PREREQS, module-level order within each framework via LIBS -- using the same topological-sort mechanism already prototyped (prototypes/gen-mod-order.sh); (4) writes the sorted result to a generated cache (the same artifact referred to at a policy level in REQ-prereqs-resolution-cached-like-depend-req). No compilation, linking, or header generation happens during this run -- it is purely discovery and graph resolution. The default make (build) target reads this cache to drive its own SUBDIR-based recursion, rather than resolving order itself on every invocation.

Refines: `prereqs-resolution-cached-like-depend-req`, `dep-order-generation-mechanism-req`

### make port generation mechanism is deferred to a later work phase; only its category (source-recipe, not binary package) is settled for now

`REQ-make-port-deferred-req-v2` · status **confirmed** · id `0f87-6a94-6a49-e347`

Supersedes REQ-make-port-deferred-req (unchanged content, re-pointed ancestor only). Confirms REQ-port-target-is-source-recipe-not-binary-package-req-v2 is deliberately left at that level of detail -- WHAT make port conceptually is (a source-based ports recipe) is settled, but HOW it is generated (templating, pkg-plist derivation, distinfo/patch handling) is deferred to a later phase, joining REQ-app-bundle-deferred-req and the rest of the standing deferred-scope list.

Why this exists as a supersession rather than an edit: the original refined REQ-port-target-is-source-recipe-not-binary-package-req, which the v1->v2 migration correctly marked superseded without updating objects that refined it (DEC-view-drift-refines-repoint-dec). No content changed; only the refines target moved to the live successor.

Refines: `port-target-is-source-recipe-not-binary-package-req-v2`

### make port produces a source-based build recipe (Makefile/distinfo/patches/pkg-plist) for the ports system to build from source, NOT a staged binary package; REQ-packaging-invokes-install-into-distrib-req applies only to binary-package-style targets (pkg, deb, rpm, msi)

`REQ-port-target-is-source-recipe-not-binary-package-req-v2` · status **confirmed** · id `0f87-6a94-6a22-7d41`

Supersedes REQ-port-target-is-source-recipe-not-binary-package-req (unchanged content, re-pointed ancestor only). Narrows the scope of REQ-packaging-invokes-install-into-distrib-req-v2 and REQ-distrib-dir-for-packaging-staging-req-v2: those describe binary-package targets only (pkg, deb, rpm, msi -- REQ-packaging-per-format-targets-req). make port is a distinct category: it generates the BSD ports-style recipe (a port Makefile, distinfo with source checksums, any needed patch files, and a pkg-plist enumerating installed paths) that points at this project's own source (typically for the ports system's own fetch step), rather than staging or embedding any pre-built binary. Exact mechanism for make port -- how source is referenced/packaged for fetching, how pkg-plist gets generated (possibly from a distrib/KEY dry-run, without embedding the binaries themselves), and the ports Makefile template itself -- is NOT yet designed, explicit open follow-up.

Why this exists as a supersession rather than an edit: the original refined REQ-distrib-dir-for-packaging-staging-req and REQ-packaging-invokes-install-into-distrib-req, both of which the v1->v2 migration correctly marked superseded without updating objects that refined them (DEC-view-drift-refines-repoint-dec). No content changed; only the refines targets moved to their live successors.

Refines: `distrib-dir-for-packaging-staging-req-v2`, `packaging-invokes-install-into-distrib-req-v2`, `packaging-per-format-targets-req`

### make run PROGRAM=<name> ARGS=<args>, invoked at workspace level, sets the platform-appropriate dynamic-library search path (LD_LIBRARY_PATH/DYLD_LIBRARY_PATH/etc.) to include workspace build/<KEY>/lib/, then executes build/<KEY>/bin/<PROGRAM> with the given arguments

`REQ-run-target-req` · status **confirmed** · id `6f38-6a8f-5180-18f8`

PROGRAM=<name> names an executable already present in the current targets workspace build/<KEY>/bin/ (does not implicitly build it first -- consistent with REQ-install-requires-prebuilt-target-req's precedent of not auto-triggering a build). ARGS=<args> is passed through to the program verbatim. Before execution, the appropriate per-OS dynamic-library search-path environment variable is set to include workspace build/<KEY>/lib/ (so shared libraries built by any framework in this workspace are found without needing a real install step) -- exact variable per REQ-os-label-set-req's os labels: LD_LIBRARY_PATH (freebsd, linux), DYLD_LIBRARY_PATH (macos), PATH-inclusion (win). Exact mechanism for locating/setting this (a small wrapper script generated by mk.workspace.mk, vs. inline make recipe logic) is an open follow-up for implementation.

Refines: `clean-removes-all-derived-artifacts-req`

### make run PROGRAM=<name> [ARGS=<args>] must work when invoked from bmake in any directory within the workspace tree (the workspace root, a framework directory, or a module directory), not only the workspace root itself, resolving PROGRAM against the enclosing workspace's aggregated build/<KEY>/bin and lib

`REQ-run-target-any-workspace-dir-req` · status **confirmed** · id `0f87-6a98-7bd0-ed4a`

Refines: `run-target-req`

### mk.toolchain.<name>.mk must explicitly set CC/CXX/LD/AR/AS to concrete cross-compiler paths/binaries based on TARGET/TARGET_ARCH/TOOLCHAIN, following the same external-orchestration pattern every bsd-make lineage (FreeBSD Makefile.inc1, NetBSD build.sh) uses; locating each toolchain's compiler must search PATH (or a defined list of install prefixes), never rely on a single hardcoded absolute path, and must fail loudly rather than silently substituting a different compiler when the requested toolchain cannot be found

`REQ-toolchain-mk-must-set-explicit-cc-paths-req-v5` · status **confirmed** · id `0f87-6a95-78e1-3ab3`

Supersedes REQ-toolchain-mk-must-set-explicit-cc-paths-req-v4 (extends it; the cross-compiler-path-mapping content is unchanged). mk.toolchain.<name>.mk needs its own explicit lookup/naming-convention logic mapping (TARGET, TARGET_ARCH, TOOLCHAIN) combinations to real compiler binary paths, and set CC/CXX/LD/AR/AS to those concrete paths directly -- mirroring how NetBSD's build.sh and FreeBSD's Makefile.inc1 do it for their own base-system builds. New in this version: locating the HOST-native compiler for a given toolchain (the non-cross-compiling case) must not rely on a single hardcoded absolute path such as exists(/usr/bin/clang) or exists(/usr/bin/gcc). It must search PATH, or a defined list of common install prefixes (/usr/bin, /usr/local/bin, /opt/local/bin for MacPorts, /usr/pkg/bin for pkgsrc, etc.), and if the requested toolchain's compiler cannot be located by any of them, the build must fail with a clear error naming what was requested and where it looked -- never silently fall through to whatever plain cc/cxx happens to resolve to, which may be an entirely different compiler.

Why this exists as a supersession rather than an edit: found empirically on real NetBSD with clang installed via pkgsrc to /usr/pkg/bin/clang (not /usr/bin/clang). With TOOLCHAIN=llvm explicitly requested and clang genuinely on PATH, the build silently used plain cc (which happened to be GCC on that system) instead -- no error, no warning. The user confirmed this is a defect to fix, not an acceptable convenience shortcut (DEC-toolchain-detection-must-search-path-dec), and that it is the same class of mistake as DEC-nested-invocation-reentrancy-guard-dec's hardcoded-path finding: a hardcoded absolute path standing in for a robust lookup.

Refines: `target-arch-toolchain-selection-is-custom-req-v4`, `toolchain-triggers-dedicated-mk-file-req-v4`

