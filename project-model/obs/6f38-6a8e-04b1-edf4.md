---
schema_version: 2
id: 6f38-6a8e-04b1-edf4
alias: validation-dynamic-subdir-confirmed-working
type: obs
status: recorded
origin: empirical test (real FreeBSD share/mk + portable bmake, 2026-08-25)
created: 2026-08-25
facets: [architecture]
rel_informed_by: [6f38-6a8a-dba2-dcc2, 6f38-6a8a-f6ee-e69f]
generator: migrate.py/v2
checksum: fcced6d2236a
---

# Empirically confirmed: SUBDIR!= (shell command-substitution assignment) correctly populates SUBDIR from a live directory scan, and bsd.subdir.mk correctly recurses into each discovered directory. The entire auto-discovery design (module/framework scanning) works via real bsd make mechanics with no fallback needed

Test performed: three module directories (mod1.m, mod2.m, mod3.m), a top-level makefile with SUBDIR!= /bin/ls -d *.m 2>/dev/null and .include <bsd.subdir.mk>, no explicit SUBDIR= list written by hand. Result: bmake correctly ran the shell command, populated SUBDIR with all three discovered directories, and bsd.subdir.mk recursed into each one in turn, running the default target successfully in each. This CONFIRMS Tier 2.4 of the validation plan (spec/60-bsd-make-validation-plan.md) with no caveats -- REQ-modules-and-frameworks-auto-discovered-req and REQ-framework-discovery-signal-req need no fallback or redesign; the mechanism they assume genuinely exists and works in real bsd make. This is the strongest positive result of the validation pass, directly de-risking the auto-discovery philosophy that the whole directory-layout design leans on.
