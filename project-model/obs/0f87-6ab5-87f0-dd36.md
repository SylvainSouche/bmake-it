---
schema_version: 2
id: 0f87-6ab5-87f0-dd36
alias: sys-mk-cflags-defaults-to-o2
type: obs
status: recorded
origin: found incidentally while debugging why a sanitizer-catch test fixture's compile log showed -O2 despite no -O flag existing anywhere in mk/*.mk; confirmed via 'bmake -f /dev/null -V DBG' -> -O2 and 'bmake -f /dev/null -V CFLAGS' -> ${DBG}, on this MacPorts host, 2026-09-24
created: 2026-09-24
rel_informed_by: [6f38-6a8c-b939-38a7]
generator: model.py/2.9.0
checksum: 93ae6d43f32b
---

# bmake's own sys.mk sets CFLAGS = ${DBG} with DBG = -O2 by default, silently applied to every build since mk.common.mk appends to CFLAGS rather than resetting it -- directly relevant to the still-open 'release/optimization flags' spec question

Not investigated further or acted on this round -- cxx-readiness item 4 was about rebuild-on-flag-change, not about deciding the optimization-flags question docs/spec/40-cli-reference.md's own 'Debug/release and extra compile flags' section already flags as unresolved. Recorded because it's directly load-bearing evidence for that open decision (an optimization default already exists today, invisibly, via bmake's own bootstrap sys.mk rather than by this project's own design) and was worth capturing before it was lost -- not yet checked whether this DBG/-O2 default is portable-bmake-specific, FreeBSD-base-specific, or universal across the phase-1 hosts.
