---
schema_version: 2
id: 0f87-6a98-2b92-702c
alias: vm-graceful-shutdown-and-netbsd-native-confirmed-dec
type: dec
status: recorded
origin: user booted the NetBSD VM with a GUI console and diagnosed the real fsck/sshd/pkgsrc failures directly, 2026-09-02
created: 2026-09-02
rel_resolves: [0f87-6a98-2997-bfb8]
generator: model.py/2.1.0
checksum: 535fb188d701
---

# VM tooling now shuts down guests gracefully; NetBSD arm64 native testing confirmed working under gcc (llvm blocked by unrelated pkgsrc corruption fallout)

vm-lib.sh gained vm_shutdown() (ssh 'shutdown -p now'/'poweroff' first, falls back to kill only if already unreachable) -- every VM stop from here on should use it, never a bare kill. Also fixed vm_scp_in() to strip macOS xattrs the same way pack-cases.sh and the earlier Docker/cross scripts already did (COPYFILE_DISABLE + --no-xattrs), which this function had been missing. Separately: clang/llvm installation via pkgsrc on this same VM is still blocked by a python313 package whose files have mismatched MD5 checksums against the pkgdb (pkg_add refuses to touch them) -- another artifact of the same filesystem corruption event, not yet resolved (rm -rf of the package dir alone wasn't sufficient; the failure persisted identically after that). gcc is unaffected since it's part of NetBSD's base system, not a pkgsrc package -- confirmed with a real end-to-end run: 27/29 under TOOLCHAIN=gcc (the same 2 known toolchain-default-assertion cases as every other forced-gcc run this session, not new failures), including both PARENT_WS test cases passing on genuine NetBSD hardware-target output.
