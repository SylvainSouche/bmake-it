---
schema_version: 2
id: 0f87-6aa0-3c04-edfb
alias: harness-tests-parent-ws-resolution
type: dec
status: recorded
origin: user statement, original rade-bmake design chat (2026-09-02); retroactively recorded 2026-09-08 from chat export and project-model snapshot, matching content already implemented/evidenced in the current model
created: 2026-09-08
facets: [architecture]
generator: model.py/2.1.0
checksum: 9984ec237365
---

# Test cases must specifically cover PARENT_WS resolution: with no parent workspace, with a parent workspace providing something not found locally, and with the SAME framework/file present in both the local workspace and a parent (or multiple parents) at different versions, verifying the correct one is actually used

User specified dedicated coverage for workspace-parenting behavior: (1) baseline with PARENT_WS= empty, confirming a workspace with no parents only sees its own frameworks; (2) a workspace with a framework NOT present locally but present in a declared parent, confirming it resolves correctly via the fallback search; (3) the more subtle and important case -- alternative/conflicting versions of the same-named framework or file present in BOTH the local workspace and a parent (or in multiple parents at different positions in the ordered PARENT_WS list) -- verifying which version actually gets used matches the documented resolution order (local first, then parents in declared order).
