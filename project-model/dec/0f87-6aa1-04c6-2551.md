---
schema_version: 2
id: 0f87-6aa1-04c6-2551
alias: linux-glibc-musl-arch-tiering
type: dec
status: recorded
origin: user statement, 2026-09-01; recovered 2026-09-09 from the original design chat's transcript text -- this object was created after the chat's 196-object export checkpoint into mkmk and was lost when that chat's sandbox reset on 2026-09-04, never re-synced
created: 2026-09-09
facets: [architecture]
rel_resolves: [6f38-6a8a-0a38-cffe]
generator: model.py/2.5.0
checksum: 8d7079fef7a4
---

# Linux first-class target tiering: glibc is supported on both amd64 and arm64; musl is first-tier on amd64 only -- musl-arm64 is not first-class

User: 'linux : one musl one glibc, glibc on amd/arm, musl only amd as first tier.' Confirmed as part of the broader first-class target matrix expansion alongside the Windows tiering work in the same session. Resolves the open axis flagged by linux-libc-choice-parallels-win-abi, which had deferred the libc-choice question 'along with the rest of Linux-specific scope' -- that scope is no longer entirely deferred; this is the specific resolution for the libc axis. This was recorded as part of a broader target-matrix confirmation that also covered Windows tiering (macOS-aarch64 {llvm,gcc}; Linux-amd64 & Linux-arm64 glibc {gcc,llvm}, Linux-amd64 musl {gcc,llvm}; FreeBSD-amd64 {llvm,gcc}; Windows-amd64 tier-1 MSVC+Cygwin, tier-2 MinGW) -- that fuller matrix, and its relationship to the narrower confirmed phase1-concrete-targets (macOS + FreeBSD only, four targets), was not itself formally recorded as a superseding object before the reset, and is a separate open question from this one.
