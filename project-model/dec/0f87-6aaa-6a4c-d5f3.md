---
schema_version: 2
id: 0f87-6aaa-6a4c-d5f3
alias: report-flag-uniform-trigger-v2
type: dec
status: recorded
origin: corrected 2026-09-16 after the first design (REPORT= also gating continue-vs-abort) was found wrong via direct user correction
created: 2026-09-16
rel_decided_by: [0f87-6aaa-6a4c-3725]
rel_supersedes: [0f87-6aaa-661a-ada2]
generator: model.py/2.4.0
checksum: e74b9b9daa04
---

# One uniform REPORT=yes flag, not separate -all target names, triggers dashboard generation for build, test, and sanitizing-test runs alike -- not a separate target name per action. Whether a failure stops the run is a SEPARATE, independent flag (FAIL_FAST=), not coupled to REPORT= (see fail-fast-independent-of-report)

Supersedes 0f87-6aaa-661a-ada2 (One uniform REPORT=yes flag, not separate -all target names, triggers report/dashboard generation for build, test, and sanitizing-test runs alike -- the default (REPORT unset) target always just does the work (build/test) with no report overhead and, for build, keeps today's fail-fast-on-first-error behavior for normal dev use; REPORT=yes additionally makes a workspace/framework build continue past one module's failure (so every module gets a chance to build and be reported) and generates the aggregated dashboard).

Carries forward what remained valid and states what changed.
