---
schema_version: 2
id: 6f38-6a8a-0550-3cf8
alias: framework-prereqs-macro
type: dec
status: recorded
origin: user statement (annotated directory-layout.md, 2026-08-22)
created: 2026-08-22
facets: [architecture]
generator: migrate.py/v2
checksum: d75b9e8aa648
---

# Framework makefile uses PREREQS= (space-delimited, ORDERED list of frameworks this one may link_with); public-only (no visibility levels); misordering causes a build error, cycle-detection behavior still to be confirmed

User specified the concrete macro for framework-level prereqs (refining REQ-framework-prereqs and REQ-uniform-makefile-filename-req): PREREQS=fw1 fw2 ... , equivalent in purpose to real mkmk's AddPrereqComponent calls in IdentityCard.h, but without any public/protected/private qualifier per prereq entry -- every declared prereq is implicitly public in mkmk terms (matches REQ-three-level-header-visibility-req, which only distinguishes visibility at the header-directory level, not per-prereq-declaration). The list is explicitly ORDERED, and user flagged that an incorrectly ordered list should raise a build error -- though the exact mechanics (what misordered means, whether this is really about detecting dependency cycles) were flagged by the user themselves as to be confirmed later. This is recorded as an open follow-up, not silently resolved.
