---
schema_version: 2
id: 0f87-6aa9-4485-ade4
alias: shared-lib-rpath-fix-dec
type: dec
status: recorded
origin: resolves shared-lib-not-runtime-loadable-obs, 2026-09-15
created: 2026-09-15
rel_resolves: [0f87-6aa9-447f-0d48]
generator: model.py/2.4.0
checksum: 30b72dc7cb26
---

# Fix: shared libs get an @rpath install name (macOS) / rely on soname (ELF); every consumer that links against a LIBS= shared lib also gets a matching -Wl,-rpath for each resolved lib-search directory -- skipped on Windows (no rpath concept)

Applied in mk.lib.mk (install_name -> @rpath/${SHLIB_NAME}; -Wl,-rpath added for the lib's own LIBS= dependencies) and mk.prog.mk (-Wl,-rpath added for its LIBS= dependencies) and mk.test.mk (-Wl,-rpath added for the module's own just-built lib, which test binaries link directly rather than via LIBS=). Verified end-to-end: greet_test now loads libgreet.1.dylib and runs without DYLD_LIBRARY_PATH or any install step.
