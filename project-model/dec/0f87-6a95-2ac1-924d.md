---
schema_version: 2
id: 0f87-6a95-2ac1-924d
alias: view-drift-refines-cycle-fix-dec
type: dec
status: recorded
origin: cycle diagnosis during view-drift review, 2026-08-31
created: 2026-08-31
facets: [architecture]
generator: model.py/2.1.0
checksum: 302221a68e8d
---

# Two refines edges in the toolchain-selection cluster were miscategorized in the original v1 authoring, forming a genuine 3-way refines cycle; both are converted to informed_by

model.py check --strict's new REFINES-DEAD check surfaced a chain-following loop across toolchain-cli-variable-req, target-arch-toolchain-selection-is-custom-req (nee target-selection-uses-bmake-native-vars-req), and toolchain-triggers-dedicated-mk-file-req: each refined the next, closing a cycle that predates any migration or view-drift work -- it was present in the original v1 authoring and never caught because v1 ran no cycle detection. Diagnosis: the three are genuinely distinct requirements (different variables, different facets), not near-duplicates needing a merge. Two of the three refines edges do not meet refines' own bar ('a more precise statement of' the SAME underlying requirement): toolchain-cli-variable-req's own body justifies its edge with 'completes the story alongside' (peer language, not precision), and target-arch-toolchain-selection-is-custom-req's edge cites a different variable (TARGET/TARGET_ARCH vs TOOLCHAIN) for background/location, not precision. Only toolchain-triggers-dedicated-mk-file-req's refines of toolchain-cli-variable-req is genuine (same variable, TOOLCHAIN, elaborated with operational precision) and is kept unchanged. The other two are re-created as informed_by edges, breaking the cycle without merging or discarding any content.
