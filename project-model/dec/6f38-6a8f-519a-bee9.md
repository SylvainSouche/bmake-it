---
schema_version: 2
id: 6f38-6a8f-519a-bee9
alias: clean-removes-all-derived-artifacts
type: dec
status: recorded
origin: user statement (2026-08-26)
created: 2026-08-26
facets: [architecture]
rel_informed_by: [6f38-6a8b-de94-3b56, 6f38-6a8c-195e-457f, 6f38-6a8c-2159-4b07, 6f38-6a8c-b76b-0154, 6f38-6a8e-7e12-a30d]
generator: model.py/2.4.0
checksum: c603ef92e3f0
---

# make clean must remove EVERYTHING derived/generated, not just build/<KEY>/: also distrib/<KEY>/, per-module .depend files, the PARENT_WS/PREREQS resolution cache, and the dependency-order generation cache -- leaving only original source

User broadened the scope of what make clean removes: 'everything that is not original source.' The previously-confirmed REQ-clean-target-scope-req only covered build/<KEY>/ (obj/bin/lib/share). This misses several other generated artifacts introduced later in the design: distrib/<KEY>/ (packaging staging, REQ-distrib-dir-for-packaging-staging-req); per-module .depend files (REQ-mkdep-style-header-dependency-tracking-req -- per real bsd.dep.mk convention, these typically live alongside the source/makefile, NOT under build/<KEY>/); the PARENT_WS/PREREQS resolution cache (REQ-prereqs-resolution-cached-like-depend-req); and the dependency-order generation cache (REQ-dep-order-generation-mechanism-req, the prototyped .gen-mod-order.mk-style file). make clean must remove ALL of these, not just build/<KEY>/, while the already-decided TARGET=/TARGET_ARCH=/TOOLCHAIN=-based scoping (current target vs. TARGET=all for every target) still applies to whichever of these are target-keyed.
