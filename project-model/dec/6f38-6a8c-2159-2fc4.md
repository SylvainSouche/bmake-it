---
schema_version: 2
id: 6f38-6a8c-2159-2fc4
alias: prereqs-resolution-cached-like-depend
type: dec
status: recorded
origin: user statement (clarifying question, 2026-08-22)
created: 2026-08-24
facets: [architecture]
generator: migrate.py/v2
checksum: ab4c85548ff5
---

# PARENT_WS/PREREQS resolution is cached as a generated .mk fragment, auto-.included by the relevant makefile, matching real bsd makes own .depend mechanism -- regenerated only when triggered/detected stale, not re-walked on every make invocation

User confirmed the caching approach, explicitly modeled on real bsd makes .depend file: a generated dependency file that gets produced by an explicit step (make depend) and is then .included automatically by subsequent make invocations, rather than being recomputed every time. Applied here: PARENT_WS resolution (REQ-workspace-parent-macro-absolute-req) and framework PREREQS resolution (REQ-framework-prereqs-macro-req, determining both header search paths and build order per REQ-framework-build-order-follows-prereqs-graph-req) produce a generated .mk fragment that is .included by the workspace/framework makefile on subsequent runs, instead of being re-walked from scratch on every invocation. Exact filename, staleness-detection mechanism (timestamp-based vs. explicit-trigger-only, matching how real bsd make handles .depend staleness), and regeneration trigger (an explicit target like make depend, or automatic detection) are not yet specified -- open follow-up for the implementation phase, but the caching APPROACH itself is confirmed.
