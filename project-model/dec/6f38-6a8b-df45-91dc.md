---
schema_version: 2
id: 6f38-6a8b-df45-91dc
alias: install-target-exists
type: dec
status: recorded
origin: user statement (clarifying question, 2026-08-22)
created: 2026-08-24
facets: [architecture]
rel_informed_by: [6f38-6a8a-0550-b9ed, 6f38-6a8b-655d-b8d7]
generator: model.py/2.4.0
checksum: d676a4600450
---

# make install exists as a distinct target, copying build/<KEY>/{bin,lib,share} to a real install destination (DESTDIR/PREFIX-style), separate from the make port/pkg/etc. packaging targets

User confirmed a make install target exists, distinct from the named per-format packaging targets (REQ-packaging-per-format-targets-req). It copies the current targets build/<KEY>/{bin,lib,share} content to a real install destination. Exact variable names for the destination not explicitly confirmed by the user yet -- proposing reuse of bsd makes own native DESTDIR/PREFIX convention (staged-root DESTDIR, final path PREFIX under it), consistent with the established pattern of reusing bsd-make-native variable names where they exist (REQ-target-selection-uses-bmake-native-vars-req did the same for TARGET=/TARGET_ARCH=). Flagged as proposed/inferred, not literally confirmed -- open follow-up to verify.
