# Project model index

schema_version 2 · 416 objects · regenerated 2026-09-24

Generated by model.py. Do not hand-edit. Read or grep this before opening any object file; paths are `project-model/<type>/<id>.md`.

| type | id | alias | status | title |
|---|---|---|---|---|
| con | 0f87-6a98-a928-6065 | cygwin-scoped-to-x86-family-only | recorded | Cygwin -- both as the host-side supplier of sh/bmake/tooling for tier-1 MSVC builds, and as the 'Cygwin-native gcc/llvm' tier-1 ABI in its own right -- is scoped to x86/amd64 targets only; it can never produce or represent arm64 win-target output, since the cygwin1.dll runtime it depends on has no arm64 port. arm64 win-target output is reachable only through MSVC (genuinely multi-arch-capable per host VS install) or mingw-w64 cross-compilation (tier 2, via an aarch64-w64-mingw32 sysroot) |
| con | 0f87-6aaa-643a-9397 | ci-integration-scope-boundary | recorded | Bmake It produces build/test/sanitize reports as static files on local disk; it does not fetch from version control (git/GitHub or otherwise) and does not publish or serve those reports anywhere -- an external integration-manager/CI process owns both of those, using Bmake It purely for build+test+report generation |
| con | 6f38-6a88-1836-b9b1 | use-bsdmake | recorded | Build system shall be implemented in BSD make, using bsd.*.mk infrastructure or reprogrammed equivalents |
| con | 6f38-6a88-1836-d9b9 | cross-platform-targets | recorded | Build system must run on macOS (third-party package-manager toolchain), Linux, and *BSD |
| dec | 0f87-6a94-6957-6170 | view-drift-refines-repoint-dec | recorded | Objects whose refines edge pointed at an ancestor later superseded are re-pointed to that ancestor's live successor |
| dec | 0f87-6a95-2ac1-924d | view-drift-refines-cycle-fix-dec | recorded | Two refines edges in the toolchain-selection cluster were miscategorized in the original v1 authoring, forming a genuine 3-way refines cycle; both are converted to informed_by |
| dec | 0f87-6a95-5088-b94c | lib-default-strips-lib-prefix-dec | recorded | LIB=' default value additionally strips a leading 'lib' prefix from the module directory name, to avoid a redundant liblibfoo.so output filename |
| dec | 0f87-6a95-6a3a-7038 | nested-invocation-reentrancy-guard-dec | recorded | Every mechanism that shells out to a nested make invocation for discovery or dependency-order analysis must guard against self-reentrant and concurrent re-invocation, as a general architectural rule -- not a one-off fix to the one instance found |
| dec | 0f87-6a95-78d4-6146 | toolchain-detection-must-search-path-dec | recorded | Toolchain compiler detection must search PATH (or a defined list of install prefixes) and fail loudly when the requested toolchain's compiler cannot be found, never silently substitute a different compiler by falling through a hardcoded single-path check |
| dec | 0f87-6a95-afab-771b | mod-order-sysroot-no-hardcoded-default-dec | recorded | Stop hardcoding /opt/local/share/mk as the BMK_SYS_MK default in mk.framework.mk; let gen-mod-order.sh's own multi-path search run instead |
| dec | 0f87-6a95-b3ca-44d7 | cross-compile-via-clang-target-sysroot-dec | recorded | Cross-compilation implemented via clang --target=/--sysroot=, one sysroot per target OS, never bundled |
| dec | 0f87-6a97-0c22-fb83 | prereqs-parent-ws-fallback-dec | recorded | Implement the PARENT_WS PREREQS fallback: forward PARENT_WS through both recursion levels, search it only when the local sibling framework is absent |
| dec | 0f87-6a98-2b92-702c | vm-graceful-shutdown-and-netbsd-native-confirmed-dec | recorded | VM tooling now shuts down guests gracefully; NetBSD arm64 native testing confirmed working under gcc (llvm blocked by unrelated pkgsrc corruption fallout) |
| dec | 0f87-6a98-5d9c-e9ad | cross-target-catch-all-error-dec | recorded | Add a final .else .error to the cross-compile target chain, naming the recognized targets, instead of silently falling through to a native build |
| dec | 0f87-6a98-5db8-a528 | cross-compile-sysroot-check-scoped-to-builds-dec | recorded | Scope the cross-compile sysroot requirement check to invocations that can actually reach a compile step, using bmake's own make(clean) predicate and a .MAKEFLAGS:M-V* check |
| dec | 0f87-6a98-7bca-d340 | run-target-common-mk-walkup-dec | recorded | Move run: out of mk.workspace.mk into mk.common.mk (shared by all four roles) with a directory walk-up: it locates the nearest ancestor makefile that includes mk.workspace.mk and re-execs make run there, so PROGRAM=/ARGS= always resolve against the workspace's own aggregated bin/lib regardless of which role directory bmake run was invoked from; also adds an explicit .MAIN: all, since common.mk is parsed before any role's own all: target and run: would otherwise silently become bmake's default goal for a bare invocation |
| dec | 0f87-6a98-92d8-0b97 | windows-toolchain-tiered-msvc-cygwin-mingw | recorded | Windows target support is tiered across three toolchain architectures: tier 1 is native MSVC (cl.exe/link.exe) run from a Cygwin-hosted bmake shell, and Cygwin-native gcc/llvm run the same way; tier 2 is mingw-w64 cross-compilation from any capable host. Supersedes the prior exclusion of native MSVC |
| dec | 0f87-6a9a-c666-3b42 | project-renamed-bmake-it | recorded | Project renamed from the working title 'rade-bmake' to 'Bmake It', with 'bmk' as the practical shorthand/variable-prefix (RADE_* -> BMK_*, rade_export.h -> bmk_export.h); the implementation directory renamed from grok/ (an arbitrary internal codename) to impl/. Legitimate attribution to CAA/RADE as a design inspiration (in README.md and bmk_export.h's own comment, both explicitly crediting Dassault Systemes RADE and the ExportedBy pattern) is kept -- crediting an influence honestly is different from using its name as your own |
| dec | 0f87-6aa0-3be6-84ee | harness-comprehensive-feature-coverage | recorded | The test harness must cover every feature of the system, not just isolated bsd-make mechanics: including actually running the built program (not just checking it compiled/linked) and verifying link resolution actually worked, for both nominal and error cases |
| dec | 0f87-6aa0-3c04-edfb | harness-tests-parent-ws-resolution | recorded | Test cases must specifically cover PARENT_WS resolution: with no parent workspace, with a parent workspace providing something not found locally, and with the SAME framework/file present in both the local workspace and a parent (or multiple parents) at different versions, verifying the correct one is actually used |
| dec | 0f87-6aa0-3c28-7d44 | harness-tool-presence-check-or-install | recorded | Before running tests (and by extension, before a real build), required tools (compilers, linkers, yacc/lex, resource compilers, mkdep, etc.) must be checked for presence; if a required tool is missing, either install it automatically or produce a meaningful diagnostic naming exactly what is missing and how to obtain it -- never fail with an opaque command-not-found error |
| dec | 0f87-6aa0-3c3d-862d | harness-tests-full-cross-build-matrix | recorded | From each build host, the test harness must exercise building for every first-class cross-compile target with every applicable toolchain -- not just the hosts own native target |
| dec | 0f87-6aa0-3c52-50a8 | harness-vm-runtime-for-cross-targets | recorded | A cross-compiled binary cannot run on the build host, so verifying its RUNTIME behavior (not just that it compiled) requires provisioning and running it on a VM of the target platform; this must happen without user intervention, OR -- if full automation is not achieved -- be clearly documented, with the harness itself detecting whether the required VM environment is present and, if not, directing the user to that documentation rather than failing silently or cryptically |
| dec | 0f87-6aa0-3c6c-7a1d | harness-native-builds-also-in-vms | recorded | Once VM-based runtime verification exists for cross-compiled targets, the same approach should also be used for NATIVE host builds -- running the native test suite inside a VM of that host OS too, rather than directly on whatever machine happens to be running the harness, for consistency and reproducibility |
| dec | 0f87-6aa1-048a-d54b | graceful-failure-cross-cutting | recorded | All execution paths must fail gracefully, with explanation, when run with invalid options or in an inappropriate location |
| dec | 0f87-6aa1-04a5-9f70 | windows-shlib-versioninfo-uniform-naming | recorded | Windows shared-library versioning is uniform across all three Windows ABIs (MSVC, Cygwin, MinGW): no filename-embedded version number, no Cygwin cyg-prefix special case -- SHLIB_MAJOR/SHLIB_MINOR translate to an embedded VERSIONINFO resource instead |
| dec | 0f87-6aa1-04c6-2551 | linux-glibc-musl-arch-tiering | recorded | Linux first-class target tiering: glibc is supported on both amd64 and arm64; musl is first-tier on amd64 only -- musl-arm64 is not first-class |
| dec | 0f87-6aa7-bf6f-3c26 | harness-target-matrix-reconciled | recorded | Reconcile and pin the full test-harness target matrix, superseding the stale two-pair phase1 scope: macOS-arm64 (host-native, gcc+llvm), FreeBSD-amd64 (14.4/15.0/15.1, gcc+llvm), NetBSD-amd64+arm64 (10.1/11.0, gcc+llvm), Linux-amd64+arm64 glibc (Debian 13.7, gcc+llvm), Linux-amd64 musl first-tier (Alpine 3.24.1, gcc+llvm; Linux-arm64 musl not first-tier), Windows-amd64 tiered (tier 1: native MSVC + Cygwin gcc/llvm; tier 2: mingw-w64 gcc/llvm cross) |
| dec | 0f87-6aa7-bf86-e31e | version-freshness-caching-policy | recorded | For every cached OS base image and toolchain version (gcc/llvm per target, MSVC/Cygwin/mingw for Windows), bmake it checks upstream for the latest stable release on demand; a cached version that is behind latest but still recent only warns, it does not auto-replace the cache |
| dec | 0f87-6aa7-bf94-1066 | harness-cache-management-cli | recorded | The harness exposes three explicit CLI targets for cache/VM lifecycle: one to download a target's OS image into the local cache and provision its VM environment, one to force the cache to supersede to the latest available version for a target, and one to display per-target status: latest version available upstream, version currently cached, and whether a VM is actually built |
| dec | 0f87-6aa7-c4f1-91a5 | vm-nesting-reentrancy-guard | recorded | When the test harness is itself running inside a VM (per harness-native-builds-also-in-vms-req), it must not launch another VM from within that VM -- it can only cross-test back against the original host environment, never spawn nested/sibling VMs |
| dec | 0f87-6aa7-c898-0d1e | cross-compile-universal-coverage | recorded | Cross-compilation must work from any supported host to any supported target; a configuration that is genuinely impossible (licensing or technical reason) must be explicitly stated as a documented limit, never a silent gap |
| dec | 0f87-6aa7-c8a9-0c94 | cross-test-universal-coverage | recorded | Testing (running/verifying built binaries) must work from any supported host to any supported target; a configuration that is genuinely impossible must be explicitly listed and documented as a limit -- e.g. running a macOS guest on non-Apple hardware, which Apple's own licensing prohibits |
| dec | 0f87-6aa7-c8b2-12cd | grid-infrastructure-alternative-to-vm | recorded | For a real production build/test operational environment, the local VM-provisioning mechanism can be replaced by a grid infrastructure: a pool of identified, dedicated virtual or physical machines usable for build or test, instead of provisioning VMs on demand |
| dec | 0f87-6aa7-df9c-d688 | doc-generation-doxygen-based | recorded | Documentation generation uses Doxygen as both parser and renderer for v1 (a later pluggable DOCS_RENDERER=doxygen|sphinx may consume Doxygen's XML for a richer Sphinx-rendered site). Each framework gets its own Doxyfile+run+tag-file, chained in PREREQS= topological order for cross-framework HTML links -- validated by CAA's own mkmancpp tool, which independently uses the identical framework+prerequisite-based linking model. A workspace-level aggregation step generates a comprehensive cross-workspace name index, a framework-listing landing page, and a workspace-wide inheritance-hierarchy view, adopted from mkmancpp's AllNames.htm/main.htm/tree.htm precedent. Default scope is public headers only (fw/include/), matching CAA's own public+protected default with private opt-in |
| dec | 0f87-6aa7-dfb2-0138 | doc-generation-local-qa-trigger | recorded | A separate trigger generates internal-scope documentation -- public plus framework-local headers (fw/local/include/), not just public -- for the framework's own developers, and doubles as a quality-assurance gate: Doxygen's own documentation-completeness warnings (undocumented/incomplete entries) are escalated to hard failures in this mode rather than left as informational warnings |
| dec | 0f87-6aa7-e041-cde8 | doc-generation-invocation-scope | recorded | Documentation generation is invokable at framework scope (bmake docs from within one framework, generating just that framework's own docs) or workspace scope (bmake docs from the workspace root, generating docs for every framework present in the workspace via the same per-framework mechanism, plus the workspace-level aggregation artifacts) |
| dec | 0f87-6aa7-e392-3337 | doc-pdf-docwith-explicit-grouping | recorded | PDF cluster membership is declared explicitly via a DOCWITH= macro per framework, not inferred from PREREQS= connectivity -- a full transitive-closure walk of PREREQS collapses into 'everything' for any non-leaf framework, the same failure mode as a single combined workspace PDF, so grouping must be a deliberate, curated declaration instead of derived from the dependency graph |
| dec | 0f87-6aa8-00e9-3173 | unit-test-feature-for-built-software | recorded | End-user modules/frameworks built with Bmake It can declare and run their own unit tests, via an agnostic test orchestrator: a test declaration is a command -- built-then-run for compiled test sources, or a bare existing script/program -- plus exit-code check, timeout, and labels, agnostic to what produces the pass/fail, reporting to JUnit XML for CI compatibility |
| dec | 0f87-6aa8-00fc-a039 | unit-test-atf-kyua-adoption | recorded | Test writing and orchestration is built on ATF (atf-c/atf-c++ for compiled test cases, atf-sh for shell-script test cases) and Kyua (the runner), reusing the proven bsd.test.mk declarative model natively within Bmake It's own mk.*.mk -- not a CMake/CTest dependency, since Bmake It is itself a CMake alternative and depending on CTest would be self-contradictory |
| dec | 0f87-6aa8-06c0-50b7 | sanitizer-support | recorded | Compiler/linker sanitizer instrumentation (AddressSanitizer, UndefinedBehaviorSanitizer, ThreadSanitizer, MemorySanitizer, LeakSanitizer) is selected via a SANITIZE= build variable (comma-separated, e.g. SANITIZE=address,undefined), composing with both make run and make test since it's a general compile/link-flag concern, not test-specific |
| dec | 0f87-6aa8-06d2-914f | unit-test-full-suite-html-report | recorded | Running the full test suite generates a full HTML report in addition to the JUnit XML machine-readable output -- JUnit XML serves CI integration, the HTML report serves human review of a full run, matching CAA mkodt's own -html precedent |
| dec | 0f87-6aa8-072c-0662 | doc-generation-per-language-tool-pluggable | recorded | Documentation generation must not be permanently locked to Doxygen -- Doxygen is the mainstream tool choice specifically for C/C++, Bmake It's current language scope; when language support extends (Java, Fortran, IDL, Express are already noted as deferred-not-ruled-out by supported-languages-req), the appropriate mainstream doc tool for that language must be pluggable in, not forced through Doxygen |
| dec | 0f87-6aa8-096b-0b7b | unit-test-per-language-tool-pluggable | recorded | Unit-test tooling must not be permanently locked to ATF/Kyua -- ATF (atf-c/atf-c++) is C/C++-specific; when language support extends beyond C/C++, the appropriate mainstream test-writing/orchestration tool for that language must be pluggable in, matching the same principle established for documentation generation |
| dec | 0f87-6aa8-fee3-e68d | per-level-local-mk-directory | recorded | Each workspace, framework, and module may have its own mk/ directory, taken into account for .include resolution with cascading visibility: at workspace level only WS/mk is visible; from within a framework, WS/mk and FW/mk; from within a module, WS/mk, FW/mk, and MOD/mk. WS/mk's own search also extends to every PARENT_WS's mk/ directory, recursively, matching PARENT_WS's existing cascading semantics for framework/header resolution |
| dec | 0f87-6aa9-0035-144b | local-mk-hook-files-and-cascade-order | recorded | Local customization uses six conventional, specifically-looked-for filenames (pre.mk, local.mk, pre.${TOOLCHAIN}.mk, local.${TOOLCHAIN}.mk, pre.${TARGET}.mk, local.${TARGET}.mk -- pre.* included before a role file's own defaults are computed, local.* after), mirroring the existing mk.toolchain.${TOOLCHAIN}.mk / mk.paths.${_HOST_OS}.mk pattern rather than an unconditional glob of the directory. When multiple levels each have a matching file, ALL are included (not most-specific-wins), in workspace -> framework -> module order -- simpler than an override rule, and consistent with CFLAGS += style accumulation rather than full replacement |
| dec | 0f87-6aa9-038c-9749 | doc-project-metadata-conventions | recorded | Doxygen's own standard project-identity fields (PROJECT_NAME/PROJECT_NUMBER/PROJECT_BRIEF/PROJECT_LOGO, HTML_FOOTER for a license notice) are populated by auto-detecting conventional workspace-root files -- VERSION, LICENSE, docs/logo.{png,svg} -- defaulting to the framework/workspace directory name for PROJECT_NAME when no override is given. No bespoke override mechanism is needed: a workspace or framework's own pre.mk (per local-mk-hook-files-and-cascade-order-req) already runs before this auto-detection, so DOC_PROJECT_NAME=/DOC_PROJECT_VERSION=/DOC_PROJECT_BRIEF=/DOC_LOGO= set there are respected |
| dec | 0f87-6aa9-39c1-674b | toolchain-detection-prefix-list-authoritative | recorded | Toolchain compiler detection checks each OS's own _TOOL_PREFIXES list, in order, BEFORE falling back to ambient /Users/sylvain/miniconda3/bin:/Users/sylvain/miniconda3/condabin:/opt/local/bin:/opt/local/sbin:/usr/local/bin:/System/Cryptexes/App/usr/bin:/usr/bin:/bin:/usr/sbin:/sbin:/var/run/com.apple.security.cryptexd/codex.system/bootstrap/usr/local/bin:/var/run/com.apple.security.cryptexd/codex.system/bootstrap/usr/bin:/var/run/com.apple.security.cryptexd/codex.system/bootstrap/usr/appleinternal/bin:/pkg/env/global/bin:/Library/Apple/usr/bin:/Library/TeX/texbin:/Library/Frameworks/Mono.framework/Versions/Current/Commands:/Users/sylvain/.lmstudio/bin:/Users/sylvain/Library/pnpm:/Users/sylvain/Library/Application Support/Claude/local-agent-mode-sessions/skills-plugin/540095af-33d3-4b54-9071-8a4d7327ae1c/ae9804db-c018-421b-b62c-0d0869405214/bin via command -v -- reversing the current priority, so the result is deterministic and controlled by Bmake It's own per-OS ordering (already correct everywhere except macOS) rather than by whatever PATH the invoking shell happens to have. macOS's own _TOOL_PREFIXES reordered to put third-party package-manager prefixes first (MacPorts /opt/local/bin, Homebrew /opt/homebrew/bin and /usr/local/bin, Nix ~/.nix-profile/bin and /run/current-system/sw/bin, pkgsrc/Darwin /opt/pkg/bin), Apple's own /usr/bin last |
| dec | 0f87-6aa9-3a88-4c34 | macos-toolchain-tiers | recorded | macOS third-party package-manager toolchains are tiered: MacPorts, Homebrew, and pkgsrc/pkgin are tier 1 (fully supported, searched first); Nix is tier 2 (still searched, lower priority) -- Apple's own /usr/bin stays last regardless of tier, per toolchain-detection-prefix-list-authoritative-req |
| dec | 0f87-6aa9-3edd-8119 | macos-multi-pkgmgr-single-vm | recorded | Testing Bmake It against multiple macOS package-manager toolchains (MacPorts/Homebrew/pkgsrc-pkgin/Nix) does not need one VM per manager -- they're userland installs to different prefixes on the same macOS kernel, coexisting fine on one system (that's what _TOOL_PREFIXES already relies on). The single already-planned macos_arm64 tart VM gets all four installed side by side; the harness selects which one a given test run exercises by pointing _TOOL_PREFIXES/environment at that manager's own prefix, not by provisioning separate VMs |
| dec | 0f87-6aa9-4485-ade4 | shared-lib-rpath-fix-dec | recorded | Fix: shared libs get an @rpath install name (macOS) / rely on soname (ELF); every consumer that links against a LIBS= shared lib also gets a matching -Wl,-rpath for each resolved lib-search directory -- skipped on Windows (no rpath concept) |
| dec | 0f87-6aaa-661a-ada2 | report-flag-uniform-trigger | superseded | One uniform REPORT=yes flag, not separate -all target names, triggers report/dashboard generation for build, test, and sanitizing-test runs alike -- the default (REPORT unset) target always just does the work (build/test) with no report overhead and, for build, keeps today's fail-fast-on-first-error behavior for normal dev use; REPORT=yes additionally makes a workspace/framework build continue past one module's failure (so every module gets a chance to build and be reported) and generates the aggregated dashboard |
| dec | 0f87-6aaa-6a4c-3725 | fail-fast-independent-of-report | recorded | REPORT=yes and FAIL_FAST=yes are two independent flags, not one: REPORT=yes controls only whether the workspace-level dashboard gets built (build-report/ or test-report/) from whatever a run left behind; FAIL_FAST=yes controls only whether one module's/framework's failure stops a workspace/framework all/test run before every other one gets attempted. Default for both is the continue-past-failure, no-dashboard behavior -- matching how test already behaved before REPORT= existed -- not fail-fast-by-default |
| dec | 0f87-6aaa-6a4c-d5f3 | report-flag-uniform-trigger-v2 | recorded | One uniform REPORT=yes flag, not separate -all target names, triggers dashboard generation for build, test, and sanitizing-test runs alike -- not a separate target name per action. Whether a failure stops the run is a SEPARATE, independent flag (FAIL_FAST=), not coupled to REPORT= (see fail-fast-independent-of-report) |
| dec | 0f87-6aaa-72be-8624 | run-id-identifies-logs | recorded | Per-run logs/reports are identified by RUN_ID=, defaulting to a timestamp+PID (date +%Y%m%d-%H%M%S-$$, collision-safe without needing GNU-only date extensions like %N), overridable so an external CI can pass its own build number or commit SHA instead -- keeps Bmake It's own VCS-blindness (ci-integration-scope-boundary) rather than hardcoding git awareness. Computed once at the top of a recursive run and forwarded down like SANITIZE=/REPORT=/FAIL_FAST=, so every module/framework touched by one run shares the same RUN_ID. A runs/latest symlink is kept pointing at the newest run, since a LAN site needs a stable link, not a timestamp to guess |
| dec | 0f87-6aaa-8c69-f751 | qt-mechanism-original-implementation | recorded | mk.qt.mk is designed from Qt's own documented, public tool contracts (moc/uic/rcc command-line behavior and output-file naming conventions) -- third-party prior art (FreeBSD ports' bsd.qt.mk, community example Makefiles) may only be consulted to confirm factual tool behavior, never as a text or code source; no copy-paste from any of it, regardless of that project's own license |
| dec | 0f87-6aaa-d63e-839a | bmk-mkdir-discovery-via-makesyspath | recorded | Bmake It's own mk/ role files are discovered by an out-of-tree project via bmake's native MAKESYSPATH mechanism, not MAKEFLAGS/-I -- a search path is the correct conceptual fit for shared/reusable make files (what <bsd.prog.mk>-style system includes already use, and what other bsd-make tooling on the same machine already relies on), not a general-purpose flags variable. Computed as MAKESYSPATH="<bmake-it>/mk:${MAKESYSPATH:-$(bmake -f /dev/null -V .SYSPATH)}" -- the -f /dev/null guard is required, not optional: bmake -V .SYSPATH alone fully parses whatever makefile happens to be in the current directory before printing the value, so any directory (including $HOME) with a stray or broken makefile silently corrupts the captured value. No change needed to mk/*.mk's own .include statements: BMK_MKDIR already auto-computes correctly via ${.PARSEDIR} regardless of whether the including file was found via a local relative path or via MAKESYSPATH -- verified against a real out-of-tree project with zero local Bmake It files and no BMK_MKDIR override anywhere |
| dec | 0f87-6ab5-7f61-a52e | cxx-link-driver-selection-dec | recorded | Select the final link driver per module: ${CXX} when SRCS contains a C++ source, or LINK_CXX=yes is set explicitly; ${CC} otherwise |
| dec | 0f87-6ab5-8184-e593 | aggregation-failure-summary-and-exit-dec | recorded | Framework/workspace 'all' and 'test' keep running every module/framework after one failure (unchanged default), but end with a nonzero exit and a named-failure summary; FAIL_FAST=yes is unaffected |
| dec | 0f87-6ab5-83f1-fa08 | header-dependency-tracking-mechanism-dec | recorded | Add real per-source header dependency tracking to mk.prog.mk/mk.lib.mk: gcc/clang -MMD -MP -MF<obj>.d per compile, conditionally .include'd; MSVC stays an explicit, documented limitation for now |
| dec | 6f38-6a88-1836-bc1f | initial-domain-model | recorded | User directly specified the workspace/framework/module domain model and core build conventions |
| dec | 6f38-6a88-188b-2db4 | resource-file-definition | recorded | Resource file = non-source data a module ships, staged into build output alongside the binary |
| dec | 6f38-6a89-b763-9d8f | keep-simplified-prereq-and-header-model | recorded | Keep the simpler original model for prereq location and header visibility levels, diverging from real mkmk on these two points |
| dec | 6f38-6a89-b79b-673b | language-scope | recorded | Language scope: C, C++, and lex/yacc grammars for now; Java/Fortran/IDL/Express out of scope |
| dec | 6f38-6a89-b810-8ff2 | windows-via-cygwin-wsl | superseded | Windows is in scope only via Cygwin or WSL, not as a native MSVC/cl.exe host |
| dec | 6f38-6a89-ba51-6adc | layered-build-output-tree | recorded | Build outputs are organized as build/$OS_ARCH/{obj,bin,lib,...} at module, framework, AND workspace levels, not just module level |
| dec | 6f38-6a89-baec-6c41 | packaging-separate-target-native-formats | recorded | Packaging for distribution is a separate make target producing native OS install packages (msi, pkg, deb, rpm, ...), not run as part of the default build |
| dec | 6f38-6a89-baec-b7d2 | aggregation-by-copy | recorded | Framework/workspace-level build/$OS_ARCH/bin,lib is populated by copying module build outputs, as a distinct build step |
| dec | 6f38-6a89-baf8-79fb | variant-not-in-os-arch | recorded | Build variant (debug/release) is not encoded in $OS_ARCH; it is a separate concern |
| dec | 6f38-6a89-bb64-3c5c | toolchain-in-target-key | recorded | Toolchain (gcc vs llvm) gets its own slot in the build directory key, coexisting like separate OS/arch targets |
| dec | 6f38-6a89-bb64-d9ed | phase1-target-scope | recorded | Phase 1 implementation scope is two OS/arch pairs (macOS on Apple Silicon, FreeBSD on x86_64), each buildable with both gcc and llvm |
| dec | 6f38-6a89-bc24-835e | refined-resource-file-model | recorded | Resource files can live at both module and framework level, support per-target variants with fallback, and are staged through the same build/<KEY>/ tree as libraries |
| dec | 6f38-6a89-bccb-4fc4 | share-dir-naming-and-fallback | recorded | Resource source directory is named share/, with a three-level specificity fallback: share/common, share/<os>, share/<os>_<arch> |
| dec | 6f38-6a89-bcf2-135b | share-overlay-copy-semantics | recorded | share/ staging is a layered overlay copy (common, then os, then os_arch, each overwriting same-named files from the previous layer), not a pick-one-directory selection |
| dec | 6f38-6a89-bd4b-3e07 | prereq-access-dynamic-only | recorded | Prerequisite access is search-path-only across declared parent workspaces (dynamic); no physical-copy mode, no mkCopyPreq/mkGetPreq-equivalent commands |
| dec | 6f38-6a89-be28-3637 | target-key-string-format | recorded | Compound target key string format is <os>-<arch>-<toolchain>, e.g. macosx-arm64-llvm; arm64/amd64 architecture granularity confirmed sufficient for phase 1, no finer ISA sub-variant needed |
| dec | 6f38-6a89-be97-9afa | os-label-set | recorded | Canonical <os> labels for the target key: macos, win, freebsd, linux; openbsd/netbsd and per-Linux-distro handling deferred to later |
| dec | 6f38-6a89-bec3-708c | kernel-and-darwin-dev-deferred | recorded | Kernel-level development (kernel modules/drivers) and Darwin/XNU-specific development are explicitly deferred, not in scope for now |
| dec | 6f38-6a8a-04ce-ed4a | uniform-makefile-filename | recorded | Every level (workspace, framework, module) uses a file literally named 'makefile' (not makefile.mk), which .includes a role-specific bsd.*.mk-style helper file |
| dec | 6f38-6a8a-04e6-6d3c | three-level-header-visibility | recorded | Header visibility is three levels, not two: framework/include (public, to prereq-declaring frameworks), framework/local/include (to all of the framework's own modules), module.m/include (to that module's own sources only) |
| dec | 6f38-6a8a-0526-337a | module-build-macros | recorded | Module makefile uses PROG= (executable name, with mk.prog.mk), or LIB= plus optional LIB_SHARED=YES (library name/kind, with mk.lib.mk); LINK_WITH= lists libraries used for both header access and linking |
| dec | 6f38-6a8a-0550-3cf8 | framework-prereqs-macro | recorded | Framework makefile uses PREREQS= (space-delimited, ORDERED list of frameworks this one may link_with); public-only (no visibility levels); misordering causes a build error, cycle-detection behavior still to be confirmed |
| dec | 6f38-6a8a-0550-8cf7 | versioned-output-naming | recorded | Binary/library output naming must support embedding a version number in the filename, following standard *nix conventions (e.g. soname-style versioning) |
| dec | 6f38-6a8a-0550-c53e | workspace-parent-macro-absolute | recorded | Workspace makefile uses PARENT_WS= (ordered, space-delimited list of ABSOLUTE paths to parent workspaces); relative paths rejected as too messy |
| dec | 6f38-6a8a-0550-d2ae | packaging-per-format-targets | recorded | Packaging uses separate named make targets per format (make port, make pkg, etc.) rather than one generic target with a format flag |
| dec | 6f38-6a8a-0550-d971 | workspace-no-own-resources | recorded | A workspace does not carry its own share/ resources; only modules and frameworks do |
| dec | 6f38-6a8a-06bf-eece | build-output-mirrors-install-prefix | recorded | Build output under build/<KEY>/ is structured to mirror a real installable prefix layout (plain Unix /usr/local-style, MacPorts /opt/local-style, or a macOS .app bundle), not an arbitrary bin/lib/share convenience layout |
| dec | 6f38-6a8a-06e9-dce9 | app-bundle-deferred | recorded | macOS .app bundle output is deferred, not needed for phase 1; phase 1 macOS output is plain command-line tools/libraries in the standard bin/lib/share prefix layout |
| dec | 6f38-6a8a-07b3-0a61 | win-target-via-cross-toolchain | recorded | Windows (win) target output can be produced via a mingw-w64-style cross-compilation toolchain from any capable build host (not exclusively from within Cygwin); WSL is confirmed to fold into the linux build host, not a distinct win-producing host; exact mingw-vs-Cygwin toolchain distinction for win targets is deferred |
| dec | 6f38-6a8a-0c4d-abe7 | target-key-omit-defaults | recorded | Both the toolchain slot and a new ABI/libc slot are omitted from the target key when they hold the default value for that OS; toolchain default is llvm, ABI default is OS-specific (msvc for win) |
| dec | 6f38-6a8a-0d9b-7cca | linux-libc-default-glibc | recorded | glibc is the implicit default libc for Linux targets; linux-<arch> alone means glibc, musl is always shown explicitly |
| dec | 6f38-6a8a-9181-481e | lib-shared-defaults-yes | recorded | LIB_SHARED defaults to YES when unset -- a library module is a shared library by default, not a static archive |
| dec | 6f38-6a8a-9181-61d3 | prog-lib-default-to-module-name | recorded | PROG and LIB default to the module's directory name (minus .m suffix) when not explicitly set |
| dec | 6f38-6a8a-9181-ce2c | module-level-build-subdirs-confirmed | recorded | Module-level build/<KEY>/ subdirectories are confirmed as obj/ (intermediates) plus bin/ or lib/ (the module's own linked artifact) and share/ (staged module resources), matching framework/workspace level naming |
| dec | 6f38-6a8a-9181-d97c | versioned-naming-follows-bsd-lib-mk | recorded | Versioned shared-library naming follows real BSD make's bsd.lib.mk convention (SHLIB_MAJOR / SHLIB_MINOR-style macros producing libfoo.so.X.Y), rather than inventing a new scheme |
| dec | 6f38-6a8a-9181-e3a5 | module-name-case-sensitivity-follows-fs | recorded | Module name case-sensitivity follows the host filesystem's own case sensitivity (e.g. distinct on case-sensitive FS, potentially colliding on case-insensitive FS) |
| dec | 6f38-6a8a-943b-031c | lib-shared-no-is-static | recorded | LIB_SHARED=NO explicitly builds a static/archive library |
| dec | 6f38-6a8a-943b-a8e2 | libs-macro-link-only-headers-via-prereqs | recorded | LINK_WITH is renamed LIBS (dev + system libraries to link with); it is link-time ONLY. Header lookup no longer uses it at all -- headers are found solely via the framework's PREREQS |
| dec | 6f38-6a8a-943b-fa0a | prereqs-misorder-no-validation | recorded | No dedicated validation/detection is built for PREREQS misordering; an incorrectly ordered PREREQS list simply surfaces as a natural 'header not found' compile error, for now |
| dec | 6f38-6a8a-967a-a54e | headers-resolved-from-source-not-build | recorded | Header lookup for a PREREQS-declared framework reads directly from that frameworks own include/ (source), never from any build/<KEY>/ output; matches real mkmk, which never touches build output for headers |
| dec | 6f38-6a8a-9a77-b1a2 | generated-headers-in-build-tree | recorded | Generated headers (lex/yacc, other tooling) are a build-time artifact, not source: private ones live in module build/<KEY>/include/; modules can explicitly promote some to public, copied up to a framework-level build/<KEY>/include/ containing ONLY generated headers; workspace-level build/<KEY>/include/ is never used for compilation, only for packaging/install |
| dec | 6f38-6a8a-9b1e-0a47 | incl-macro-promotes-generated-headers | recorded | Module makefile uses INCL= to list which generated headers are promoted to public (copied up to framework build/<KEY>/include/) |
| dec | 6f38-6a8a-9be8-b758 | workspace-build-sequence | recorded | Consolidated end-to-end build sequence at workspace level: resolve PARENT_WS, resolve each frameworks PREREQS, build each module (compile to obj, link to module bin/lib) and copy up to framework bin/lib, then copy up to workspace bin/lib; resources follow the same module-to-framework-to-workspace cascade |
| dec | 6f38-6a8a-a40e-c2dd | framework-build-order-follows-prereqs-graph | recorded | A frameworks build order across the workspace is determined by the dependency graph implied by all frameworks PREREQS declarations: a framework is only built after every framework it lists in PREREQS has finished building |
| dec | 6f38-6a8a-d738-9661 | sibling-module-ordering-link-only | recorded | Sibling-module ordering within a framework applies ONLY to the link step of modules whose LIBS= names another sibling module; compilation of every module is order-independent, since headers never come from sibling modules |
| dec | 6f38-6a8a-d8b2-1579 | fused-build-per-module-full-dependency-order | recorded | Build strategy is fused compile+link per module/framework (bsd.*.mk-idiomatic), not global compile-all-then-link-all phases; a dependency (sibling module via LIBS, or any module/framework supplying a consumed promoted-generated header) must be FULLY built before its dependent starts, since generated+promoted headers create a real compile-time dependency, not just links link-time one |
| dec | 6f38-6a8a-d9a5-914d | shlib-major-minor-cross-platform-emission | recorded | SHLIB_MAJOR/SHLIB_MINOR is the single cross-platform source of truth for library versioning; mk.lib.mk emits the appropriate platform-native mechanism per target: ELF symlink chain (libfoo.so.MAJOR) on FreeBSD/Linux, dylib symlink chain plus linker -compatibility_version/-current_version flags on macOS |
| dec | 6f38-6a8a-dba2-7b93 | modules-and-frameworks-auto-discovered | recorded | Framework modules (*.m subdirectories) and workspace frameworks (any immediate subdirectory) are auto-discovered by scanning, not explicitly listed in the parent makefile -- consistent with src/ source auto-discovery |
| dec | 6f38-6a8a-f6ee-0a31 | prereqs-mandatory-even-empty | recorded | PREREQS= is now a MANDATORY variable in every framework makefile, even when the framework has no prerequisites (PREREQS= with nothing after it) |
| dec | 6f38-6a8a-f6ee-245c | framework-discovery-via-prereqs-in-makefile | recorded | A workspace subdirectory is recognized as a framework if it contains a makefile declaring PREREQS= (now mandatory); typical framework structure also includes module (*.m) subdirectories and/or a share/ directory, though the makefile+PREREQS check is the actual discovery signal |
| dec | 6f38-6a8b-655d-fd33 | target-selection-uses-bmake-native-vars | recorded | Target OS/arch selection on the make command line reuses real bsd make cross-build variable names, TARGET= and TARGET_ARCH=, rather than inventing new ones |
| dec | 6f38-6a8b-66e7-33d7 | toolchain-cli-variable | recorded | Toolchain selection on the make command line uses TOOLCHAIN= (e.g. make TOOLCHAIN=gcc); unspecified means the current default (llvm) |
| dec | 6f38-6a8b-67a0-4f62 | toolchain-triggers-dedicated-mk-file | recorded | TOOLCHAIN=<name> triggers .include of a dedicated toolchain mk file (e.g. mk.toolchain.gcc.mk / mk.toolchain.llvm.mk) that sets the underlying CC/CXX/LD/AR/AS choices, consistent with the projects existing role-specific .mk file pattern |
| dec | 6f38-6a8b-de94-5df7 | clean-target-scope | recorded | make clean removes only the current (or explicitly specified via TARGET=/TARGET_ARCH=/TOOLCHAIN=) targets build/<KEY>/ subtree; TARGET=all is a special value that wipes build/ output for every target at once |
| dec | 6f38-6a8b-df45-91dc | install-target-exists | recorded | make install exists as a distinct target, copying build/<KEY>/{bin,lib,share} to a real install destination (DESTDIR/PREFIX-style), separate from the make port/pkg/etc. packaging targets |
| dec | 6f38-6a8b-dfb1-18e7 | destdir-prefix-confirmed | recorded | Confirmed: make install uses bsd makes own native DESTDIR (staging root) and PREFIX (final install path under it) variables |
| dec | 6f38-6a8c-0d67-5fcc | install-requires-prebuilt-target | recorded | make install requires the current (or selected) target to have already been built; it does not trigger a build itself. Cross-install onto a different filesystem/mount is deferred for later |
| dec | 6f38-6a8c-195e-b3df | distrib-dir-for-packaging-staging | recorded | Packaging targets (make port, make pkg, etc.) stage into a distrib/<KEY> directory at workspace root, parallel to build/<KEY>, rather than an arbitrary temporary DESTDIR or working directly from build/<KEY> |
| dec | 6f38-6a8c-1a1d-da5d | packaging-invokes-install-into-distrib | recorded | Packaging targets populate distrib/KEY by internally invoking make install with DESTDIR set to that directory |
| dec | 6f38-6a8c-1ba0-c38a | port-target-is-source-recipe-not-binary-package | recorded | make port is fundamentally different from make pkg/deb/rpm: it generates a SOURCE-based build recipe (Makefile, distinfo, patches, pkg-plist) for the ports system to fetch/patch/build itself, not a staged binary package; the install-into-distrib mechanism (REQ-packaging-invokes-install-into-distrib-req) applies only to binary-package-style targets |
| dec | 6f38-6a8c-2025-9f02 | make-port-deferred | recorded | The make port mechanism (source-recipe generation) is explicitly deferred to a later phase, not designed now, joining the standing list of later items |
| dec | 6f38-6a8c-2159-2fc4 | prereqs-resolution-cached-like-depend | recorded | PARENT_WS/PREREQS resolution is cached as a generated .mk fragment, auto-.included by the relevant makefile, matching real bsd makes own .depend mechanism -- regenerated only when triggered/detected stale, not re-walked on every make invocation |
| dec | 6f38-6a8c-b76b-ec7f | mkdep-style-header-dependency-tracking | recorded | Standard bsd make mkdep-style per-source .depend header dependency tracking is in scope, distinct from the PARENT_WS/PREREQS resolution cache: touching a header only triggers recompilation of the .o files that actually (transitively) include it |
| dec | 6f38-6a8c-b939-8ea9 | debug-release-via-native-bsd-flags | recorded | Debug vs release is not a single MODE= switch; it is controlled via real bsd makes own existing flag variables (DEBUG_FLAGS for debug symbols, optimization-level flags for release), reused as-is rather than wrapped in a new abstraction |
| dec | 6f38-6a8c-b9f4-d59f | flags-reuse-bsd-native-vars | recorded | Per-module compile/link flags reuse bsd makes own native CFLAGS/CXXFLAGS/LDFLAGS/YFLAGS/LFLAGS variables directly (appended via += in the module makefile); no new LOCAL_*FLAGS-style wrapper macros are introduced |
| dec | 6f38-6a8c-bbfe-7516 | add-prereq-add-parent-convenience-targets | recorded | Convenience make targets add-prereq and add-parent append entries to PREREQS= and PARENT_WS= respectively, editing the makefile text directly, rather than requiring hand-editing |
| dec | 6f38-6a8c-beda-010d | tests-docs-versioning-deferred | recorded | Test infrastructure, documentation generation, and package versioning/metadata (name, version, maintainer for deb/rpm/pkg) are all explicitly deferred to a later work phase |
| dec | 6f38-6a8d-1de3-846e | copy-up-collision-diff-aware | recorded | Copy-up collisions are diff-aware: identical-content collisions are silent, differing-content collisions warn but still overwrite (build proceeds), EXCEPT the deliberate share/ specificity overlay (platform-specific legitimately overwriting generic) which is expected behavior and never warns |
| dec | 6f38-6a8e-7e12-7f94 | dep-order-generation-mechanism-prototyped | recorded | A working prototype confirms the dependency-ordered SUBDIR generation mechanism: query each module makefiles LIBS= in isolation via bmake -f <dir>/makefile -V LIBS, topologically sort with Kahn algorithm ignoring out-of-framework references, emit a generated SUBDIR= .mk fragment consumed by bsd.subdir.mk |
| dec | 6f38-6a8f-5180-601d | run-target | recorded | make run PROGRAM=<name> ARGS=<args> builds a correct runtime environment (dynamic library search path set appropriately per-OS: LD_LIBRARY_PATH on Linux/FreeBSD, DYLD_LIBRARY_PATH on macOS) and executes the named built program with the given arguments, mirroring real mkmk's mkrun command |
| dec | 6f38-6a8f-5180-7741 | help-target | recorded | make help lists every valid make target and every valid variable (with valid values) applicable at the current level (module/framework/workspace), self-documented by each role-specific mk.*.mk file |
| dec | 6f38-6a8f-519a-bee9 | clean-removes-all-derived-artifacts | recorded | make clean must remove EVERYTHING derived/generated, not just build/<KEY>/: also distrib/<KEY>/, per-module .depend files, the PARENT_WS/PREREQS resolution cache, and the dependency-order generation cache -- leaving only original source |
| dec | 6f38-6a8f-5206-44f4 | build-system-self-test-harness | recorded | A dedicated, script-driven (not make-driven) test harness validates the mk.*.mk implementation itself, distinct from end-user test infrastructure (which remains deferred): test cases are packaged as archives, expanded fresh into a temp directory per run, built with bmake, result recorded, temp directory deleted |
| dec | 6f38-6a91-03ec-7871 | link-time-not-yet-built-guard | recorded | Before invoking the linker for a module with LIBS= entries, an explicit check verifies each referenced library has actually been built; a missing dependency produces a clear, explicit 'not yet built/linked' diagnostic rather than relying on the linker's own generic missing-library error |
| dec | 6f38-6a91-03ec-8e6f | order-cache-auto-triggered | recorded | The default make (build) target automatically runs make order first if the cache is missing or stale, so a fresh checkout builds correctly with plain make; make order remains available as its own explicit target for inspection/debugging |
| dec | 6f38-6a91-03ec-ae47 | order-target-blank-run | recorded | make order (workspace level) is an explicit target that performs a blank run -- no compilation or linking, just discovery + PREREQS/LIBS graph resolution across the whole workspace -- writing the result to a cache that the build target then consumes, instead of resolving order inline on every build |
| impl | 0f87-6a98-5de3-5c78 | impl-cross-compile-check-scoped | asserted | mk.toolchain.llvm.mk: -V/clean guard around the sysroot-requirement check |
| impl | 0f87-6a98-5de3-61b8 | impl-cross-compile-triple-quirks | asserted | mk.toolchain.llvm.mk: freebsd/linux/macos cross-target branches |
| impl | 0f87-6a98-5de3-a99c | impl-cross-compile-netbsd | asserted | mk.toolchain.llvm.mk: netbsd cross-target branch |
| impl | 0f87-6a98-5de3-c340 | impl-cross-target-catch-all-error | asserted | mk.toolchain.llvm.mk: final .else .error on the cross-target chain |
| impl | 0f87-6a98-5e47-0c71 | impl-prereqs-parent-ws-fallback | asserted | PARENT_WS forwarding through the recursive build chain + PREREQS local-then-parent fallback search |
| impl | 0f87-6a98-5e9c-0a14 | impl-mod-order-no-hardcoded-sys-mk | asserted | mk.framework.mk: BMK_SYS_MK passed through only if the caller set it, no hardcoded MacPorts default |
| impl | 0f87-6a98-5f76-2a44 | impl-nested-invocation-reentrancy-guard | asserted | BMAKE resolved to an absolute path before nested invocation (gen-mod-order.sh, gen-fw-order.sh, run-tests.sh) + atomic temp-file-then-rename cache writes |
| impl | 0f87-6a98-5ff4-1b42 | impl-prog-lib-default-naming | asserted | mk.prog.mk/mk.lib.mk: PROG=/LIB= default to the module dirname with .m stripped, LIB= additionally strips a leading lib prefix |
| impl | 0f87-6a98-5ff4-5d24 | impl-flags-reuse-native-vars | asserted | mk.common.mk: CFLAGS/CXXFLAGS += DEBUG_FLAGS, the bsd-native-variable-reuse pattern the flags mechanism follows |
| impl | 0f87-6a98-76a7-f34f | impl-prereqs-mandatory | asserted | mk.framework.mk: .error if PREREQS is not defined at all, even empty |
| impl | 0f87-6a98-76c2-a96e | impl-libs-macro-link-only | asserted | mk.prog.mk/mk.lib.mk: LIBS= becomes -l<name> in LDFLAGS only, never touches CFLAGS/CXXFLAGS -I paths |
| impl | 0f87-6a98-76ed-e260 | impl-shlib-major-minor-emission | asserted | mk.lib.mk: platform-branched shared-lib naming -- libfoo.MAJOR.dylib + compatibility/current_version on macOS, libfoo.so.MAJOR + soname elsewhere |
| impl | 0f87-6a98-7718-886d | impl-incl-promotes-generated-headers | asserted | mk.lib.mk: _promote_incl copies each INCL= entry up to the framework's build/<KEY>/include/, unlisted generated headers stay module-private |
| impl | 0f87-6a98-772e-6156 | impl-linux-libc-default-glibc | asserted | mk.common.mk: target-key ABI suffix only appended when ABI != glibc on Linux, so glibc stays implicit/omitted |
| impl | 0f87-6a98-7755-574e | impl-install-requires-prebuilt | asserted | mk.workspace.mk: install: refuses (exit 1) if BUILD_ROOT doesn't already exist, no implicit build-first |
| impl | 0f87-6a98-77f0-ca62 | impl-lib-shared-new-macro | asserted | mk.lib.mk: LIB_SHARED=YES|NO, defaulting to YES, drives whether SHLIB_MAJOR/static-only path is taken |
| impl | 0f87-6a98-77f0-fa5a | impl-framework-discovery-signal | asserted | mk.workspace.mk: a workspace subdir is discovered as a framework by grepping its makefile for a ^PREREQS= line |
| impl | 0f87-6a98-7829-4019 | impl-help-target | asserted | mk.help.mk: make help prints available targets/macros per role |
| impl | 0f87-6a98-7829-d674 | impl-self-test-harness | asserted | tests/harness/run-tests.sh + pack-cases.sh + tests/cases/: the packaged, pure-shell test harness |
| impl | 0f87-6a98-7918-e195 | impl-add-prereq-target | asserted | mk.framework.mk: make add-prereq FW=<name> appends <name> to the current framework's PREREQS= line |
| impl | 0f87-6a98-7946-1d85 | impl-run-target | asserted | mk.common.mk: make run PROGRAM=<name> ARGS=<args> -- sets the platform dynamic-lib search path to include the workspace's build/<KEY>/lib/, execs build/<KEY>/bin/<PROGRAM>, no implicit build |
| impl | 0f87-6a98-8b7f-9215 | impl-link-time-not-yet-built-guard | asserted | mk.prog.mk / mk.lib.mk: before invoking the linker for a module with LIBS=, a shell pre-check searches each resolved lib-search directory for lib<name>.{a,so,dylib}; a missing one fails with an explicit 'prerequisite library X has not been built yet' error naming the searched dirs, instead of proceeding to an opaque linker error |
| impl | 0f87-6a98-8ee9-ae83 | impl-copy-up-diff-aware | asserted | scripts/diff-aware-copy.sh, invoked from mk.prog.mk/mk.lib.mk/mk.framework.mk's copy-up targets: recursively copies SRC into DEST, silently for byte-identical files, with a 'copy-up collision' warning (not a hard error) for files that differ; the share/ specificity overlay in mk.framework.mk's _aggregate step is intentionally left on plain cp -a, since it must never warn regardless of content difference |
| impl | 0f87-6a98-96de-1355 | impl-win-host-detection | asserted | mk.common.mk: Cygwin's uname -s (version-suffixed, e.g. CYGWIN_NT-10.0-19045) is glob-matched via :Mcygwin* and normalized to the project's own os label 'win', alongside the existing darwin/freebsd/linux exact-match branches |
| impl | 0f87-6a98-96de-1865 | impl-msvc-toolchain | asserted | mk.toolchain.msvc.mk + scripts/msvc-cc-wrapper.sh + scripts/msvc-lib-wrapper.sh: tier-1 native MSVC support -- CC/CXX/AR point at wrapper scripts that translate the gcc/clang-style flags mk.prog.mk/mk.lib.mk already emit (-c/-o/-I/-D/-L/-l/-shared/-Wl,--out-implib) into cl.exe/link.exe/lib.exe syntax, including Cygwin-POSIX-to-Windows path translation via cygpath -w; refuses to cross-compile (MSVC only targets Windows from Windows) and requires cl.exe/lib.exe already on PATH (Developer environment active), both checked with clear .error diagnostics scoped to skip -V queries and clean |
| impl | 0f87-6a98-96de-331e | impl-win-target-naming | asserted | mk.prog.mk/mk.lib.mk: toolchain-agnostic win-target output naming shared by all three tiers -- PROG gets a .exe suffix (even when explicitly given, since it's a hard platform requirement not a convention), shared libs become LIB.dll + LIB.lib import lib (via -Wl,--out-implib, translated per-toolchain), static libs become LIB.lib; the redundant symlink+bonus-static-archive steps that exist for Unix targets are skipped for win since the import lib IS the .lib a consumer links against, and duplicating it as a plain static archive would silently overwrite it; the link-time not-yet-built guard's existence check also recognizes .lib |
| impl | 0f87-6a98-96de-785b | impl-mingw-cross-toolchain | asserted | mk.toolchain.llvm.mk: tier-2 mingw-w64 cross-compilation branch (TARGET=win, --target=<arch>-w64-mingw32 --sysroot=BMK_MINGW_SYSROOT -fuse-ld=lld), added alongside the existing freebsd/linux/macos/netbsd cross branches, no Cygwin/Windows host required; the catch-all unsupported-cross-target .error message updated to list win as recognized |
| impl | 0f87-6a9a-b8d2-c192 | impl-rade-export-header | asserted | include/bmk_export.h + mk.lib.mk: a portable BMK_DLLEXPORT/BMK_DLLIMPORT macro pair (CAA/RADE ExportedBy-style), on every module's include path via mk.common.mk; mk.lib.mk auto-defines <LIB>_BUILDING (uppercased) only while compiling that module's own sources, so a library's own two-line header boilerplate (#if defined(<LIB>_BUILDING) ... BMK_DLLEXPORT ... #else ... BMK_DLLIMPORT) resolves correctly for both the module building the DLL and any consumer merely linking against it; no-op on non-Windows targets (GCC visibility attribute or nothing), so the same header works unchanged across every toolchain/platform. examples/myworkspace/Hello/include/greet.h updated to use it as the worked example |
| impl | 0f87-6a9a-d311-f245 | impl-vcvarsall-auto-discovery | asserted | mk.vcvarsall.mk (included from mk.toolchain.msvc.mk only when cl.exe isn't already on PATH): replicates vcvarsall.bat's environment discovery in pure bmake -- locates the VS install via vswhere.exe, reads the default MSVC toolset version from Microsoft.VCToolsVersion.default.txt, finds the highest-versioned installed Windows SDK, and constructs the PATH/INCLUDE/LIB triple itself; CC/CXX/AR in mk.toolchain.msvc.mk then prefix the wrapper scripts with 'env PATH=... INCLUDE=... LIB=...' so every invocation carries the right environment without needing a Developer Command Prompt or a manual vcvarsall.bat call first. A real bug found and fixed during verification: bmake's own exists()/file-read builtins need the Cygwin/POSIX form of a path (backslash Windows paths are taken as literal filename characters, not separators) |
| impl | 0f87-6aa9-025e-d5c5 |  | asserted | mk.docs.mk: per-framework Doxygen generation (Doxyfile+HTML+tag file), PREREQS-ordered TAGFILES chaining for cross-framework links, project-metadata (PROJECT_NAME/NUMBER/BRIEF/LOGO, HTML_FOOTER license notice) auto-detected from workspace-root VERSION/LICENSE/docs/logo.* |
| impl | 0f87-6aa9-0267-4adc |  | verified | mk.test.mk: TESTS_CXX=/TESTS_C=/TESTS_SH= declarations compiled/staged against atf-c++/atf-c, Kyuafile generation, kyua test + report-junit/report-html |
| impl | 0f87-6aa9-0267-bd7c |  | asserted | mk.workspace.mk docs target: iterates SUBDIR_FRAMEWORKS invoking each framework's own docs, then generates a workspace landing page with the same project-metadata treatment (name/version/logo/license) |
| impl | 0f87-6aa9-0275-667f |  | asserted | mk.local.mk + hooks in mk.workspace.mk/mk.framework.mk/mk.prog.mk/mk.lib.mk: per-level mk/ directory search (PARENT_WS outermost, then WS, FW, MOD), six conventional hook filenames (pre/local x unconditional/TOOLCHAIN/TARGET), pre included before role-file defaults, local included after, all matching levels cascaded (not override) |
| impl | 0f87-6aa9-3a6f-6f56 |  | asserted | mk.toolchain.llvm.mk/mk.toolchain.gcc.mk: _TOOL_PREFIXES checked before ambient $PATH (command -v as last resort only); mk.paths.darwin.mk reordered (MacPorts/Homebrew/Nix/pkgsrc before /usr/bin) and expanded beyond MacPorts+Homebrew to Nix and pkgsrc/Darwin |
| impl | 0f87-6aa9-448d-537c | impl-shared-lib-rpath | asserted | mk.lib.mk / mk.prog.mk / mk.test.mk: @rpath install names + -Wl,-rpath for every LIBS=/test-own-lib search directory (skipped on TARGET==win) |
| impl | 0f87-6aaa-4155-c8fd | impl-sanitize-support | asserted | mk.common.mk: SANITIZE= parses to -fsanitize=, mutual-exclusion fail-loud for address/thread/memory, MSVC restricted to address (translated by msvc-cc-wrapper.sh); composes with run/test via shared CFLAGS/CXXFLAGS/LDFLAGS |
| impl | 0f87-6aaa-6201-a430 | impl-test-workspace-aggregation | asserted | mk.framework.mk/mk.workspace.mk: test/test-all recurse into every module/framework (SANITIZE= forwarded); workspace test-all builds one dashboard (test-report/index.html) linking every module's HTML+JUnit report, existence-scanned after the recursive run, with a per-module PASS/FAIL derived from <failure>/<error> tags in its JUnit XML |
| impl | 0f87-6aaa-697c-4c30 | impl-test-single-selection | asserted | mk.test.mk: TEST=<name> filters to one declared test, erroring on no match (empty(VAR:M${TEST}) bmake idiom -- an earlier draft wrapped it in an extra ${...}, which silently always matched empty, always erroring; fixed and verified against both a real match and a real mismatch). mk.framework.mk/mk.workspace.mk: pre-filter which modules/frameworks get invoked via a -V query of each module's TESTS_CXX/TESTS_C/TESTS_SH before forwarding TEST=, so a non-matching module is silently skipped rather than erroring; FW=<name> narrows workspace-level recursion to one framework |
| impl | 0f87-6aaa-6a6e-00d0 | impl-build-workspace-aggregation | asserted | mk.framework.mk _build_modules / mk.workspace.mk _build_frameworks: each module/framework's build output tee'd to build.log unconditionally; a failure touches build.failed and continues to the next one unless FAIL_FAST=yes; mk.workspace.mk's all: target generates build-report/index.html from those markers when REPORT=yes |
| impl | 0f87-6aaa-72d2-8ffc | impl-run-id-history | asserted | mk.common.mk: RUN_ID defaults to date+PID, overridable, computed once and forwarded through recursion; mk.framework.mk/mk.workspace.mk write build.log under runs/${RUN_ID}/ with a runs/latest symlink; mk.test.mk writes test-results.xml/test-report-html under the same convention; workspace build-report/test-report dashboards live at <REPORT_DIR>/${RUN_ID}/ with their own latest symlink and scan only the current RUN_ID's artifacts |
| impl | 0f87-6aaa-d6a5-b4df | impl-install-env-script | asserted | scripts/install-env.sh: computes MAKESYSPATH via bmake -f /dev/null -V .SYSPATH (guarded against a stray/broken makefile in the invocation directory), appends an idempotent marker-delimited block to the invoking user's own shell rc file (zsh/bash/fish/POSIX sh detected via $SHELL, not guessed) by default, or writes /etc/profile.d/bmake-it.sh under --system (Linux only, needs root); --uninstall removes exactly what was added; explicit y/N confirmation (or -y) before touching any file |
| impl | 0f87-6ab5-7f76-0dcf | impl-cxx-link-driver-selection | verified | _CCLINK computation and its use in mk.prog.mk's/mk.lib.mk's final link recipes |
| impl | 0f87-6ab5-81a2-7bb0 | impl-aggregation-failure-exit-code | verified | _check_build_failures:/_check_test_failures: in mk.framework.mk and mk.workspace.mk, plus the new test.failed marker |
| impl | 0f87-6ab5-8442-b2dc | impl-header-dependency-tracking | verified | -MMD -MP -MF<obj>.d per compile plus conditional .d .include in mk.prog.mk/mk.lib.mk |
| nreq | 6f38-6a89-b763-b866 | no-four-level-header-visibility | proposed | The system shall not implement the four-level (Public/Protected/Private/Local) header visibility hierarchy |
| nreq | 6f38-6a89-b763-cfc9 | no-identitycard-special-file | proposed | The system shall not require a dedicated per-framework identity-card special file for declaring prereqs |
| nreq | 6f38-6a89-bd4b-c4ce | no-copy-preq-commands | proposed | The system shall not provide a physical-copy prerequisite mode or dedicated mkCopyPreq/mkGetPreq-equivalent commands |
| nreq | 6f38-6a8a-0550-58a7 | no-workspace-level-share-source | proposed | The system shall not provide a workspace-level share/ resource SOURCE directory |
| obs | 0f87-6a95-6a27-378f | nested-bmake-discovery-self-reentrancy-risk | recorded | Module discovery and dependency-order analysis mechanisms (gen-mod-order.sh, gen-fw-order.sh) shell out to nested bmake invocations with no reentrancy guard and no absolute-path resolution of the make binary, causing redundant re-discovery on every nested parse and a catastrophic self-recursion when the invoked name resolves back to a wrapper of itself |
| obs | 0f87-6a95-7899-5807 | toolchain-detection-hardcoded-path-silently-wrong-compiler | recorded | mk.toolchain.llvm.mk and mk.toolchain.gcc.mk detect the compiler via a single hardcoded absolute path (exists(/usr/bin/clang), exists(/usr/bin/gcc)) rather than a PATH search, so on any system where the compiler lives elsewhere (pkgsrc's /usr/pkg/bin, MacPorts' /opt/local/bin, Homebrew, etc.) TOOLCHAIN=llvm silently falls back to whatever plain cc happens to be, with no warning that the requested toolchain was not actually used |
| obs | 0f87-6a95-afab-2ced | mod-order-sysroot-hardcoded-macports-fallback | recorded | gen-mod-order's nested -V LIBS/LIB queries silently see no dependencies on any non-macOS host |
| obs | 0f87-6a97-0c17-17c1 | prereqs-parent-ws-fallback-unimplemented | recorded | PREREQS resolution never actually searched PARENT_WS, despite a confirmed REQ requiring it |
| obs | 0f87-6a97-47f7-9bf9 | alpine-musl-shared-lib-runtime-symbol-resolution-broken | recorded | Alpine/musl cannot resolve a custom shared library's exported symbol at runtime, even in a minimal, product-independent reproduction |
| obs | 0f87-6a97-bd07-f29e | qemu-tcg-x86-64-boot-stall-on-host | recorded | FreeBSD and NetBSD amd64 both stall deterministically at the identical boot-loader-to-kernel transition point under QEMU/TCG on this host |
| obs | 0f87-6a97-e99b-fc2c | macos-vm-connectivity-was-expect-case-bug | recorded | The macOS tart VM was never actually stuck -- an expect script bug (case-sensitive 'password:' match vs the real 'Password:' prompt) made every provisioning attempt hang indefinitely, looking exactly like an unreachable/hung VM |
| obs | 0f87-6a98-2997-bfb8 | netbsd-vm-flaky-ssh-was-filesystem-corruption | recorded | The NetBSD arm64 VM's flaky/inconsistent SSH behavior all session was real filesystem corruption from repeated unclean QEMU kills, not a networking or auth quirk |
| obs | 0f87-6a98-5d9c-ef85 | cross-target-silent-native-fallback-risk | recorded | An unrecognized cross-compile TARGET compiled with zero cross flags and no warning, silently producing a host-native binary while claiming to target something else |
| obs | 0f87-6a98-5db8-74f2 | cross-compile-sysroot-check-fired-on-non-build-invocations | recorded | The cross-compile sysroot requirement check fired even for a bare -V query or a clean, neither of which ever invokes a compiler |
| obs | 0f87-6a98-7bc3-e76b | run-target-workspace-root-only-limitation | recorded | make run only worked when invoked from the workspace root's own makefile; user pointed out that run is conceptually workspace-level but should be usable from any directory inside the workspace tree |
| obs | 0f87-6a98-92d2-c40d | windows-toolchain-scope-corrected | recorded | User corrected the Windows toolchain scope: native MSVC (cl.exe/link.exe) run from a Cygwin-hosted bmake shell is now explicitly in scope as a first-class (tier-1) option, alongside Cygwin-native gcc/llvm (also tier-1); mingw-w64 cross-compilation moves from 'the only path' to tier-2 (in scope, lower priority) |
| obs | 0f87-6a98-a928-82da | cygwin-runtime-has-no-arm64-port | recorded | Cygwin's own FAQ confirms cygwin1.dll has never been ported to any ARM ISA (32- or 64-bit) -- only x86/x86_64 builds exist; on ARM64 Windows the x86_64 build runs under Windows' built-in x64 emulation (Prism), not natively |
| obs | 0f87-6a9a-af21-456a | win-arm64-vm-construction-not-automatable | recorded | Unlike the NetBSD/FreeBSD/Linux VMs (fully unattended via autounattend/cloud-init-style provisioning, no human at the console), this session's Windows 11 arm64 VM required substantial manual/interactive intervention that has no scripted equivalent yet: an OOBE network-requirement bypass via Shift+F10 command prompt (autounattend.xml's oobeSystem pass never actually applied, cause not fully diagnosed), GUI-driven Device Manager driver installation for NetKVM (no scriptable offline injection path found from macOS -- DISM is Windows-native), a keyboard-layout mismatch that required swapping to a physical PC keyboard mid-session, and a failed attempt at boot-critical virtio-blk driver registration that triggered Windows' automatic repair. The test harness's goal of automated, no-external-intervention VM construction is NOT currently met for the win/Cygwin/MSVC tier -- reproducing this VM today requires a human at the console, not just a script |
| obs | 0f87-6a9a-b50f-e8d7 | msvc-dll-needs-explicit-exports | recorded | Same-turn testing of the DLL (shared-lib, LIB_SHARED=YES default) path with TOOLCHAIN=msvc found a genuine gap distinct from any wrapper bug: cl.exe/link.exe compiled greet.c and linked greet.dll successfully (confirmed via file(1) and the .dll existing on disk), but produced NO import library (greet.lib) despite /IMPLIB: being correctly passed -- because greet.c, written in a portable/Unix-idiom style with no __declspec(dllexport) markup, exports zero symbols under MSVC's model. Unlike gcc/clang(-mingw), which export every global symbol from a DLL by default (matching Unix .so/.dylib behavior, confirmed working via the Cygwin-gcc test the same session), MSVC/link.exe require EXPLICIT dllexport annotation (or a .def file, or /EXPORT: on the link line) for any symbol to appear in the DLL's import library. rade-bmake's current shared-library convention (plain portable C, no annotations, matching the gcc/clang-mingw default-export model) does not work out of the box for TOOLCHAIN=msvc's shared-lib case; the static-lib case (LIB_SHARED=NO) is unaffected since static archives have no export/visibility concept and was confirmed fully working |
| obs | 0f87-6a9a-c666-229d | rade-is-dassault-trademark | recorded | RADE (Rapid Application Development Environment) is a Dassault Systemes-associated term from their CAA/CATIA ecosystem; using it as this project's own name/prefix (the working title 'rade-bmake', the RADE_ variable prefix, rade_export.h) risked a real naming/trademark conflict rather than legitimate attribution |
| obs | 0f87-6aa7-b958-558a | vm-image-caching-layering-unaddressed | recorded | harness-native-builds-also-in-vms-req's existing scope says nothing about image/package caching, offline construction, or layered image structure (OS/software/data) across host+target combinations |
| obs | 0f87-6aa7-d87b-29e6 | harness-scaffolding-predates-frozen-matrix | recorded | Existing tests/harness/ scaffolding (platforms.mk, Dockerfiles, VM image URLs) predates harness-target-matrix-reconciled-req's frozen versions and version-freshness-caching-policy-req's pinning discipline -- not drift, since no version was formally specified before this session's freeze |
| obs | 0f87-6aa7-dd89-04d6 | external-tool-generation-config-topic | recorded | Open topic, not yet scoped: a general mechanism for external code-generation tools (like yacc/lex, but generalized beyond them) and how their configuration gets declared per-module/framework |
| obs | 0f87-6aa7-dd92-ede9 | per-workspace-local-mk-override-topic | recorded | Open topic, not yet scoped: per-workspace local mk configuration override -- a specific directory (e.g. a local mk/) that, if present in a workspace, is taken into account during the build |
| obs | 0f87-6aa9-0adc-f17c | collection-level-architecture-topic | recorded | Open topic, not yet scoped: a fourth architectural level above workspace (Collection -> Workspace(=Application) -> Framework -> Module), functioning like a ports collection (FreeBSD Ports / NetBSD pkgsrc) -- holds multiple independent applications, each its own workspace, auto-discovered the same way a workspace discovers frameworks (dirs whose makefile declares PARENT_WS=) |
| obs | 0f87-6aa9-39b8-d2f0 | macos-toolchain-detection-not-deterministic | recorded | macOS toolchain detection is non-deterministic across invocation environments: command -v clang (ambient shell /Users/sylvain/miniconda3/bin:/Users/sylvain/miniconda3/condabin:/opt/local/bin:/opt/local/sbin:/usr/local/bin:/System/Cryptexes/App/usr/bin:/usr/bin:/bin:/usr/sbin:/sbin:/var/run/com.apple.security.cryptexd/codex.system/bootstrap/usr/local/bin:/var/run/com.apple.security.cryptexd/codex.system/bootstrap/usr/bin:/var/run/com.apple.security.cryptexd/codex.system/bootstrap/usr/appleinternal/bin:/pkg/env/global/bin:/Library/Apple/usr/bin:/Library/TeX/texbin:/Library/Frameworks/Mono.framework/Versions/Current/Commands:/Users/sylvain/.lmstudio/bin:/Users/sylvain/Library/pnpm:/Users/sylvain/Library/Application Support/Claude/local-agent-mode-sessions/skills-plugin/540095af-33d3-4b54-9071-8a4d7327ae1c/ae9804db-c018-421b-b62c-0d0869405214/bin) is tried before _TOOL_PREFIXES, so which compiler wins depends entirely on the caller's PATH ordering, not on Bmake It's own logic -- and the _TOOL_PREFIXES fallback list itself puts /usr/bin (Apple's Xcode toolchain) before /opt/local/bin (MacPorts), contradicting CON-cross-platform-targets's own 'macOS (MacPorts toolchain)' constraint. Every other per-OS paths file correctly puts that OS's base system first (the right default there); macOS is the one platform where the base-system compiler is NOT the intended default, making it the sole exception the shared PATH-first algorithm gets wrong |
| obs | 0f87-6aa9-4262-9beb | macports-complete-container-stack | recorded | MacPorts alone supplies a complete container/VM harness stack (docker CLI + colima daemon + lima + tart) -- Homebrew is not required |
| obs | 0f87-6aa9-447f-0d48 | shared-lib-not-runtime-loadable-obs | recorded | Shared libraries built by mk.lib.mk were not runtime-loadable by anything outside their own build directory -- bare install_name (macOS) with no consumer-side rpath anywhere |
| obs | 0f87-6aa9-4495-2e81 | atf-cxx-libcxx-autoptr-gap-obs | recorded | MacPorts atf-c++ 0.21 headers use std::auto_ptr, which libc++ removes under C++17 mode (clang's default since ~16); libstdc++/gcc keeps it deprecated-but-present at the same standard level -- a real, host-specific toolchain-packaging gap, not a Bmake It bug |
| obs | 0f87-6aaa-93e5-324a | variant-optional-builds-topic | recorded | Open topic, not yet scoped: support for build variants (beyond the already-settled debug/release-no-separate-directory case) and optional builds -- a module/framework whose inclusion depends on a toggled feature or a probed dependency's presence -- plus a discovery mechanism for which variants/optional pieces are actually available on a given host |
| obs | 0f87-6ab5-7f50-7430 | cxx-modules-link-with-cc-bug | recorded | mk.prog.mk and mk.lib.mk always link with ${CC}, never ${CXX} -- a C++ program or shared library fails to link |
| obs | 0f87-6ab5-8175-e29c | aggregation-exit-code-masked-bug | recorded | Framework/workspace 'all' and 'test' always exit 0 even when a module/framework genuinely failed, with FAIL_FAST=no (the default) |
| obs | 0f87-6ab5-83dc-9fe1 | header-dependency-tracking-not-wired-for-real | recorded | mk.prog.mk/mk.lib.mk never track per-source header dependencies at all -- changing a header does not trigger a rebuild, and the model's own 'confirmed' REQ claiming otherwise doesn't apply to this project's architecture |
| obs | 0f87-6ab5-83fe-dbd1 | msvc-header-dependency-tracking-deferred | recorded | MSVC header dependency tracking (/showIncludes) is explicitly deferred -- msvc-cc-wrapper.sh does not synthesize a .d-equivalent file, so a header change under TOOLCHAIN=msvc does not trigger a rebuild |
| obs | 6f38-6a88-1836-f6c4 | rade-inspiration | recorded | Goal is to mimic Dassault Systemes RADE CLI tool and project structure |
| obs | 6f38-6a88-1965-0d15 | mkmk-java-and-windows-specifics | recorded | Real mkmk also supports Java (with client/server/common module segregation) and Windows-specific mechanisms (DLL export/import macro pattern, COM type-library import); these appear out of scope given the project's C/C++ and macOS/Linux/BSD-only stated scope |
| obs | 6f38-6a88-1965-243b | mkmk-command-phases-and-cli | recorded | Real mkmk runs four phases by default (framework identity-card processing, public-header-list generation, module build-file check/update, the build itself) and exposes CLI options controlling target scope, parallelism, and build variants |
| obs | 6f38-6a88-1965-448a | mkmk-runtime-view-and-resources | recorded | Real mkmk separates a build-time file tree from a run-time file tree; non-source runtime data (icons, message files, etc.) lives in per-framework, optionally per-OS-specific directories and is copied or symlinked into a consolidated run-time view by a dedicated command |
| obs | 6f38-6a88-1965-500b | mkmk-module-buildfile-macros | recorded | Real mkmk's per-module build file uses named macros: object type, program/library name, link libraries, included sub-modules, per-language extra flags, per-language optimization, and a build-disable switch |
| obs | 6f38-6a88-1965-b0e1 | mkmk-interface-visibility-levels | recorded | Real mkmk headers have four visibility levels (framework-level Public/Protected/Private, module-level Local), each in its own directory, with build-time checks on cross-level and cross-framework includes |
| obs | 6f38-6a88-1965-e898 | mkmk-workspace-concatenation | recorded | mkmk supports static (copy) and dynamic (search-path) access to prerequisite frameworks across a list of workspaces |
| obs | 6f38-6a88-1965-e9db | mkmk-identitycard-mechanism | recorded | In real mkmk, a framework's prerequisite list lives in a dedicated per-framework special file (an identity-card file), separate from any module build file |
| obs | 6f38-6a8a-085d-2587 | cygwin-native-vs-mingw-cross-are-separate-packages | recorded | Cygwin-native gcc and a mingw-w64 cross-compiler are separate packages/toolchains; which one produces a cygwin1.dll dependency is determined by the target triple, not by the shell/OS hosting the compiler process |
| obs | 6f38-6a8a-0a04-99a8 | win-target-has-three-abi-flavors | recorded | win targets have three distinct possible ABI/runtime flavors (MinGW, Cygwin, MSVC), unlike every other OS in the model which has one canonical ABI -- the win target key may eventually need a 4th slot, not just os-arch-toolchain |
| obs | 6f38-6a8a-0a38-cffe | linux-libc-choice-parallels-win-abi | recorded | Linux targets also have a libc choice (glibc vs musl, primarily), parallel to the win ABI question -- another potential axis beyond os-arch-toolchain, deferred along with the rest of Linux-specific scope |
| obs | 6f38-6a8c-30a5-87fe | cache-mechanism-needs-empirical-check | recorded | Exact staleness-detection/regeneration-trigger mechanism for the PARENT_WS/PREREQS cache is deferred until real bsd.*.mk and FreeBSDs build.*.mk infrastructure can be checked hands-on for what they actually provide |
| obs | 6f38-6a8d-7558-017a | validation-yacc-lex-suffix-rules-confirmed | recorded | Empirically confirmed: real bsd.prog.mk .y/.l suffix rules work automatically with zero explicit rule declarations, producing per-file-named output (grammar.c/grammar.h, not y.tab.c/y.tab.h), avoiding multi-grammar collisions |
| obs | 6f38-6a8d-7578-ecbc | validation-lib-shared-does-not-exist-natively | recorded | Empirically confirmed: real bsd.lib.mk has no LIB_SHARED=YES|NO toggle at all. LIB alone always triggers a static .a; SHLIB_MAJOR presence ADDITIONALLY triggers a shared .so -- both build simultaneously, not either/or |
| obs | 6f38-6a8d-75c1-95f6 | validation-target-arch-inert-without-basesystem | recorded | Empirically confirmed: TARGET=/TARGET_ARCH= have ZERO effect on the actual compiler invoked for a standalone bsd.prog.mk-based project (outside FreeBSD base-system buildworld/Makefile.inc1 infrastructure). Setting TARGET_ARCH=arm64 on an amd64 host still produced a plain x86-64 binary |
| obs | 6f38-6a8e-048e-e43d | validation-destdir-native-prefix-is-not | recorded | Empirically confirmed: DESTDIR composes correctly and is genuinely bsd-make-native (staging root prepended to BINDIR etc.). But PREFIX is NOT a native bsd.prog.mk/bsd.lib.mk concept at all -- neither file references it; real FreeBSD base-system install uses hardcoded individual *DIR variables (BINDIR, LIBDIR, etc.) directly, with no unifying PREFIX that derives them |
| obs | 6f38-6a8e-04b1-edf4 | validation-dynamic-subdir-confirmed-working | recorded | Empirically confirmed: SUBDIR!= (shell command-substitution assignment) correctly populates SUBDIR from a live directory scan, and bsd.subdir.mk correctly recurses into each discovered directory. The entire auto-discovery design (module/framework scanning) works via real bsd make mechanics with no fallback needed |
| obs | 6f38-6a8e-04db-4b40 | validation-debug-flags-confirmed-native | recorded | Empirically confirmed: DEBUG_FLAGS is genuinely bsd-make-native -- bsd.debug.mk appends it to CFLAGS/CXXFLAGS, and its presence/absence also toggles default binary stripping (STRIP defaults to -s, stripping, only when DEBUG_FLAGS is unset). COPTFLAGS is NOT the optimization-level variable in real FreeBSD share/mk -- that specific naming guess was wrong; the actual default -O2 traces elsewhere (not fully identified in this pass) |
| obs | 6f38-6a8e-71e6-ce0a | validation-cross-compile-is-external-orchestration-everywhere | recorded | Investigated portable bmakes own mk-bmake (generic pmake) and mk-netbsd (NetBSD) file sets in addition to FreeBSD share/mk: confirmed cross-compilation is an EXTERNAL orchestration pattern across the entire bsd-make family, not something any variant of bsd.prog.mk/bsd.lib.mk implements internally. Also confirmed no variant auto-creates install destination directories |
| obs | 6f38-6a91-03a8-2bc2 | validation-bsd-dep-mk-automatic-wiring-broken | recorded | Empirically confirmed: bsd.dep.mk's automatic depend: wiring does not do real work in either readily-available lineage -- modern FreeBSD requires meta-mode/filemon or DIRDEPS_BUILD infrastructure not present standalone; NetBSD's bundled version is a literal no-op (rm -f .depend; touch .depend). The underlying mkdep TOOL itself, called directly, produces correct transitive header dependencies |
| obs | 6f38-6a91-4c89-0397 | validation-compiler-native-md-dependency-tracking-confirmed | recorded | Corrects OBS-validation-bsd-dep-mk-automatic-wiring-broken, which tested the wrong mechanism. Real bsd.sys.mk/bsd.dep.mk wire the COMPILER's own -MD flag to generate a per-object .depend.<obj> file automatically on every ordinary compile -- no separate make depend step, no mkdep tool invocation needed at all. Confirmed fully working end to end: touching a shared header correctly triggered recompilation of exactly the right .o files |
| obs | 6f38-6a91-a2cf-a42d | validation-md-wiring-is-freebsd-specific-not-family-wide | recorded | The compiler-native -MD dependency-tracking wiring confirmed working is specific to FreeBSD's own actual bsd.sys.mk -- NOT a general BSD-make-family property. Cross-checked against NetBSD's own bundled mk-netbsd set: zero -MD flags, zero per-object dependency files, and touching a header correctly produced NO rebuild at all. Whether MacPorts' own bsdmake port (a phase-1 target host) includes this wiring is unknown and untestable in this sandbox |
| req | 0f87-6a94-678e-e640 | module-prog-or-lib-macro-req | confirmed | A module's makefile declares either PROG=<name> (executable, via .include <mk.prog.mk>) or LIB=<name> (library, via .include <mk.lib.mk>) |
| req | 0f87-6a94-696d-1192 | prog-lib-default-to-module-name-req-v2 | superseded | When PROG= or LIB= is not set in a module's makefile, it defaults to the module's directory name with the .m suffix stripped |
| req | 0f87-6a94-696d-71fc | install-requires-prebuilt-target-req-v2 | confirmed | make install fails/refuses if the current (or selected) targets build/<KEY>/ output does not already exist -- it does not implicitly build first; cross-filesystem install is deferred, not designed |
| req | 0f87-6a94-696d-9094 | toolchain-cli-variable-req-v2 | superseded | Toolchain selection on the make command line uses TOOLCHAIN=<gcc|llvm>; unspecified defaults to the current default toolchain, llvm |
| req | 0f87-6a94-696d-920d | os-label-set-req-v2 | confirmed | The target key <os> component uses one of: macos, win, freebsd, linux; openbsd, netbsd, and Linux-distro-specific labels are deferred, not decided for phase 1 |
| req | 0f87-6a94-696d-a08d | distrib-dir-for-packaging-staging-req-v2 | confirmed | A workspace-level distrib/<KEY>/ directory, parallel to build/<KEY>/ and keyed the same way, is where packaging targets (make port, make pkg, ...) stage an install-prefix-shaped tree before building the actual per-format package artifact from it |
| req | 0f87-6a94-6a03-808d | win-target-via-cross-toolchain-req-v2 | superseded | Producing win-target output does not require running the build from within Cygwin; it can be cross-compiled from any capable host via a mingw-w64-class toolchain, same as any other cross-compile target; the exact mingw-vs-Cygwin toolchain distinction is deferred, not in scope for phase 1 |
| req | 0f87-6a94-6a03-a659 | toolchain-triggers-dedicated-mk-file-req-v2 | superseded | TOOLCHAIN=<gcc|llvm> selects and .includes a dedicated toolchain mk file (mk.toolchain.<name>.mk) that sets CC/CXX/LD/AR/AS and related tool choices for that toolchain; these remain individually overridable via normal make variable precedence |
| req | 0f87-6a94-6a03-da95 | packaging-invokes-install-into-distrib-req-v2 | confirmed | Packaging targets stage distrib/KEY by invoking install with DESTDIR=distrib/KEY, reusing the install/DESTDIR/PREFIX mechanism rather than a separate copy step |
| req | 0f87-6a94-6a22-7d41 | port-target-is-source-recipe-not-binary-package-req-v2 | confirmed | make port produces a source-based build recipe (Makefile/distinfo/patches/pkg-plist) for the ports system to build from source, NOT a staged binary package; REQ-packaging-invokes-install-into-distrib-req applies only to binary-package-style targets (pkg, deb, rpm, msi) |
| req | 0f87-6a94-6a22-b625 | toolchain-mk-must-set-explicit-cc-paths-req-v2 | superseded | mk.toolchain.<name>.mk must explicitly set CC/CXX/LD/AR/AS to concrete cross-compiler paths/binaries based on TARGET/TARGET_ARCH/TOOLCHAIN, following the same external-orchestration pattern every bsd-make lineage (FreeBSD Makefile.inc1, NetBSD build.sh) uses -- TARGET/TARGET_ARCH cannot be passed through to bsd.prog.mk/bsd.lib.mk expecting them to select a compiler themselves |
| req | 0f87-6a94-6a22-d2c7 | target-arch-toolchain-selection-is-custom-req-v2 | superseded | TARGET=/TARGET_ARCH= variable NAMES are still reused (for familiarity with bsd make convention), but the actual cross-compiler SELECTION logic must be implemented by this project itself (in mk.toolchain.<name>.mk or a new mk.target.mk), not inherited from bsd.prog.mk/bsd.lib.mk, which do nothing with these variables outside FreeBSDs base-system build infrastructure |
| req | 0f87-6a94-6a49-82b0 | toolchain-cli-variable-req-v3 | superseded | Toolchain selection on the make command line uses TOOLCHAIN=<gcc|llvm>; unspecified defaults to the current default toolchain, llvm |
| req | 0f87-6a94-6a49-e347 | make-port-deferred-req-v2 | confirmed | make port generation mechanism is deferred to a later work phase; only its category (source-recipe, not binary package) is settled for now |
| req | 0f87-6a94-6a57-ca0d | toolchain-triggers-dedicated-mk-file-req-v3 | superseded | TOOLCHAIN=<gcc|llvm> selects and .includes a dedicated toolchain mk file (mk.toolchain.<name>.mk) that sets CC/CXX/LD/AR/AS and related tool choices for that toolchain; these remain individually overridable via normal make variable precedence |
| req | 0f87-6a94-6a73-6506 | toolchain-mk-must-set-explicit-cc-paths-req-v3 | superseded | mk.toolchain.<name>.mk must explicitly set CC/CXX/LD/AR/AS to concrete cross-compiler paths/binaries based on TARGET/TARGET_ARCH/TOOLCHAIN, following the same external-orchestration pattern every bsd-make lineage (FreeBSD Makefile.inc1, NetBSD build.sh) uses -- TARGET/TARGET_ARCH cannot be passed through to bsd.prog.mk/bsd.lib.mk expecting them to select a compiler themselves |
| req | 0f87-6a94-6a73-e75c | target-arch-toolchain-selection-is-custom-req-v3 | superseded | TARGET=/TARGET_ARCH= variable NAMES are still reused (for familiarity with bsd make convention), but the actual cross-compiler SELECTION logic must be implemented by this project itself (in mk.toolchain.<name>.mk or a new mk.target.mk), not inherited from bsd.prog.mk/bsd.lib.mk, which do nothing with these variables outside FreeBSDs base-system build infrastructure |
| req | 0f87-6a95-270b-85c4 | flags-reuse-bsd-native-vars-req-v2 | confirmed | A module supplies additional compile or link options by appending directly to bsd makes own native CFLAGS/CXXFLAGS/LDFLAGS/YFLAGS/LFLAGS variables in its makefile (e.g. CFLAGS+=-DFOO); no project-specific LOCAL_*FLAGS wrapper macros exist |
| req | 0f87-6a95-2add-8126 | target-arch-toolchain-selection-is-custom-req-v4 | confirmed | TARGET=/TARGET_ARCH= variable NAMES are still reused (for familiarity with bsd make convention), but the actual cross-compiler SELECTION logic must be implemented by this project itself (in mk.toolchain.<name>.mk or a new mk.target.mk), not inherited from bsd.prog.mk/bsd.lib.mk, which do nothing with these variables outside FreeBSDs base-system build infrastructure |
| req | 0f87-6a95-2afd-4e9b | toolchain-cli-variable-req-v4 | confirmed | Toolchain selection on the make command line uses TOOLCHAIN=<gcc|llvm>; unspecified defaults to the current default toolchain, llvm |
| req | 0f87-6a95-2b36-05b2 | toolchain-mk-must-set-explicit-cc-paths-req-v4 | superseded | mk.toolchain.<name>.mk must explicitly set CC/CXX/LD/AR/AS to concrete cross-compiler paths/binaries based on TARGET/TARGET_ARCH/TOOLCHAIN, following the same external-orchestration pattern every bsd-make lineage (FreeBSD Makefile.inc1, NetBSD build.sh) uses -- TARGET/TARGET_ARCH cannot be passed through to bsd.prog.mk/bsd.lib.mk expecting them to select a compiler themselves |
| req | 0f87-6a95-2b36-c1c5 | toolchain-triggers-dedicated-mk-file-req-v4 | confirmed | TOOLCHAIN=<gcc|llvm> selects and .includes a dedicated toolchain mk file (mk.toolchain.<name>.mk) that sets CC/CXX/LD/AR/AS and related tool choices for that toolchain; these remain individually overridable via normal make variable precedence |
| req | 0f87-6a95-509a-8559 | prog-lib-default-to-module-name-req-v3 | confirmed | When PROG= or LIB= is not set in a module's makefile, it defaults to the module's directory name with the .m suffix stripped; for LIB= specifically, a leading 'lib' prefix on that name is also stripped (e.g. libgreet.m defaults to LIB=greet, not LIB=libgreet) |
| req | 0f87-6a95-6a53-9e2e | nested-invocation-reentrancy-guard-req | confirmed | Any build-system mechanism that shells out to a nested make invocation to perform discovery or dependency-order analysis (module discovery, framework discovery, LIBS=/PREREQS= topological ordering) must guard against self-reentrant and concurrent re-invocation: the invoked make binary must be resolved to an absolute path, never a bare name looked up via PATH, and any cache file the mechanism writes must not be unconditionally deleted-and-regenerated in a way that races a concurrent or nested invocation of the same discovery for the same directory |
| req | 0f87-6a95-78e1-3ab3 | toolchain-mk-must-set-explicit-cc-paths-req-v5 | confirmed | mk.toolchain.<name>.mk must explicitly set CC/CXX/LD/AR/AS to concrete cross-compiler paths/binaries based on TARGET/TARGET_ARCH/TOOLCHAIN, following the same external-orchestration pattern every bsd-make lineage (FreeBSD Makefile.inc1, NetBSD build.sh) uses; locating each toolchain's compiler must search PATH (or a defined list of install prefixes), never rely on a single hardcoded absolute path, and must fail loudly rather than silently substituting a different compiler when the requested toolchain cannot be found |
| req | 0f87-6a95-b3d4-d67d | cross-compile-per-target-triple-quirks-req | confirmed | Each cross target's clang triple/sysroot layout must match that platform's real distro/vendor conventions, not a generic guess |
| req | 0f87-6a95-c754-c645 | cross-compile-same-os-cross-arch-req | confirmed | Cross-compilation also works within the same OS across architectures (e.g. Linux/arm64 host targeting Linux/amd64), not just across OS boundaries |
| req | 0f87-6a97-d20a-cb7c | cross-compile-netbsd-target-req | confirmed | Cross-compilation to NetBSD is supported as a fourth cross target, alongside freebsd/linux/macos |
| req | 0f87-6a98-5da8-9fc2 | cross-target-selection-must-fail-loudly-req | confirmed | Selecting an unrecognized cross-compile TARGET must fail loudly (a named error), never silently compile natively while claiming that target |
| req | 0f87-6a98-5dca-b115 | cross-compile-check-scoped-to-actual-builds-req | confirmed | The cross-compile sysroot requirement only blocks invocations that can reach a compile step -- not a -V query or a clean |
| req | 0f87-6a98-7bd0-ed4a | run-target-any-workspace-dir-req | confirmed | make run PROGRAM=<name> [ARGS=<args>] must work when invoked from bmake in any directory within the workspace tree (the workspace root, a framework directory, or a module directory), not only the workspace root itself, resolving PROGRAM against the enclosing workspace's aggregated build/<KEY>/bin and lib |
| req | 0f87-6a98-92e4-4528 | win-target-tiered-toolchain-req | confirmed | Windows target support is available via three toolchain paths -- tier 1: native MSVC (cl.exe/link.exe) run from a Cygwin-hosted bmake shell, command-line only, matching every other platform's workflow; tier 1: Cygwin-native gcc/llvm run the same way; tier 2: mingw-w64 cross-compilation from any capable host, no Cygwin required. All three produce genuine win-target output |
| req | 0f87-6a9a-b51c-b267 | msvc-shared-lib-export-req | confirmed | For TOOLCHAIN=msvc specifically, a module's shared-library build (LIB_SHARED=YES, the default) must produce a usable import library (.lib) even when the source uses no __declspec(dllexport) annotations -- matching the zero-annotation-needed convention that already works for every other toolchain/target in this project. Exact mechanism (auto-generate a .def exporting every global symbol via dumpbin/nm-equivalent, require annotations project-wide with a portable macro, or something else) is an open follow-up, not yet decided |
| req | 0f87-6a9a-d35b-a3ed | msvc-no-manual-vcvarsall-req | confirmed | TOOLCHAIN=msvc must work from a plain Cygwin shell with no prior manual setup -- no Developer Command Prompt, no manually-run vcvarsall.bat -- discovering the installed MSVC toolset and Windows SDK itself |
| req | 0f87-6aa0-3bf1-5dfa | harness-comprehensive-feature-coverage-req | confirmed | Test harness coverage spans every system feature (not just isolated bsd-make mechanics), includes actually executing built programs and verifying correct link resolution (not just successful compilation), and covers both nominal (expected-success) and error (expected-failure, verifying graceful diagnostics) cases |
| req | 0f87-6aa0-3c1c-ba14 | harness-tests-parent-ws-resolution-req | confirmed | Test cases cover PARENT_WS resolution comprehensively: workspace with no parents, a framework resolved only via a parent, and conflicting same-named frameworks/files present at multiple levels (local + parent, or multiple parents) verifying the documented precedence order is actually honored |
| req | 0f87-6aa0-3c33-cbda | harness-tool-presence-check-or-install-req | confirmed | The test harness verifies presence of every tool a test case needs before running it; missing tools are either auto-installed (where reasonable, e.g. via the host package manager) or reported with a specific, actionable diagnostic naming the tool and how to obtain it |
| req | 0f87-6aa0-3c48-5074 | harness-tests-full-cross-build-matrix-req | confirmed | The test harness builds, from each host, every first-class cross-compile target with every applicable toolchain -- the full (host x target x toolchain) matrix, not just host-native builds |
| req | 0f87-6aa0-3c61-0a27 | harness-vm-runtime-for-cross-targets-req | confirmed | For every cross-compiled target, the harness provisions/uses a VM of that target platform to actually execute the built binary and verify its runtime behavior; this is either fully automated (no user intervention) or clearly documented with the harness detecting VM-environment presence and pointing to setup docs when absent |
| req | 0f87-6aa0-3c7b-b243 | harness-native-builds-also-in-vms-req | confirmed | Native-host test suites also run inside a provisioned VM of that host OS (not directly on the ambient machine running the harness), using the same VM infrastructure built for cross-target runtime verification -- applied once that infrastructure exists |
| req | 0f87-6aa1-0496-b3d2 | graceful-failure-cross-cutting-req | confirmed | Every make target and macro validates its own inputs and directory-level context, failing with a clear, named diagnostic rather than a raw make/tool error or silent misbehavior |
| req | 0f87-6aa1-04b8-9a6d | windows-shlib-versioninfo-uniform-naming-req | confirmed | Windows/Cygwin/MinGW shared-library output uses a plain, unversioned filename and embeds SHLIB_MAJOR/SHLIB_MINOR versioning via a VERSIONINFO resource instead -- identically across all three Windows ABIs, with no Cygwin cyg-prefix special case |
| req | 0f87-6aa1-04d8-2e97 | linux-glibc-musl-arch-tiering-req | confirmed | Linux glibc is a first-class target on both amd64 and arm64; musl is first-class on amd64 only, with musl-arm64 explicitly not first-tier |
| req | 0f87-6aa7-bf76-6a71 | harness-target-matrix-reconciled-req | confirmed | The test harness's full target matrix is macOS-arm64 (host-native, gcc+llvm), FreeBSD-amd64 (14.4/15.0/15.1, gcc+llvm), NetBSD-amd64+arm64 (10.1/11.0, gcc+llvm), Linux-amd64+arm64 glibc (Debian 13.7, gcc+llvm), Linux-amd64 musl first-tier (Alpine 3.24.1, gcc+llvm; Linux-arm64 musl explicitly not first-tier), and Windows-amd64 tiered (tier 1: native MSVC + Cygwin gcc/llvm; tier 2: mingw-w64 gcc/llvm cross) |
| req | 0f87-6aa7-bf8d-4b6b | version-freshness-caching-policy-req | confirmed | bmake it checks the latest stable upstream release for every cached OS image and toolchain version; a cached version that is behind latest but still recent only warns that a newer version exists, it is not auto-replaced -- refreshing to latest requires the explicit force-supersede CLI target |
| req | 0f87-6aa7-bf9c-22f7 | harness-cache-management-cli-req | confirmed | The harness provides a CLI target to download a target's OS image to cache and build its VM environment, a CLI target to force-supersede a target's cache to the latest available version, and a CLI target to display per-target status: latest version upstream, version currently cached, and whether a VM is built |
| req | 0f87-6aa7-c4f7-414a | vm-nesting-reentrancy-guard-req | confirmed | A harness process running inside a VM must never itself launch another VM; any cross-testing it needs to perform reaches back to the host environment that launched it, not a newly-spawned nested VM |
| req | 0f87-6aa7-c89f-1b0a | cross-compile-universal-coverage-req | confirmed | Every supported host can cross-compile to every supported target, except where explicitly documented as impossible for a stated licensing or technical reason |
| req | 0f87-6aa7-c8a9-216c | cross-test-universal-coverage-req | confirmed | Every supported host can test (run and verify) binaries for every supported target, except where explicitly documented as impossible -- e.g. macOS as a guest is restricted to genuine Apple hardware under Apple's own licensing |
| req | 0f87-6aa7-c8b3-4034 | grid-infrastructure-alternative-to-vm-req | confirmed | The harness's build/test execution backend is pluggable: locally-provisioned VMs (the default/dev mechanism) or a grid of pre-identified dedicated virtual/physical machines (for a real production operational environment) can both serve as the place a given host/target build or test actually runs |
| req | 0f87-6aa7-dfa7-17a5 | doc-generation-doxygen-based-req | confirmed | Documentation is generated per-framework via Doxygen (Doxyfile + tag-file per framework, chained in PREREQS= order for cross-framework links), aggregated at the workspace level into a comprehensive name index, a framework-listing landing page, and an inheritance-hierarchy view, defaulting to public-header scope only |
| req | 0f87-6aa7-dfc9-38a1 | doc-generation-local-qa-trigger-req | confirmed | A distinct CLI trigger generates internal (public+local scope) documentation and escalates Doxygen's undocumented/incomplete-documentation warnings to hard failures, for use as an internal-documentation and quality-assurance-enforcement mechanism |
| req | 0f87-6aa7-e04d-c63d | doc-generation-invocation-scope-req | confirmed | bmake docs generates just the enclosing framework's own documentation when invoked from within a framework, or documentation for every framework present in the workspace (plus workspace-level aggregation) when invoked from the workspace root |
| req | 0f87-6aa7-e39b-037f | doc-pdf-docwith-explicit-grouping-req | confirmed | A framework's makefile may declare DOCWITH= naming other frameworks to bundle into one combined PDF (single Doxygen run, real working cross-references), instead of a per-framework PDF with dead cross-framework links; frameworks not named in any DOCWITH= get their own individual PDF |
| req | 0f87-6aa8-00f3-4ed1 | unit-test-feature-for-built-software-req | confirmed | A module or framework declares unit tests -- compiled test sources built and run, or bare scripts/programs invoked directly -- registered with a timeout and labels and checked by exit code, agnostic to what produces the pass/fail; results are reported as JUnit XML |
| req | 0f87-6aa8-0108-371a | unit-test-atf-kyua-adoption-req | confirmed | Unit tests are written against ATF (atf-c/atf-c++ for compiled cases, atf-sh for script cases) and run via Kyua, with Bmake It's own mk.test.mk providing the bsd.test.mk-style declarative registration (TESTS_C/TESTS_CXX/TESTS_SH-equivalent macros) natively, no CMake/CTest dependency |
| req | 0f87-6aa8-06c8-38b1 | sanitizer-support-req | confirmed | SANITIZE=<name>[,<name>...] selects compiler/linker sanitizer instrumentation, usable with both make run and make test; an invalid/mutually-exclusive combination fails loudly with a named error; toolchain/platform combinations that don't support a given sanitizer are explicitly documented as limits |
| req | 0f87-6aa8-06d2-77a9 | unit-test-full-suite-html-report-req | confirmed | Running the full test suite (bmake test with an all-tests invocation) generates a full HTML report alongside the JUnit XML output |
| req | 0f87-6aa8-0736-7365 | doc-generation-per-language-tool-pluggable-req | confirmed | The documentation-generation mechanism is pluggable per language: Doxygen is the tool for C/C++ specifically, and extending Bmake It's language support must be able to bring that language's own appropriate mainstream doc tool rather than being forced through Doxygen |
| req | 0f87-6aa8-0975-8512 | unit-test-per-language-tool-pluggable-req | confirmed | The unit-test mechanism is pluggable per language: ATF/Kyua is the tool for C/C++ specifically, and extending Bmake It's language support must be able to bring that language's own appropriate mainstream test tool rather than being forced through ATF |
| req | 0f87-6aa8-fef5-54d8 | per-level-local-mk-directory-req | confirmed | A workspace/framework/module's own mk/ directory is part of the .include search path at that level and every level nested inside it (workspace: WS/mk; framework: WS/mk + FW/mk; module: WS/mk + FW/mk + MOD/mk), with WS/mk's search extended to every PARENT_WS's own mk/ directory |
| req | 0f87-6aa9-003f-a123 | local-mk-hook-files-and-cascade-order-req | confirmed | At each visible level, Bmake It looks for pre.mk (included before role-file defaults are computed) and local.mk (included after), plus toolchain- and target-conditional variants (pre.${TOOLCHAIN}.mk, local.${TOOLCHAIN}.mk, pre.${TARGET}.mk, local.${TARGET}.mk); every level's matching file is included, not just the most specific, in workspace then framework then module order |
| req | 0f87-6aa9-0393-f302 | doc-project-metadata-conventions-req | confirmed | Doxygen's PROJECT_NAME/PROJECT_NUMBER/PROJECT_BRIEF/PROJECT_LOGO and a license-notice HTML_FOOTER are auto-detected from conventional workspace-root files (VERSION, LICENSE, docs/logo.{png,svg}) and silently omitted when absent; a workspace or framework's own pre.mk overrides any of them, no separate mechanism needed |
| req | 0f87-6aa9-39c9-34a5 | toolchain-detection-prefix-list-authoritative-req | confirmed | Toolchain compiler detection (mk.toolchain.llvm.mk/mk.toolchain.gcc.mk) checks the current OS's _TOOL_PREFIXES list first, in order, before falling back to ambient /Users/sylvain/miniconda3/bin:/Users/sylvain/miniconda3/condabin:/opt/local/bin:/opt/local/sbin:/usr/local/bin:/System/Cryptexes/App/usr/bin:/usr/bin:/bin:/usr/sbin:/sbin:/var/run/com.apple.security.cryptexd/codex.system/bootstrap/usr/local/bin:/var/run/com.apple.security.cryptexd/codex.system/bootstrap/usr/bin:/var/run/com.apple.security.cryptexd/codex.system/bootstrap/usr/appleinternal/bin:/pkg/env/global/bin:/Library/Apple/usr/bin:/Library/TeX/texbin:/Library/Frameworks/Mono.framework/Versions/Current/Commands:/Users/sylvain/.lmstudio/bin:/Users/sylvain/Library/pnpm:/Users/sylvain/Library/Application Support/Claude/local-agent-mode-sessions/skills-plugin/540095af-33d3-4b54-9071-8a4d7327ae1c/ae9804db-c018-421b-b62c-0d0869405214/bin via command -v; each per-OS mk.paths.<os>.mk lists prefixes in that OS's own correct priority order -- base system first everywhere except macOS, where third-party package managers (MacPorts, Homebrew, Nix, pkgsrc/Darwin) come before Apple's own /usr/bin |
| req | 0f87-6aa9-3a90-fa13 | macos-toolchain-tiers-req | confirmed | macOS _TOOL_PREFIXES searches tier 1 (MacPorts /opt/local/bin, Homebrew /opt/homebrew/bin + /usr/local/bin, pkgsrc/pkgin /opt/pkg/bin) before tier 2 (Nix ~/.nix-profile/bin + /run/current-system/sw/bin), with Apple's own /usr/bin last regardless |
| req | 0f87-6aa9-3ee5-3533 | macos-multi-pkgmgr-single-vm-req | confirmed | The harness's single macOS VM (tart-based) has MacPorts, Homebrew, pkgsrc/pkgin, and Nix all installed side by side; testing against a specific package manager means pointing _TOOL_PREFIXES/environment at that manager's own prefix for that test run, not provisioning a separate VM per manager |
| req | 0f87-6aa9-426f-c1e1 | macports-layer-package-list-req | confirmed | MacPorts layer of the macOS harness VM installs: bmake, llvm (clang/lld), gcc, docker, colima, lima, tart -- a complete toolchain+container+VM stack from one package manager, no Homebrew needed |
| req | 0f87-6aa9-448d-4154 | shared-lib-runtime-loadable-req | confirmed | A shared library built by mk.lib.mk must be loadable at runtime by any consumer that linked against it (test binaries, executables via LIBS=), without an install step or manually-set DYLD_LIBRARY_PATH/LD_LIBRARY_PATH -- via rpath, not a bare install name/soname alone |
| req | 0f87-6aaa-6025-a8fa | test-workspace-aggregation-req | confirmed | bmake test/test-all recurse across the whole workspace: invoked from a framework, run every module's declared tests; invoked from the workspace root, run every framework's modules and aggregate every module's HTML/JUnit report into one workspace-level dashboard linking to each |
| req | 0f87-6aaa-665e-fded | build-workspace-aggregation-req | superseded | A framework/workspace-level bmake all always captures each module's build output to a log file (<module>/build/<KEY>/build.log, tee'd so console output is unchanged) -- REPORT unset keeps today's fail-fast-on-first-error abort; REPORT=yes instead continues past a failing module so every module gets a chance to build, and afterward generates a workspace-level build-report/index.html dashboard (build-report/index.html) listing every framework/module with PASS/FAIL and a link to its build.log, mirroring the test dashboard |
| req | 0f87-6aaa-6971-23ad | test-single-selection-req | confirmed | TEST=<name> narrows a test run to one declared test (module: errors loudly if <name> doesn't match any of TESTS_CXX/TESTS_C/TESTS_SH; framework/workspace: pre-filters which modules get invoked at all, so a non-matching module is silently skipped, not an error). FW=<name> at workspace level narrows recursion to one framework. Both compose with REPORT= and SANITIZE= |
| req | 0f87-6aaa-6a5e-378f | build-workspace-aggregation-req-v2 | confirmed | A framework/workspace-level bmake all always captures each module's build output to a log file (<module>/build/<KEY>/build.log, tee'd so console output is unchanged). One module's build failure does not stop every other module from still being attempted, unless FAIL_FAST=yes (fail-fast-independent-of-report); REPORT=yes generates a workspace-level build-report/index.html dashboard listing every framework/module with PASS/FAIL and a link to its build.log, mirroring the test dashboard |
| req | 0f87-6aaa-72be-9e29 | run-history-not-overwritten-req | confirmed | build.log (module and framework rollup) and test's test-results.xml/test-report-html are written under a per-run runs/<RUN_ID>/ subdirectory of build/<KEY>/, not directly in it -- so re-running the same arch-toolchain target does not silently overwrite the previous run's logs/results. A runs/latest symlink always points at the newest RUN_ID. The workspace-level build-report/ and test-report/ dashboards are similarly written under <REPORT_DIR>/<RUN_ID>/ with their own latest symlink, and only ever scan the current run's own RUN_ID's artifacts, not older ones |
| req | 0f87-6aaa-877c-68d2 | run-history-pruning-deferred-req | confirmed | A retention/pruning mechanism for runs/<RUN_ID>/ history (delete everything older than a given time, or delete all) is needed eventually -- a long-running CI setup accumulates every run's logs/reports forever with only clean's blunt this-target-key-or-everything scope to reclaim space by hand. Deferred: not designed, not blocking current use |
| req | 0f87-6aaa-8c69-4170 | qt-development-without-plumbing-req | confirmed | Qt application development is supported without depending on Qt's own build-system plumbing (qmake, or CMake's Qt integration modules) -- only the real toolchain tools are invoked directly: moc, uic, rcc, and the C/C++ compiler/linker Bmake It already drives. Qt is a C++ toolkit, not a new language, so this sits within the existing C/C++ language scope, not an expansion of it |
| req | 0f87-6aaa-8c78-02af | qt-mk-mechanism-req | confirmed | mk.qt.mk: QT_MODULES=<names> (e.g. Core Widgets Gui, no Qt5/Qt6 prefix) resolves compile/link flags via pkg-config against Qt${QT_VERSION}<name> (QT_VERSION defaults 6, overridable to 5), the same pkg-config-based discovery pattern already used for atf-c++/atf-c -- not qmake -query. .ui and .qrc files are auto-discovered from src/ like .y/.l; MOC input is NOT a declared macro but auto-discovered by scanning the module's own include/*.h for Q_OBJECT/Q_GADGET/Q_NAMESPACE. Generated-file destinations closely mirror the existing yacc/lex convention (generated-headers-in-build-tree-req): .ui produces ui_<name>.h in the module's own build/<KEY>/obj/, copied to build/<KEY>/include/ (private by default, promotable via INCL= exactly like a yacc header); a Q_OBJECT header produces moc_<name>.cpp compiled straight from build/<KEY>/obj/ (no header, not INCL=-promotable, same as yacc's .c half); .qrc produces qrc_<name>.cpp the same way |
| req | 0f87-6aaa-8c78-60a0 | qt-progressive-test-harness-req | confirmed | A progressive-complexity Qt test harness verifies the mechanism level by level, each level headless/CI-runnable (no real display server required): Level 0 -- QtCore-only console app (QCoreApplication, QString, qDebug()), zero code generation, proves module discovery/link only; Level 1 -- adds a QObject-derived class with a signal/slot connected in a QCoreApplication event loop, proves MOC discovery+generation+link, still headless; Level 2 -- a QWidget+.ui-driven app run under QT_QPA_PLATFORM=offscreen (Qt's own supported headless platform plugin), proves UIC; Level 3 -- adds an embedded .qrc resource (e.g. an icon), proves RCC. QML/Qt Quick is out of scope for this initial harness -- a further, separate level if ever needed |
| req | 0f87-6aaa-d646-cf1d | qt | superseded | scripts/install-env.sh sets up MAKESYSPATH so any out-of-tree project can find Bmake It's mk/ files without per-invocation flags: user-level by default (appends an idempotent, clearly-delimited block to the right shell rc file for the invoking user's shell -- bash/zsh/POSIX sh, detected not guessed), --system on Linux (/etc/profile.d/bmake-it.sh, one file, no per-user dotfile edits, needs elevated privileges), --uninstall removes exactly what was added and nothing else. Prints what it is about to modify and requires explicit confirmation (or -y) before touching any file -- this is persistent-configuration-modifying, not a silent side effect of anything else. macOS and Windows system-level equivalents are not designed here -- user-level (which works everywhere, including those hosts) covers the gap for now |
| req | 0f87-6aaa-d65f-eeca | install-env-script-req | confirmed | scripts/install-env.sh sets up MAKESYSPATH so any out-of-tree project can find Bmake It's mk/ files without per-invocation flags: user-level by default (appends an idempotent, clearly-delimited block to the right shell rc file for the invoking user's shell -- bash/zsh/POSIX sh, detected not guessed), --system on Linux (/etc/profile.d/bmake-it.sh, one file, no per-user dotfile edits, needs elevated privileges), --uninstall removes exactly what was added and nothing else. Prints what it is about to modify and requires explicit confirmation (or -y) before touching any file -- this is persistent-configuration-modifying, not a silent side effect of anything else. macOS and Windows system-level equivalents are not designed here -- user-level (which works everywhere, including those hosts) covers the gap for now |
| req | 0f87-6ab5-7f6d-4ef5 | cxx-link-driver-selection-req | confirmed | mk.prog.mk/mk.lib.mk must link a module with ${CXX} when its SRCS contain a C++ source or LINK_CXX=yes is set, and with ${CC} otherwise |
| req | 0f87-6ab5-8197-16bf | aggregation-failure-exit-code-req | confirmed | mk.framework.mk/mk.workspace.mk's 'all' and 'test' must exit non-zero and name every failed child if any module/framework failed, even with FAIL_FAST=no |
| req | 0f87-6ab5-840b-56ae | header-dependency-tracking-req | confirmed | mk.prog.mk/mk.lib.mk must track per-source header dependencies for gcc/clang (-MMD -MP per object, conditionally included) so a header change triggers exactly the affected objects to rebuild; MSVC is an explicit, documented gap |
| req | 0f87-6ab5-841f-3f9a | compiler-native-dependency-tracking-inapplicable-to-own-recipes | confirmed | mk.prog.mk/mk.lib.mk define fully explicit ${_OBJDIR}/${_s:R}.o: ${.CURDIR}/src/${_s} compile rules with hand-written recipes, and never .include bsd.sys.mk, bsd.prog.mk, bsd.lib.mk, or any other real bsd.*.mk file -- confirmed by grepping every .include across mk.common.mk/mk.prog.mk/mk.lib.mk/mk.framework.mk/mk.workspace.mk. Real bsd.sys.mk's own -MD/-MF wiring (present on FreeBSD, confirmed absent from NetBSD's bundled mk-netbsd set, MacPorts status now moot) therefore never applies to this project's own compiled objects on any host -- header dependency tracking needs its own explicit mechanism, not inherited reuse. See header-dependency-tracking-req for what was actually built. |
| req | 6f38-6a88-1836-0f69 | default-build-host-os-arch | confirmed | The default build target is the OS/architecture of the machine running the build |
| req | 6f38-6a88-1836-1be8 | module-makefile-fields | superseded | A module's makefile.mk declares libraries to link with, the object type (static lib / shared lib / executable), and may provide additional compile or link options |
| req | 6f38-6a88-1836-352a | module-is-basic-link-unit | confirmed | A module is the basic link unit: it generates an executable, a static library, or a shared library |
| req | 6f38-6a88-1836-3d88 | build-obj-os-arch-output | superseded | Building a module generates an obj directory containing an OS_ARCH subdirectory; build output is placed there |
| req | 6f38-6a88-1836-5571 | output-naming-os-conventions | confirmed | Generated binaries and libraries follow host OS naming conventions: program NAME.exe on Windows / NAME on *NIX and macOS; libraries prefixed lib on *NIX (not on Windows) |
| req | 6f38-6a88-1836-74a5 | module-src-auto-discovery | confirmed | All source files found in a module's src directory are automatically taken as compilation inputs; the makefile does not need to enumerate them |
| req | 6f38-6a88-1836-74c9 | workspace-parent-search-order | confirmed | A workspace may declare an ordered list of parent workspaces; the build looks up missing elements in parent workspaces in declaration order |
| req | 6f38-6a88-1836-a7fa | framework-contains-modules | confirmed | A framework contains one or more modules |
| req | 6f38-6a88-1836-bfe8 | module-dir-naming-convention | confirmed | A module's directory is named NAME.m, where NAME is the module/library/program name unless overridden |
| req | 6f38-6a88-1836-c0f9 | default-toolchain-llvm | confirmed | The default C/C++ compiler is LLVM/clang; the legacy OS-native compiler (e.g. system gcc) may be used as an alternative |
| req | 6f38-6a88-1836-dd30 | framework-header-exposure | superseded | A framework exposes headers to the outside via its include directory, and to its own modules via a local/include directory |
| req | 6f38-6a88-1836-f948 | framework-prereqs | confirmed | A framework's makefile declares a prereqs list of other frameworks it depends on; prereq headers are accessible when building, and generated binaries can be linked against prereqs |
| req | 6f38-6a88-1836-fe77 | workspace-is-set-of-frameworks | confirmed | A workspace is a set of frameworks |
| req | 6f38-6a88-1892-f318 | module-resource-files | superseded | A module may carry resource files (non-source data) which are staged into the build output alongside the binary |
| req | 6f38-6a89-b79d-c3c3 | supported-languages | confirmed | The build system supports C, C++, and lex/yacc grammar sources (compiled to C/C++); other mkmk-supported languages (Java, Fortran, IDL, Express) are out of scope for now |
| req | 6f38-6a89-b810-3b02 | windows-hosts-cygwin-wsl | superseded | Cygwin and WSL are supported build hosts, treated as *NIX-like environments; no native MSVC/Windows-toolchain support is required |
| req | 6f38-6a89-ba51-340e | build-output-tree-structure | confirmed | Build outputs live under a build/$OS_ARCH/ directory at module, framework, and workspace levels; obj/ holds per-module intermediate object files, bin/ and lib/ hold final linked artifacts, keyed per target to support cross-compiling multiple targets from the same source tree |
| req | 6f38-6a89-baec-0f81 | packaging-native-formats-separate-target | confirmed | A dedicated make target builds native OS deployment packages (pkg/deb/rpm/msi/...) from a target's build output; packaging is not part of the default build |
| req | 6f38-6a89-baec-3053 | aggregation-step-copies-outputs | confirmed | A distinct build step copies each module's linked artifacts into its framework's and the workspace's build/$OS_ARCH/{bin,lib}, after module-level compilation and linking |
| req | 6f38-6a89-bb05-134c | variant-out-of-scope-for-dirtree | confirmed | $OS_ARCH identifies OS+architecture only; debug/release build variant does not get its own directory in the build tree |
| req | 6f38-6a89-bb64-0874 | phase1-concrete-targets | superseded | Phase 1 supports exactly four build targets: macOS/arm64 with llvm, macOS/arm64 with gcc, FreeBSD/x86_64 with llvm, FreeBSD/x86_64 with gcc |
| req | 6f38-6a89-bb64-3595 | target-key-includes-toolchain | confirmed | The build directory key (used throughout build/<KEY>/ at module, framework, and workspace levels) is a compound of OS, architecture, AND toolchain -- not OS+arch alone -- so different toolchains for the same OS/arch coexist in the same source tree |
| req | 6f38-6a89-bc24-f8a0 | resource-file-model | confirmed | Both modules and frameworks may carry resource files (non-source runtime data); resources support per-target variants with a fallback hierarchy, and are staged into build/<KEY>/ alongside obj/bin/lib, copied upward module to framework to workspace the same way as linked artifacts |
| req | 6f38-6a89-bccb-d5b8 | share-dir-fallback-hierarchy | confirmed | A module/framework resource source tree uses share/common/, share/<os>/, and share/<os>_<arch>/ subdirectories, most specific wins, with no toolchain axis |
| req | 6f38-6a89-bcf2-3f23 | share-overlay-copy-order | confirmed | Staging share/ resources for a target is a three-layer overlay copy: share/common/, then share/<os>/, then share/<os>_<arch>/, each layer overwriting same-named files from the previous one |
| req | 6f38-6a89-be33-cbe1 | target-key-string-format-req | superseded | The compound target key is formatted as <os>-<arch>-<toolchain>, e.g. macosx-arm64-llvm, freebsd-amd64-gcc; arch uses each platform native architecture name (arm64, amd64) at ISA-baseline granularity, not finer per-CPU-generation variants |
| req | 6f38-6a89-bea6-65e0 | os-label-set-req | superseded | The target key <os> component uses one of: macos, win, freebsd, linux; openbsd, netbsd, and Linux-distro-specific labels are deferred, not decided for phase 1 |
| req | 6f38-6a89-bec3-9ca3 | userland-only-scope | confirmed | The build system targets userspace/userland binaries and libraries only; kernel modules/drivers and Darwin/XNU-specific kernel development are out of scope for now |
| req | 6f38-6a8a-04e6-c63c | uniform-makefile-filename-req | confirmed | Workspace, framework, and module levels each use a file named literally 'makefile', which .includes a role-specific helper: <mk.workspace.mk>, <mk.framework.mk>, <mk.prog.mk>, or <mk.lib.mk> |
| req | 6f38-6a8a-04f2-45d5 | three-level-header-visibility-req | confirmed | Headers have three visibility levels: framework/include (public, to prereq-declaring frameworks), framework/local/include (to the frameworks own modules), and module.m/include (to that modules own sources only) |
| req | 6f38-6a8a-0550-79c4 | workspace-parent-macro-absolute-req | confirmed | A workspace's makefile declares PARENT_WS=<path1> <path2> ... , an ordered, space-delimited list of ABSOLUTE paths to parent workspace roots; relative paths are not used |
| req | 6f38-6a8a-0550-8bb8 | versioned-output-naming-req | confirmed | Module output naming supports an embedded version number following standard *nix conventions for versioned shared libraries (e.g. soname-style MAJOR.MINOR.PATCH suffixing) |
| req | 6f38-6a8a-0550-b9ed | packaging-per-format-targets-req | confirmed | Each native packaging format has its own named make target (e.g. make port, make pkg, ...); there is no single generic 'make package' target that takes a format parameter |
| req | 6f38-6a8a-0550-cadc | framework-prereqs-macro-req | confirmed | A framework's makefile declares PREREQS=fw1 fw2 ... , a space-delimited ORDERED list of frameworks it may link_with; all prereqs are implicitly public (no per-entry visibility qualifier); an incorrectly ordered list should produce a build error |
| req | 6f38-6a8a-0550-fc94 | module-build-macros-req | superseded | Module makefile variables: PROG=name (executable, mk.prog.mk), or LIB=name with optional LIB_SHARED=YES (library, mk.lib.mk); LINK_WITH=lib1 lib2 ... controls both header visibility and link-time linking |
| req | 6f38-6a8a-06bf-bede | build-output-mirrors-prefix-layout | confirmed | build/<KEY>/{bin,lib,share} mirrors a standard *NIX install-prefix layout for FreeBSD/Linux/MacPorts targets; macOS .app bundle output (a structurally different layout: Contents/MacOS, Contents/Frameworks, Contents/Resources) is a distinct, not-yet-designed alternative |
| req | 6f38-6a8a-06e9-e018 | app-bundle-deferred-req | confirmed | Phase 1 does not require macOS .app bundle output; macOS targets produce plain command-line executables/libraries via the standard bin/lib/share prefix layout |
| req | 6f38-6a8a-07b3-1a82 | win-target-via-cross-toolchain-req | superseded | Producing win-target output does not require running the build from within Cygwin; it can be cross-compiled from any capable host via a mingw-w64-class toolchain, same as any other cross-compile target; the exact mingw-vs-Cygwin toolchain distinction is deferred, not in scope for phase 1 |
| req | 6f38-6a8a-0c4d-d8e3 | target-key-omit-defaults-req | confirmed | The target key omits the toolchain suffix when toolchain=llvm (the default) and omits the ABI/libc suffix when it equals that OS default; both are shown explicitly only when non-default |
| req | 6f38-6a8a-0d9b-2b26 | linux-libc-default-glibc-req | confirmed | glibc is the default libc for Linux targets and is omitted from the target key; musl is always shown explicitly (e.g. linux-arm64-musl) |
| req | 6f38-6a8a-9181-0693 | prog-lib-default-to-module-name-req | superseded | When PROG= or LIB= is not set in a module's makefile, it defaults to the module's directory name with the .m suffix stripped |
| req | 6f38-6a8a-9181-1ebf | module-level-build-subdirs-req | confirmed | Module-level build/<KEY>/ contains obj/ (intermediate objects), bin/ or lib/ (the module's own linked artifact, matching whether it's a PROG or LIB module), and share/ (staged module resources) -- same subdirectory names as framework/workspace level |
| req | 6f38-6a8a-9181-8613 | module-name-case-sensitivity-req | confirmed | Module directory name case-sensitivity is whatever the host filesystem provides; the build system does not itself enforce or normalize case |
| req | 6f38-6a8a-9181-8b21 | versioned-naming-bsd-lib-mk-req | confirmed | Shared-library version numbering uses BSD make's own bsd.lib.mk conventions (e.g. SHLIB_MAJOR/SHLIB_MINOR-style macros), producing libfoo.so.X.Y with the standard symlink chain, rather than a project-invented scheme |
| req | 6f38-6a8a-9181-db38 | lib-shared-defaults-yes-req | superseded | In a module using mk.lib.mk, LIB_SHARED defaults to YES (shared library) when not explicitly set; static/archive libraries require explicitly opting out |
| req | 6f38-6a8a-943b-5c7e | lib-shared-no-is-static-req | superseded | Setting LIB_SHARED=NO in a library module's makefile builds a static/archive library instead of the default shared library |
| req | 6f38-6a8a-943b-a3af | libs-macro-link-only-headers-via-prereqs-req | confirmed | Module makefile uses LIBS=lib1 lib2 ... (dev and/or system libraries to link with, link-time only, replacing LINK_WITH). Header include-path resolution is driven solely by the containing framework's PREREQS -- LIBS has no role in header lookup |
| req | 6f38-6a8a-943b-c5a3 | prereqs-misorder-no-validation-req | confirmed | The build system does not validate PREREQS ordering or detect cycles; a misordered PREREQS list surfaces only as an ordinary header-not-found compile error, not a dedicated diagnostic |
| req | 6f38-6a8a-967a-e5b0 | headers-resolved-from-source-req | confirmed | Header search paths for a framework are built directly from the include/ directories of the frameworks listed in its PREREQS (resolved locally first, then via PARENT_WS in order); build/<KEY>/ is never consulted for headers |
| req | 6f38-6a8a-9a77-a8c9 | generated-headers-in-build-tree-req | confirmed | Generated headers are build artifacts, not source: private by default in module build/<KEY>/include/; explicitly-promoted public ones are copied up to a generated-only framework build/<KEY>/include/, which IS consulted (alongside fw/include/) when resolving a PREREQS frameworks headers; no workspace-level build/<KEY>/include/ is ever used for compilation |
| req | 6f38-6a8a-9b1e-1229 | incl-macro-promotes-generated-headers-req | confirmed | A module makefile declares INCL=<header1> <header2> ... to name which of its generated headers are promoted to public (copied up to the framework build/<KEY>/include/ on module build); unlisted generated headers remain module-private |
| req | 6f38-6a8a-9be8-14df | workspace-build-sequence-req | confirmed | The workspace-level build proceeds in order: resolve PARENT_WS, resolve each frameworks PREREQS (headers), build each module (compile-then-link-then-copy-up to framework), copy framework outputs up to workspace, with share/ resources following the same module-to-framework-to-workspace cascade |
| req | 6f38-6a8a-a40e-8342 | framework-build-order-follows-prereqs-graph-req | confirmed | Frameworks are built in a topological order derived from the workspace-wide PREREQS dependency graph: a framework only builds after all frameworks named in its PREREQS have completed building; behavior on an actual dependency cycle remains unspecified |
| req | 6f38-6a8a-d738-faf7 | sibling-module-ordering-link-only-req | superseded | Within a framework, module compilation (src/ to obj/) is fully order-independent/parallelizable since headers never come from sibling modules; only the link step of a module is ordering-constrained, and only with respect to sibling modules it names in LIBS= |
| req | 6f38-6a8a-d8b2-fbd8 | fused-build-per-module-full-dependency-order-req | confirmed | Modules and frameworks build via a fused compile+link (and header-generation) cycle, one unit at a time, in an order where a dependency (via LIBS, or via a consumed promoted-generated header) is FULLY completed before its dependent starts building at all |
| req | 6f38-6a8a-d9a5-6177 | shlib-major-minor-cross-platform-emission-req | confirmed | A library module declares SHLIB_MAJOR (and optionally SHLIB_MINOR); mk.lib.mk emits libfoo.so.MAJOR[.MINOR] with an ELF symlink chain on FreeBSD/Linux, and libfoo.MAJOR.dylib with a dylib symlink chain PLUS linker -compatibility_version/-current_version flags on macOS |
| req | 6f38-6a8a-dba2-dcc2 | modules-and-frameworks-auto-discovered-req | confirmed | A frameworks modules are discovered by scanning for *.m-suffixed immediate subdirectories; a workspaces frameworks are discovered by scanning immediate subdirectories -- neither is explicitly listed in the parent makefile |
| req | 6f38-6a8a-f6ee-940d | prereqs-mandatory-even-empty-req | confirmed | Every framework makefile must declare PREREQS= explicitly, even with no value, e.g. PREREQS= for a framework with no prerequisites |
| req | 6f38-6a8a-f6ee-e69f | framework-discovery-signal-req | confirmed | Workspace-level framework discovery treats an immediate subdirectory as a framework if it contains a makefile declaring PREREQS=; this is the sole discovery signal (build/ is excluded because it has no such makefile) |
| req | 6f38-6a8b-655d-b8d7 | target-selection-uses-bmake-native-vars-req | superseded | Non-default OS/architecture target selection on the make command line uses TARGET= and TARGET_ARCH=, matching real bsd make/FreeBSD cross-build variable naming; default (unset) means the host OS/arch |
| req | 6f38-6a8b-66e7-9fa6 | toolchain-cli-variable-req | superseded | Toolchain selection on the make command line uses TOOLCHAIN=<gcc|llvm>; unspecified defaults to the current default toolchain, llvm |
| req | 6f38-6a8b-67a0-9bd5 | toolchain-triggers-dedicated-mk-file-req | superseded | TOOLCHAIN=<gcc|llvm> selects and .includes a dedicated toolchain mk file (mk.toolchain.<name>.mk) that sets CC/CXX/LD/AR/AS and related tool choices for that toolchain; these remain individually overridable via normal make variable precedence |
| req | 6f38-6a8b-de94-3b56 | clean-target-scope-req | superseded | make clean removes only the current or explicitly-selected target build/<KEY>/ subtree (via TARGET=/TARGET_ARCH=/TOOLCHAIN=); TARGET=all clears build/ output for every target at once |
| req | 6f38-6a8b-df45-59b4 | install-target-exists-req | confirmed | make install copies the current targets build/<KEY>/{bin,lib,share} to a real install destination, distinct from the per-format packaging targets (make port, make pkg, ...); proposed to reuse bsd makes native DESTDIR/PREFIX variables, pending confirmation |
| req | 6f38-6a8b-dfb1-cc5e | destdir-prefix-confirmed-req | superseded | make install DESTDIR=<staging-root> PREFIX=<final-path> installs build/<KEY>/{bin,lib,share} into $(DESTDIR)$(PREFIX)/{bin,lib,share}, using bsd makes own native DESTDIR/PREFIX variables unchanged |
| req | 6f38-6a8c-0d67-2019 | install-requires-prebuilt-target-req | superseded | make install fails/refuses if the current (or selected) targets build/<KEY>/ output does not already exist -- it does not implicitly build first; cross-filesystem install is deferred, not designed |
| req | 6f38-6a8c-195e-457f | distrib-dir-for-packaging-staging-req | superseded | A workspace-level distrib/<KEY>/ directory, parallel to build/<KEY>/ and keyed the same way, is where packaging targets (make port, make pkg, ...) stage an install-prefix-shaped tree before building the actual per-format package artifact from it |
| req | 6f38-6a8c-1a1d-ed75 | packaging-invokes-install-into-distrib-req | superseded | Packaging targets stage distrib/KEY by invoking install with DESTDIR=distrib/KEY, reusing the install/DESTDIR/PREFIX mechanism rather than a separate copy step |
| req | 6f38-6a8c-1ba0-4647 | port-target-is-source-recipe-not-binary-package-req | superseded | make port produces a source-based build recipe (Makefile/distinfo/patches/pkg-plist) for the ports system to build from source, NOT a staged binary package; REQ-packaging-invokes-install-into-distrib-req applies only to binary-package-style targets (pkg, deb, rpm, msi) |
| req | 6f38-6a8c-2025-f48c | make-port-deferred-req | superseded | make port generation mechanism is deferred to a later work phase; only its category (source-recipe, not binary package) is settled for now |
| req | 6f38-6a8c-2159-4b07 | prereqs-resolution-cached-like-depend-req | confirmed | PARENT_WS and PREREQS resolution results are cached in a generated, auto-.included .mk fragment (modeled directly on real bsd makes .depend mechanism), not recomputed by walking the filesystem on every make invocation |
| req | 6f38-6a8c-b76b-0154 | mkdep-style-header-dependency-tracking-req | superseded | Each module maintains a generated, mkdep-style .depend file tracking which of its source files include which headers (transitively), so make only recompiles the .o files actually affected when a header changes |
| req | 6f38-6a8c-b939-38a7 | debug-release-via-native-bsd-flags-req | confirmed | Debug vs release build variant is controlled via real bsd makes own native flag variables (DEBUG_FLAGS for debug symbols, an optimization-level flags variable for release), not a new unified MODE= switch |
| req | 6f38-6a8c-b9f4-3c16 | flags-reuse-bsd-native-vars-req | superseded | A module supplies additional compile or link options by appending directly to bsd makes own native CFLAGS/CXXFLAGS/LDFLAGS/YFLAGS/LFLAGS variables in its makefile (e.g. CFLAGS+=-DFOO); no project-specific LOCAL_*FLAGS wrapper macros exist |
| req | 6f38-6a8c-bbfe-2001 | add-prereq-add-parent-convenience-targets-req | confirmed | make add-prereq FW=<name> appends <name> to the current frameworks PREREQS=; make add-parent WS=<abs-path> appends <abs-path> to the workspaces PARENT_WS=; both edit the makefile text directly rather than requiring hand-editing |
| req | 6f38-6a8c-beda-c0ba | tests-docs-versioning-deferred-req | confirmed | Test infrastructure, documentation generation, and package versioning/metadata are out of scope for the current design pass, deferred to later |
| req | 6f38-6a8d-1de3-1237 | copy-up-collision-diff-aware-req | confirmed | Copy-up collision handling is diff-aware: identical files copy silently; differing files that are a genuine (non-overlay) collision emit a warning but the build still proceeds (overwrite, not hard error); the deliberate share/ specificity overlay is exempted from warnings entirely since it is expected, by-design behavior |
| req | 6f38-6a8d-7578-0ea3 | lib-shared-is-new-not-reused-req | confirmed | LIB_SHARED=YES|NO is a NEW, project-specific macro (not a reuse of any native bsd.lib.mk mechanism); mk.lib.mk must translate it into the real bsd.lib.mk mechanism (SHLIB_MAJOR presence), and suppressing the static archive entirely (true shared-only output) needs custom logic since bsd.lib.mk has no native way to do that |
| req | 6f38-6a8d-75c1-0ba0 | target-arch-toolchain-selection-is-custom-req | superseded | TARGET=/TARGET_ARCH= variable NAMES are still reused (for familiarity with bsd make convention), but the actual cross-compiler SELECTION logic must be implemented by this project itself (in mk.toolchain.<name>.mk or a new mk.target.mk), not inherited from bsd.prog.mk/bsd.lib.mk, which do nothing with these variables outside FreeBSDs base-system build infrastructure |
| req | 6f38-6a8e-048e-f811 | prefix-is-new-not-reused-req | confirmed | PREFIX is a NEW, project-specific variable (not a reuse of any native bsd.prog.mk/bsd.lib.mk mechanism); mk.prog.mk/mk.lib.mk must derive the individual *DIR variables (BINDIR, LIBDIR, etc.) from it themselves, since bare bsd.prog.mk/bsd.lib.mk have no such derivation. DESTDIR remains correctly bsd-make-native, confirmed working as expected |
| req | 6f38-6a8e-71e6-f189 | toolchain-mk-must-set-explicit-cc-paths-req | superseded | mk.toolchain.<name>.mk must explicitly set CC/CXX/LD/AR/AS to concrete cross-compiler paths/binaries based on TARGET/TARGET_ARCH/TOOLCHAIN, following the same external-orchestration pattern every bsd-make lineage (FreeBSD Makefile.inc1, NetBSD build.sh) uses -- TARGET/TARGET_ARCH cannot be passed through to bsd.prog.mk/bsd.lib.mk expecting them to select a compiler themselves |
| req | 6f38-6a8e-7e12-a30d | dep-order-generation-mechanism-req | confirmed | Build order for both module-level (LIBS=) and framework-level (PREREQS=) dependency graphs is generated via: query each unit dependency macro in isolation (bmake -f <dir>/makefile -V <MACRO>), topologically sort (Kahn), ignore out-of-scope references, emit a generated SUBDIR= .mk fragment consumed by bsd.subdir.mk |
| req | 6f38-6a8f-5180-18f8 | run-target-req | confirmed | make run PROGRAM=<name> ARGS=<args>, invoked at workspace level, sets the platform-appropriate dynamic-library search path (LD_LIBRARY_PATH/DYLD_LIBRARY_PATH/etc.) to include workspace build/<KEY>/lib/, then executes build/<KEY>/bin/<PROGRAM> with the given arguments |
| req | 6f38-6a8f-5180-1a89 | help-target-req | confirmed | make help lists every valid make target with a description, and every valid variable with a description and its valid values (e.g. LIB_SHARED=YES|NO), scoped to what's applicable at the current directory level |
| req | 6f38-6a8f-519a-d42b | clean-removes-all-derived-artifacts-req | confirmed | make clean removes every generated/derived artifact for the selected target scope: build/<KEY>/, distrib/<KEY>/, per-module .depend files, the PARENT_WS/PREREQS resolution cache, and the dependency-order generation cache -- leaving the tree in a state indistinguishable from a fresh checkout |
| req | 6f38-6a8f-5206-3e89 | build-system-self-test-harness-req | confirmed | The Bmake It project itself has a script-driven test harness (not a make target) validating mk.*.mk behavior: test cases packaged as archives, each expanded into a fresh temp directory, built with bmake, result recorded against a declared expectation, temp directory deleted; documented per case |
| req | 6f38-6a91-03a8-6697 | mkdep-tool-works-wiring-must-be-custom-req | superseded | mk.prog.mk/mk.lib.mk must NOT rely on inherited bsd.dep.mk automatic depend: wiring, which is confirmed non-functional standalone in both tested lineages; instead they must implement a thin, explicit depend: target that invokes the standalone mkdep tool (or equivalent) directly and captures its output -- the underlying per-source-file header-scanning capability itself is confirmed to work correctly |
| req | 6f38-6a91-03ec-2589 | order-cache-auto-triggered-req | confirmed | make automatically regenerates the order cache (running the equivalent of make order) if it is missing or stale, before proceeding with the build; make order remains separately invocable on its own |
| req | 6f38-6a91-03ec-9f14 | order-target-blank-run-req | confirmed | make order performs a workspace-wide blank run (discovery + full PREREQS/LIBS dependency-graph resolution, no compilation or linking) and writes the topologically-sorted result to a cache; the build target consumes that cache rather than re-resolving order inline |
| req | 6f38-6a91-03ec-fc4e | link-time-not-yet-built-guard-req | confirmed | Before linking a module, mk.prog.mk/mk.lib.mk verify each LIBS= entry's build artifact actually exists; a missing one produces an explicit 'dependency not yet built' error naming the specific missing library, rather than an opaque linker failure |
| req | 6f38-6a91-4c89-ba40 | compiler-native-dependency-tracking-req | superseded | Per-source header dependency tracking is fully native and requires no custom wiring: bsd.sys.mk already passes -MD -MF.depend.<obj> to the compiler on every ordinary compile, producing correct per-object dependency files automatically, confirmed to correctly drive incremental rebuilds when a shared header changes. Supersedes the earlier REQ-mkdep-tool-works-wiring-must-be-custom-req, which was based on testing the wrong mechanism (the separate make depend target/mkdep tool, not the compiler-native path that actually matters) |
| req | 6f38-6a91-a2cf-5c3c | md-dependency-tracking-freebsd-specific-req | confirmed | Compiler-native -MD dependency tracking is confirmed to work automatically ONLY when using FreeBSD's own actual bsd.sys.mk (e.g. on a real FreeBSD host's native /usr/share/mk); it is confirmed ABSENT from NetBSD's own bundled mk set. Whether MacPorts' bsdmake (a phase-1 target host) includes it is unverified and must be checked on a real Mac before relying on it there |
| test | 0f87-6a9a-a9fa-3729 | test-cygwin-gcc-hello-win-amd64 | passing | Manually verified on a real Windows 11 arm64 VM (QEMU/hvf, Cygwin x86_64-under-emulation): bmake TOOLCHAIN=gcc built examples/myworkspace/Hello end to end -- greet.dll + greet.lib import lib (no 'lib' prefix), hello.exe, correct win-amd64-gcc target key; file(1) confirms genuine PE32+ x86-64 Windows executable; hello.exe actually runs and dynamically links greet.dll at runtime, printing the expected output; bmake run PROGRAM=hello.exe also verified working (PATH-based dynamic-library search var) |
| test | 0f87-6a9a-b50f-0b7d | test-msvc-static-hello-win-amd64 | passing | Manually verified on the real Windows 11 arm64 VM: bmake TOOLCHAIN=msvc LIB_SHARED=NO built examples/myworkspace/Hello end to end via cl.exe/lib.exe through msvc-cc-wrapper.sh/msvc-lib-wrapper.sh -- greet.lib (static archive via lib.exe), hello.exe correctly linked against it (-lgreet -> greet.lib resolved by our own link-time-not-built-guard); file(1) confirms genuine PE32+ x86-64 Windows executable (6 sections, MSVC-produced, distinct from gcc's 18-section output for the same source); hello.exe actually runs and prints the expected output. This is the first genuine, real-hardware confirmation that the MSVC toolchain wiring itself (cygpath path translation, gcc-style-to-cl.exe/lib.exe flag translation, vcvarsall.bat environment activation) works end to end |
| test | 0f87-6a9a-bb2c-9372 | test-msvc-shared-hello-win-amd64 | passing | Manually verified on the real Windows 11 arm64 VM: after adding bmk_export.h + mk.lib.mk's auto <LIB>_BUILDING define and updating greet.h to use the BMK_DLLEXPORT/BMK_DLLIMPORT pattern, bmake TOOLCHAIN=msvc (default LIB_SHARED=YES, the shared-DLL path that previously produced no import library) now genuinely creates greet.dll + greet.lib + greet.exp via link.exe's 'Creating library ... and object ...' output; hello.exe links against greet.lib successfully and, at runtime, actually loads greet.dll and prints the expected output. Closes msvc-shared-lib-export-req |
| test | 0f87-6a9a-d35b-6e0a | test-msvc-vcvarsall-auto-win-amd64 | passing | Manually verified on the real Windows 11 arm64 VM: bmake TOOLCHAIN=msvc invoked from a plain SSH-spawned Cygwin shell (command -v cl confirmed empty beforehand, no vcvarsall.bat ever called) built the entire Hello framework end to end via mk.vcvarsall.mk's own vswhere.exe-based discovery -- greet.dll+greet.lib+greet.exp and hello.exe, both framework passes, clean exit 0; hello.exe confirmed as genuine PE32+ x86-64 and actually runs, printing the expected output |
| uc | 0f87-6aaa-6432-66a7 | integration-manager-ci-cycle | proposed | An integration manager retrieves modifications from version control, builds and tests all code across the workspace (producing a reviewable report for each), periodically also runs sanitizer-instrumented tests, and publishes every generated report to a team-accessible LAN site |
