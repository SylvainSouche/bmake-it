---
schema_version: 2
id: 0f87-6a94-696d-1192
alias: prog-lib-default-to-module-name-req-v2
type: req
status: superseded
origin: view-drift review after v1->v2 migration, 2026-08-30
created: 2026-08-30
facets: [architecture]
rel_decided_by: [0f87-6a94-6957-6170]
rel_refines: [0f87-6a94-678e-e640, 6f38-6a88-1836-bfe8]
rel_supersedes: [6f38-6a8a-9181-0693]
generator: model.py/2.1.0
checksum: 026062b79fa5
---

# When PROG= or LIB= is not set in a module's makefile, it defaults to the module's directory name with the .m suffix stripped

Supersedes REQ-prog-lib-default-to-module-name-req (unchanged content, re-pointed ancestor only). Refines REQ-module-prog-or-lib-macro-req and REQ-module-dir-naming-convention: a module directory named NAME.m defaults to PROG=NAME (if .include <mk.prog.mk>) or LIB=NAME (if .include <mk.lib.mk>) when the corresponding macro is left unset in the module's makefile.

Why this exists as a supersession rather than an edit: the original refined REQ-module-build-macros-req, which the v1->v2 migration correctly marked superseded without updating objects that refined it (DEC-view-drift-refines-repoint-dec). REQ-module-build-macros-req was itself split across two successors (REQ-module-prog-or-lib-macro-req for the PROG=/LIB= facet this REQ concerns, and REQ-libs-macro-link-only-headers-via-prereqs-req for the unrelated LINK_WITH=/LIBS= facet) -- `model.py supersede` correctly refused to guess which heir applies and required this refines edge to be added explicitly, added via `model.py link` to REQ-module-prog-or-lib-macro-req. No content changed; only the refines target moved to the correct live successor.
