---
schema_version: 2
id: 6f38-6a8c-30a5-87fe
alias: cache-mechanism-needs-empirical-check
type: obs
status: recorded
origin: user statement (clarifying question, 2026-08-22)
created: 2026-08-24
facets: [architecture]
generator: migrate.py/v2
checksum: 8b88775b3b02
---

# Exact staleness-detection/regeneration-trigger mechanism for the PARENT_WS/PREREQS cache is deferred until real bsd.*.mk and FreeBSDs build.*.mk infrastructure can be checked hands-on for what they actually provide

User deferred the open follow-up from REQ-prereqs-resolution-cached-like-depend-req (exact generated filename, staleness detection, regeneration trigger) explicitly until implementation time, when the real bsd.*.mk family and FreeBSDs related build.*.mk infrastructure can be inspected empirically to see what mechanisms they already provide that can be reused directly, rather than designing this in the abstract now. Consistent with CON-use-bsdmakes general principle of reusing existing bsd.*.mk infrastructure where it fits -- in this case, the plan is to verify fit before finalizing the design rather than assume it.
