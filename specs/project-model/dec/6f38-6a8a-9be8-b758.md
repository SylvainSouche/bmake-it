---
schema_version: 2
id: 6f38-6a8a-9be8-b758
alias: workspace-build-sequence
type: dec
status: recorded
origin: user statement (clarifying question, 2026-08-22)
created: 2026-08-23
facets: [architecture]
generator: migrate.py/v2
checksum: 826b797af70d
---

# Consolidated end-to-end build sequence at workspace level: resolve PARENT_WS, resolve each frameworks PREREQS, build each module (compile to obj, link to module bin/lib) and copy up to framework bin/lib, then copy up to workspace bin/lib; resources follow the same module-to-framework-to-workspace cascade

User walked through the overall build sequence explicitly, tying together previously-separate pieces into one ordered flow: (1) resolve PARENT_WS to establish the fallback workspace search list (REQ-workspace-parent-macro-absolute-req); (2) resolve each frameworks PREREQS to establish header search paths (REQ-framework-prereqs-macro-req, REQ-headers-resolved-from-source-req, REQ-generated-headers-in-build-tree-req) -- no cycle/order validation performed (REQ-prereqs-misorder-no-validation-req); (3) for each module: compile all present src/ files to module build/<KEY>/obj/, link into module build/<KEY>/bin/ or lib/ (REQ-module-build-macros-req, REQ-module-level-build-subdirs-req), then copy the result up to the framework build/<KEY>/bin/lib/ (REQ-aggregation-step-copies-outputs); (4) copy framework build/<KEY>/bin/lib/ up to workspace build/<KEY>/bin/lib/; (5) resource files (share/) follow the same module-to-framework-to-workspace cascade, each level applying its own 3-layer overlay (REQ-share-overlay-copy-order) before being copied up (REQ-resource-file-model). This is primarily a consolidation of already-confirmed pieces into one explicit ordered sequence, not new design -- recorded so the end-to-end flow exists as a single reference rather than only being reconstructable by cross-referencing many separate REQs.
