---
id: 6f38-6a8a-f6ee-245c
alias: framework-discovery-via-prereqs-in-makefile
type: dec
status: recorded
origin: user statement (clarifying question, 2026-08-22)
created: 2026-08-23
facets: [architecture]
relationships: {decides: [REQ-6f38-6a8a-f6ee-e69f]}
---

# A workspace subdirectory is recognized as a framework if it contains a makefile declaring PREREQS= (now mandatory); typical framework structure also includes module (*.m) subdirectories and/or a share/ directory, though the makefile+PREREQS check is the actual discovery signal

Resolves REQ-modules-and-frameworks-auto-discovered-reqs open discrimination-signal follow-up: a workspace-root subdirectory is treated as a framework if it has a file named makefile that declares PREREQS= (now mandatory per REQ-prereqs-mandatory-even-empty-req). build/ is naturally excluded since it contains no such makefile. User also noted a framework typically has module (*.m) subdirectories and/or a share/ directory -- this is expected/typical structure, not itself part of the discovery test; the makefile+PREREQS check is the actual discriminator.
