# LIB unset — dir is libgreet.m -> LIB=greet (prog-lib-default-to-module-name-req-v3:
# '.m' suffix and a leading 'lib' are both stripped from the default).
LIB_SHARED=YES
SHLIB_MAJOR=1
.include <mk.lib.mk>
